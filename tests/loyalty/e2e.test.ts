import { describe, it, expect } from 'vitest';

describe('Loyalty Bonus E2E', () => {
  it('customer sees bonus offer and selects free item', async () => {
    // Placeholder: full E2E would use Playwright
    // 1. Navigate to /order/frankys-pasta
    // 2. Add items to cart
    // 3. Check eligibility (should call /api/loyalty/eligibility)
    // 4. Banner shows "You're eligible for free X"
    // 5. Click Wählen
    // 6. Modal opens with options
    // 7. Select item
    // 8. POST /api/loyalty/redeem
    // 9. Proceed to checkout
    // 10. Free item appears with price 0.00
    expect(true).toBe(true);
  });
});
