'use client';
import { useState } from 'react';
import Link from 'next/link';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
export function TrainingRunner({ progress }: { progress: any }) { const lessons = progress.module?.inhalt?.lessons ?? []; const [answers, setAnswers] = useState<Record<string, string[]>>({}); const [result, setResult] = useState<any>(); const [error, setError] = useState(''); const [pending, setPending] = useState(false); async function submit() { setPending(true); const response = await fetch(`/api/training/my/${progress.id}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ answers }) }); const data = await response.json(); setPending(false); if (!response.ok) setError(data.error); else setResult(data); }
  if (result) return <Card><CardContent className="p-8 text-center"><h1 className="text-2xl font-bold">{result.passed ? 'Schulung bestanden' : 'Noch nicht bestanden'}</h1><p className="mt-2">Ergebnis: {result.score} %</p><Link href="/mitarbeiter/schulungen"><Button className="mt-5">Zu meinen Schulungen</Button></Link></CardContent></Card>;
  return <div className="space-y-4"><Link href="/mitarbeiter/schulungen" className="text-sm text-muted-foreground">← Meine Schulungen</Link><header><h1 className="text-2xl font-bold">{progress.module?.titel}</h1>{progress.module?.beschreibung && <div className="mt-2 text-muted-foreground" dangerouslySetInnerHTML={{ __html: progress.module.beschreibung }} />}</header>{lessons.map((lesson: any, index: number) => <Card key={lesson.id}><CardHeader><CardTitle className="text-base">{index + 1}. {lesson.title ?? lesson.question}</CardTitle></CardHeader><CardContent>{lesson.type === 'text' ? <p className="whitespace-pre-wrap">{lesson.body}</p> : lesson.type === 'image' ? <img src={lesson.url} alt={lesson.title} className="max-h-96 rounded object-contain" /> : lesson.type === 'video' ? <a className="font-semibold text-indigo-700 underline" href={lesson.url} target="_blank" rel="noreferrer">Video öffnen</a> : lesson.type === 'document' ? <a className="font-semibold text-indigo-700 underline" href={lesson.url} target="_blank" rel="noreferrer">Dokument öffnen</a> : <div className="space-y-2">{lesson.options.map((option: any) => <label key={option.id} className="flex min-h-12 items-center gap-3 rounded border p-3"><input type="checkbox" checked={(answers[lesson.id] ?? []).includes(option.id)} onChange={(e) => setAnswers({ ...answers, [lesson.id]: e.target.checked ? [...(answers[lesson.id] ?? []), option.id] : (answers[lesson.id] ?? []).filter((id) => id !== option.id) })} />{option.label}</label>)}</div>}</CardContent></Card>)}{error && <p className="rounded bg-red-50 p-3 text-sm text-red-800">{error}</p>}<Button size="lg" className="w-full" disabled={pending || lessons.filter((l: any) => l.type === 'quiz').some((l: any) => !answers[l.id]?.length)} onClick={submit}>{pending ? 'Wird ausgewertet…' : 'Schulung abschließen'}</Button></div>; }
