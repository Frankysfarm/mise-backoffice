import { PairingClient } from './client';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Kasse verbinden · Mise' };

export default function POSPairPage() {
  return <PairingClient />;
}
