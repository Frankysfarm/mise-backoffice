export const metadata = {
  title: 'FoodFlow — Ein Cockpit für dein Restaurant',
  description:
    'Dienstplan, Lager, Kasse, Lieferung, Lieferando-Anschluss — alles in einem System. 14 Tage alle Module gratis.',
};

export default function WelcomeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
