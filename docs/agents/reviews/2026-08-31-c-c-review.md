# Claude Review — Packet C-C (Wiederkehrende Aufgaben + Übergaben)

- Branch: `vollausbau/c-c`, Diff: `e401d12..52e8936` (13 files, +575/-3)
- Packet: `docs/agents/tasks/2026-08-31-c-c-aufgaben-uebergaben.md`
- Report under review: `docs/agents/reports/2026-08-31-c-c.md`
- Read-only review. No source file was modified.

## VERDICT

**VERDICT: FAIL** — 4 BLOCKER, 9 MAJOR, 11 MINOR, 5 NOTE.

## Verified gate results (run by the reviewer in this worktree)

| Command | Result |
| --- | --- |
| `./node_modules/.bin/vitest run --configLoader native` | **exit 0** — `Test Files 35 passed (35)`, `Tests 231 passed (231)`, duration 6.08s |
| `./node_modules/.bin/tsc --noEmit -p .` | **exit 0**, no diagnostics |
| `./node_modules/.bin/next build` | not run (excluded by the review instruction) |
| `scripts/tests/077_recurring_tasks_handovers.sql` | not run — no database on this host |
| Playwright | not run — the packet's own spec self-skips without `E2E_AUTH_STATE` |

The report's numeric test claims (231/231, tsc exit 0, Playwright not green, SQL smoke not
executed) match what I observed and are honest. Note that `--configLoader native` works
here, so the report's statement that `--configLoader runner` is *required* is not
reproducible; it is a harmless inaccuracy.

---

## BLOCKER

### B1 — `materialize_recurring_operational_tasks` has no tenant/location scope and is reachable from a user route
`supabase/migrations/20260831120000_recurring_tasks_and_handover_ack.sql:124` (called from `app/api/operations/tasks/route.ts:47`)

The cursor is `select * from public.operational_task_templates where trigger_type='manual' and aktiv and paused_at is null and deleted_at is null` — no `tenant_id`/`location_id` predicate, and the function accepts no tenant argument. The API route validates the caller's location (`route.ts:25-29`) and then discards `input.locationId` entirely. Failure mode: a manager of tenant A pressing „Jetzt erzeugen" writes `operational_tasks` rows into **every other tenant and location in the database**, and the returned count reflects foreign tenants' data. This is the exact scope violation coordination rule 1 forbids.
*Fix:* add `p_tenant_id uuid, p_location_id uuid` parameters, filter the template cursor on both, and pass the already-validated `actor.tenant_id` / `input.locationId` from the route (the cron path may loop per tenant).

### B2 — „Individueller Rhythmus" materializes daily; the owner-facing preview disagrees with the database
`supabase/migrations/20260831120000_recurring_tasks_and_handover_ack.sql:105,112` vs `lib/operations/recurrence.ts:63`

`v_anchor := coalesce((p_rule->>'anchor')::date, p_day)`. The client never puts `anchor` into `recurrence_rule` (`operations-client.tsx:16` builds `{kind,time,weekdays,day,ordinal,weekday,interval,unit}`), and `save_recurring_operational_template` stores the rule verbatim (`migration:88`) — it writes the anchor only into the unused `recurrence_anchor` **column**, which `recurring_template_due_at` never reads. So `v_anchor = p_day` and `(p_day - v_anchor) % N = 0` is **true on every day**. Failure mode: owner selects „alle 2 Tage", the preview correctly shows 31.08./02.09./04.09., and the materializer then creates the task on 31.08., 01.09., 02.09., … — 14 duplicate-looking tasks per fortnight instead of 7. The same fallback makes `weekly` degenerate to daily for any rule saved without `weekday`.
*Fix:* pass `v_template.recurrence_anchor` into `recurring_template_due_at` and use it as the interval origin (`(p_day - v_anchor::date) % N`), and unit-test the SQL, not only the TypeScript twin.

### B3 — Two incompatible handover acknowledgement paths on the same table; the legacy path destroys the audit chain
`supabase/migrations/20260831120000_...sql:152-156` vs `app/api/operations/responsibility/route.ts:336-337`, list filters at `app/mitarbeiter/page.tsx:153` and `app/(neo)/neo/app/mitarbeiter/page.tsx:63`

The pre-existing „Übergabe annehmen" button (`app/mitarbeiter/my-operations.tsx:73`, `app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:323`) still writes `status='angenommen', accepted_at, accepted_by` directly, skipping `read_at`/`confirmed_at`. Failure mode A: a handover created in the new UI and accepted in the Mitarbeiter-App shows forever as **„Ungelesen"** with an amber unconfirmed border in the new Übergabe-Audit, and the packet's required chain (wer gelesen, wer bestätigt, wann) is permanently empty for it. Failure mode B: the reverse — the new „Gelesen" action sets `status='gelesen'`, which is **not** in the `.in('status',['offen','angenommen'])` filters of either Mitarbeiter view, so the handover silently disappears from the recipient's app, and the legacy `accept_handover` (`.eq('status','offen')`, `route.ts:337`) then returns 409 „Übergabe wurde bereits bearbeitet." This violates coordination rule 5 (no parallel systems) as well as owner point 4.
*Fix:* route the legacy `accept_handover` through `acknowledge_responsibility_handover` (read+confirm in one call) and add `'gelesen'` to both Mitarbeiter status filters.

### B4 — The incoming shift cannot acknowledge a handover: the only UI is manager-plus
`app/(neo)/neo/app/ablaeufe/aufgaben/page.tsx:9`

`requireManagerPlus()` allows only `manager|backoffice|admin` (`lib/auth/requireRole.ts:15-17`); `teamleiter`, `mitarbeiter` and all service roles are redirected to `/login?reason=forbidden`. The new „Gelesen"/„Bestätigt" buttons (`operations-client.tsx:27`) exist nowhere else, and nothing was added to `app/mitarbeiter/*`. Failure mode: the Folgeschicht — the persona the owner explicitly names in point 4 and the report's own „Mobile — Folgeschicht/Mitarbeiter" click path — literally cannot open the page it is told to open, so „Gelesen + Bestätigt mit Person und Zeitstempel" is unreachable in production. The API allows these roles; only the UI gate blocks them.
*Fix:* surface the read/confirm buttons in the existing `app/mitarbeiter` handover card (which already renders for the recipient) rather than only behind `requireManagerPlus`.

---

## MAJOR

### M1 — New `create_handover` endpoint bypasses the existing role gate on the same table
`app/api/operations/tasks/route.ts:51-54`

The pre-existing writer requires manager **or** department lead (`app/api/operations/responsibility/route.ts:317-318`). The new action performs no role check at all, so any authenticated employee — `mitarbeiter`, `server`, `cook`, kiosk accounts — can insert a binding handover record naming an arbitrary colleague as recipient. Since no non-manager UI exists (see B4), the loosened check buys nothing and only removes a deliberate control.
*Fix:* apply the same `manager || isDepartmentLead(actor, departmentId)` check before the insert.

### M2 — „Pausieren" and „Löschen" leave up to 14 days of already-materialized instances
`app/api/operations/tasks/route.ts:40` and `supabase/migrations/20260831120000_...sql:119`

The materializer runs on a 14-day horizon every 3 minutes (`vercel.json:8-11`), so at any moment ~14 days of future instances exist. Pause/delete only flips `aktiv`/`paused_at`/`deleted_at` on the template and never touches `operational_tasks`. Failure mode: the owner pauses „Kaffeemaschine reinigen", and the task keeps appearing, escalating and e-mailing staff daily for two weeks — the control looks broken.
*Fix:* on pause/delete also cancel future instances (`update operational_tasks set status='storniert' where template_id=… and recurrence_due_at > now() and status='offen'`).

### M3 — Weekday numbers are rendered raw in the owner UI
`lib/operations/recurrence.ts:71,74`

`recurrenceLabel` returns `Wochentage: 1, 3, 5` and `Monatlich: 1. Wochentag`. These ISO-weekday integers are shown as the headline badge of every rule card (`operations-client.tsx:25`). Owner point 7 forbids technical data in the normal UI, and „1. Wochentag" is not meaningful German.
*Fix:* map to `['Mo','Di','Mi','Do','Fr','Sa','So']` and render „Jeden 1. Montag im Monat".

### M4 — Weekly and monthly-weekday recurrences are hard-wired to Monday
`app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:24` (conditional fields), `:12` (`weekday:1, ordinal:1`)

The form renders extra inputs only for `weekdays`, `monthly_day` and `interval`. Choosing „Wöchentlich" or „Monatlich · erster Montag" leaves `weekday`/`ordinal` at their defaults, so every such rule fires on Monday / the first Monday, with no way to pick another day and no indication in the label.
*Fix:* show the weekday chooser for `weekly` and a weekday+ordinal pair for `monthly_weekday`.

### M5 — Übergabe-Audit has no date-range filter; the owner is told to edit the URL
`app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:27`

The copy reads „Standort und Zeitraum lassen sich über die Seitenadresse filtern." The page reads `from`/`to` from `searchParams` (`page.tsx:18-19`) but renders no date inputs. The packet requires „Filter by Standort and date range"; asking an owner to hand-edit query strings is exactly the technical knowledge the owner order rules out.
*Fix:* add two `<input type="date">` fields that push `?from=…&to=…`.

### M6 — Handover „offene Aufgaben" are captured but never shown to the incoming shift
`app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:26` (write) vs `:27` (audit render)

`open_task_ids` is collected (all open tasks preselected) and stored, but the audit card renders only `incidents`, `inventory_notes`, `damage_notes`, `cleaning_notes`, `important_notes`. Failure mode: the Folgeschicht confirms a handover without ever seeing the open tasks it is confirming — the first bullet of owner point 4 produces write-only data.
*Fix:* join the task titles for `open_task_ids` in `page.tsx` and list them in the audit card.

### M7 — Raw database exception text is surfaced as the owner-facing error banner
`app/api/operations/tasks/route.ts:63` → `operations-client.tsx:17,21`

The catch-all returns `error.message` verbatim, and the client renders `result.error` in the red banner. Failure mode: two managers edit concurrently and the second sees the red banner „template outside location"; a constraint violation shows `new row for relation "operational_task_templates" violates check constraint …`. English/technical strings in a German owner UI.
*Fix:* log the raw message server-side and return a fixed German sentence (the sibling helper at `app/api/operations/responsibility/route.ts:352-357` sanitises only a few patterns and is not a sufficient model).

### M8 — Every escalation e-mails each recipient twice
`app/api/cron/operational-escalations/route.ts:38,41`

The lookback is `now - 5 min` while the cron fires every 3 minutes (`vercel.json:10`), so a task whose `last_escalated_at` was just set matches on the current run **and** the next one. With three escalation levels that is six e-mails per overdue task, with no outbox/dedupe row.
*Fix:* make the window match the cadence (or persist a `escalation_email_sent_at` marker and filter on it).

### M9 — The ack-state-machine unit test exercises dead code, not the shipped behaviour
`lib/operations/handover-state.ts:3`, `tests/operations/recurring-tasks-handovers.test.ts:24-30`

`advanceHandover` is imported by the test only — no route, page or component uses it. Production acknowledgement lives in `acknowledge_responsibility_handover` (`migration:144`), which behaves **differently**: the TS twin treats a repeated „read" as idempotent (`readAt: state.readAt ?? at`), the SQL falls through to `raise exception 'invalid handover action'`. The packet asked for tests of the ack state machine; the green test provides no evidence about what actually ships.
*Fix:* delete `handover-state.ts` and cover the RPC in `scripts/tests/077_…sql`, or make the route call the TS machine.

---

## MINOR

- `app/(neo)/neo/app/ablaeufe/aufgaben/page.tsx:18-19` — `+02:00` is hard-coded for the audit range, so `?from=…` is off by one hour for all winter dates. *Fix:* build the boundary with the Berlin zone instead of a literal offset.
- `supabase/migrations/20260831120000_...sql:152` — re-sending „read" when `read_at` is already set falls into the `else` branch and raises `invalid handover action` (HTTP 409) instead of being a no-op; the report's own manual step 3 expects a no-op. *Fix:* `if p_action='read' then … end if;` with an internal null-guard.
- `supabase/migrations/20260831120000_...sql:57` vs `supabase/migrations/20260830154500_daily_clarity_automation.sql:76` — at level 1 both the sweep and the new trigger notify `accountable_employee_id`, so that person gets two identical in-app notifications per escalation. *Fix:* exclude `new.escalation_owner_employee_id` in the trigger's recipient set.
- `supabase/migrations/20260831120000_...sql:92,96` — saving an edit always sends `active:true`, and the upsert resets `paused_at` and forces `deleted_at=null`; a paused or deleted rule is silently revived by „Bearbeiten". There is also no explicit „Fortsetzen" button. *Fix:* add a `resume_rule` action and stop clearing `deleted_at` on update.
- `app/(neo)/neo/app/mitarbeiter/page.tsx:55-58` — the Schichtabläufe template list filters `trigger_type='shift'` but not `deleted_at is null`, so rules of kind Schicht/Öffnung/Schließung deleted in the new page stay visible and re-editable there. *Fix:* add `.is('deleted_at', null)`.
- `app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:27` — the copy claims „Unbestätigte Übergaben stehen oben", but `page.tsx:25` orders by `created_at desc` only. *Fix:* sort unconfirmed first client-side or state the real ordering.
- `app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:23,18` — „idempotent", „Materialisierung" and „bereit zur Materialisierung" are engineering jargon in an owner-facing surface. *Fix:* „Ein zweiter Klick legt dieselbe Fälligkeit nicht noch einmal an."
- `app/api/cron/operational-escalations/route.ts:40` — the „Vertretung" recipient is `delegated_from_employee_id`, i.e. the person who delegated the task, not the `stellvertretung` from `department_responsibility_assignments` that the in-app trigger correctly uses. *Fix:* resolve the deputy the same way the trigger does.
- `app/api/cron/operational-escalations/route.ts:47` — `${task.title}` is interpolated into the HTML mail body unescaped, and the CTA uses the relative href `/mitarbeiter#meine-aufgaben`, which is not clickable in a mail client. *Fix:* HTML-escape the title and use an absolute `NEXT_PUBLIC_APP_URL` link.
- `supabase/migrations/20260831120000_...sql:103` — `recurring_template_due_at` is declared `immutable` although it depends on the `Europe/Berlin` zone definition; it should be `stable`. *Fix:* change the volatility marker.
- `app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:15,26` — all open tasks (up to the 200-row limit from `page.tsx:24`) are preselected as checkboxes in one flat list; on a busy location the handover form becomes a 200-item wall on a phone. *Fix:* group by Bereich and preselect nothing above ~20 items.
- `app/api/operations/tasks/route.ts:10` — `recurrenceRule` is accepted as an opaque `z.record(z.unknown())`; the TypeScript `validateRecurrenceRule` (`lib/operations/recurrence.ts:40`) is never applied server-side, so shape validation depends entirely on the RPC's `kind`-only check. *Fix:* parse with the existing validator before calling the RPC.

## NOTE

- Coordination rules are otherwise respected: migration prefix `20260831120000` is inside the reserved C-C range, `app/(neo)/neo/app/shell.tsx` and the layout are untouched, and `app/(neo)/neo/app/ablaeufe/page.tsx` changes exactly one existing card line. Migration is additive with explicit `begin/commit`, `add column if not exists`, `create … if not exists`, and re-runnable constraint/trigger guards.
- The `security definer` functions all set `search_path=public,pg_temp`; grants correctly keep `materialize_recurring_operational_tasks` off `authenticated`. RLS on `responsibility_handovers` and the operational tables is unchanged and remains tenant+location scoped; the new columns inherit it. Every new read path uses the service client but filters `tenant_id` **and** `location_id` explicitly — reviewed and correct in `page.tsx:21-25`.
- Reuse is genuine at the data layer: no second handover or task table was created, and shift/opening/closing rules are routed into the existing `trigger_type='shift'` materializer instead of a new one. The parallelism is at the API/UI/state-machine layer (B3, M1, M9), not in the schema.
- The report's OPEN ITEMS are honest about the missing database run, the Playwright situation and the absent handover photo uploader. Its DB CHANGES section is, however, materially incomplete on security: it states „privilegierte Materialisierung ist nicht für `authenticated` ausführbar" without disclosing that a manager route invokes it unscoped with the service key (B1).
- The dense single-line formatting of the new TSX matches the existing house style in `app/(neo)/neo/app/ablaeufe/page.tsx` and is not raised as a finding.
- Recommendation for the integrator: do not merge. B1 and B3 have blast radius beyond this packet (cross-tenant writes; regression of a shipped employee flow) and should be fixed before C-D/C-E/C-F integration, since they touch shared operational tables.

---

## Re-review round 1

- Diff re-reviewed: `git diff e401d12..HEAD` (fix commit `98bc00a`, 17 files, +800/-15)
- Read-only. No source file was modified. Verification used a private throwaway database
  restored from the production schema snapshot; nothing touched production.

### Verified gate results (run by the reviewer in this worktree)

| Command | Result |
| --- | --- |
| `./node_modules/.bin/vitest run --configLoader native` | **exit 0** — `Test Files 35 passed (35)`, `Tests 230 passed (230)` |
| `./node_modules/.bin/tsc --noEmit -p .` | **exit 0**, no diagnostics |
| `bash migration-dryrun.sh supabase/migrations/20260831120000_recurring_tasks_and_handover_ack.sql` | **exit 0** — `restore: 0 errors, tables=243` / `OK 20260831120000_recurring_tasks_and_handover_ack.sql` |
| `scripts/tests/077_recurring_tasks_handovers.sql` (run by the reviewer against the restored snapshot) | **exit 0** — all assertions passed |

Dry-run caveat, disclosed for honesty: the **first** invocation reported
`FAIL 20260831120000_…` with `/tmp/dryrun-…log` containing
`FATAL: database "dryrun" does not exist`. That is shared-infrastructure contention, not a
migration defect — the script's `drop database if exists dryrun with (force); create database
dryrun;` was executed concurrently by another packet's run (a sibling `dryrun_ce` database
exists on the same container) and pulled the database out from under the apply step. The
immediate re-run of the same command returned `OK`. To remove the race from the result I also
restored the same snapshot into a private database and applied the migration twice:
`APPLY1 OK`, `APPLY2(rerun) OK` (only `… already exists, skipping` notices) — the migration
applies cleanly to the production schema and is re-runnable. **Dry-run verdict: PASS.**

Beyond static reading, the following were executed against the restored production schema:
`save_recurring_operational_template`, `materialize_recurring_operational_tasks`,
`recurring_template_due_at`, `acknowledge_responsibility_handover` and
`process_operational_escalations`, with fixtures for two tenants and three locations.

### Per-finding verdicts

**B1 — VERIFIED-FIXED.** `materialize_recurring_operational_tasks(uuid,uuid,timestamptz)`
now takes and filters on `p_tenant_id`/`p_location_id`
(`supabase/migrations/20260831120000_…sql:119,124`); the route passes the validated
`actor.tenant_id`/`input.locationId` (`app/api/operations/tasks/route.ts:61`) and the cron
loops per distinct scope (`app/api/cron/operational-escalations/route.ts:19-21`).
Proven on the snapshot: with active `manual` templates in tenant A/location 1, tenant
A/location 2 and tenant B, `materialize(A, L1)` returned `8` and *all 8* rows landed in
`location_id = L1`; no row was written for L2 or tenant B. Grants confirmed:
`materialize… = supabase_admin=X | service_role=X` (no `authenticated`).
`save_recurring_operational_template` correctly raised `forbidden` for a `mitarbeiter` actor.

**B2 — VERIFIED-FIXED.** `recurring_template_due_at` now takes `p_anchor`
(`:102`), the materializer passes `v_template.recurrence_anchor` (`:126`), and the save RPC
persists an anchor on insert without resetting it on update (`:92-96`). Proven on the
snapshot for „alle 2 Tage" with anchor 31.08.: due on 31.08., 02.09., 04.09., 06.09.,
`null` on 01./03./05.09. Weekly without an explicit `weekday` now follows the anchor's
weekday (anchor Wed 02.09. → 02.09. and 09.09. only), month-end clamps
(`day=31` → 28.02.2027 09:00 Berlin) and Berlin wall time survives DST
(09:00 → `07:00+00` in summer, `08:00+00` in winter). A future anchor also behaves
(negative modulo still aligns).

**B3 — NOT-FIXED (residual; downgraded BLOCKER → MAJOR).** The two parts named in the
finding are done: the legacy endpoint now goes through the RPC
(`app/api/operations/responsibility/route.ts:336-338`) and both Mitarbeiter status filters
include `'gelesen'` (`app/mitarbeiter/page.tsx:153`,
`app/(neo)/neo/app/mitarbeiter/page.tsx:63`). But widening those filters made the new
`'gelesen'` state reachable in a third surface that was not adapted:
`app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:323` still renders its only action
under `handover.status === 'offen'` and still derives its badge from
`status === 'angenommen' ? 'Angenommen' : 'Annahme offen'`; the page's select
(`app/(neo)/neo/app/mitarbeiter/page.tsx:60`) does not fetch `read_at`/`confirmed_at`.
Failure mode: the recipient taps „Gelesen" in the Mitarbeiter-App or the manager page, then
opens the Neo Mitarbeiter tab — the handover shows „Annahme offen" with **no button at all**,
so that surface is a dead end for every handover that was read but not yet confirmed.
Downgraded to MAJOR because the recipient does have a working path in
`app/mitarbeiter/my-operations.tsx`.

**B4 — VERIFIED-FIXED.** `app/mitarbeiter/my-operations.tsx:73-79,123` now renders separate
„Gelesen" and „Bestätigen" buttons for `to_employee_id === actorId`, posts to
`/api/operations/tasks` with `read_handover`/`confirm_handover`, and highlights unconfirmed
handovers with an amber border plus an explicit
`Bestätigt / Gelesen · Bestätigung offen / Ungelesen` badge. `locationId` is threaded from
`app/mitarbeiter/page.tsx:313`. The role gate in the route is the RPC's
recipient-or-location-management check, not `requireManagerPlus`, so `mitarbeiter` and
`teamleiter` can now acknowledge. Proven on the snapshot: recipient `read` → `gelesen`,
`confirm` → `angenommen`; an unrelated employee got `forbidden`.

**M1 — VERIFIED-FIXED.** `app/api/operations/tasks/route.ts:66` requires
`manager || isDepartmentLead(departmentId)` with the schedule-aware check from
`lib/operations/responsibility-scope`, matching the pre-existing writer.

**M2 — VERIFIED-FIXED.** `route.ts:56` cancels future open instances
(`status='storniert'` where `template_id`, `status='offen'`, `recurrence_due_at > now()`)
on both `pause_rule` and `delete_rule`, and skips that update for the new `resume_rule`.

**M3 — VERIFIED-FIXED.** `lib/operations/recurrence.ts:76-82` maps ISO weekdays to
German names: „Wochentage: Mo, Mi, Fr", „Wöchentlich am Montag",
„Jeden 1. Montag im Monat". No raw integers remain in the label.

**M4 — VERIFIED-FIXED (functionally).** A weekday `<select>` is rendered for `weekly` and an
ordinal+weekday pair for `monthly_weekday`
(`app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:22-23,34`), and the server-side
validator now rejects `weekly`/`monthly_weekday` without `weekday`
(`recurrence.ts:49,51`, applied at `route.ts:46`). Placement is a new MINOR (N3).

**M5 — VERIFIED-FIXED.** Two `<input type="date">` fields push `?from=…&to=…`
(`operations-client.tsx:25`); `page.tsx:19-22` validates them and builds the boundary with
`berlinDateBoundary`. The stale instruction text is a new MINOR (N5).

**M6 — NOT-FIXED.** `page.tsx:30-34` resolves `open_task_ids` to titles, but only for the
**manager-plus audit page**, and it does so by prepending the list into `important_notes`.
The surface the finding was about — the incoming shift — still renders only
`handover.note` (`app/mitarbeiter/my-operations.tsx:123`), and
`app/mitarbeiter/page.tsx:150` does not even select `open_task_ids`. Failure mode unchanged:
the Folgeschicht taps „Gelesen" and „Bestätigen" without ever seeing the open tasks it is
confirming. See also N2 and N6.

**M7 — VERIFIED-FIXED.** `route.ts:78-79` logs the raw error server-side
(`console.error('Operations action failed', error)`) and returns the fixed German sentence
„Die Aktion konnte nicht gespeichert werden. Bitte erneut versuchen."; the ack path returns
„Erst lesen, dann bestätigen." / „Übergabe konnte nicht geöffnet werden." No `error.message`
reaches the client from this route any more.

**M8 — VERIFIED-FIXED.** `app/api/cron/operational-escalations/route.ts:41` uses
`now - 3 min`, matching the `*/3 * * * *` schedule in `vercel.json:10`. NOTE: the window is
now exactly equal to the cadence, so a run that fires slightly early can still re-match a
task once; a persisted send marker would be strictly safer.

**M9 — VERIFIED-FIXED.** `lib/operations/handover-state.ts` is deleted (only
`recurrence.ts` and `responsibility-scope.ts` remain), no reference to it survives anywhere
in the tree, and the unit test no longer imports it. The shipped RPC is now covered by
`scripts/tests/077_recurring_tasks_handovers.sql:22-25`, which I executed green against the
production schema snapshot, and I additionally exercised the live state machine (see B4).

**MINOR-1 — VERIFIED-FIXED.** `berlinDateBoundary` (`recurrence.ts:36`) replaces the
hard-coded `+02:00` and resolves the offset through the Berlin zone.

**MINOR-2 — VERIFIED-FIXED.** `migration:152-153` guards `read` with
`if v_row.read_at is null`. Proven: a second `read` (even by a different manager) left
`read_by`/`read_at` untouched and returned `gelesen` without raising.
`confirm` before `read` raises `handover must be read first`; a second `confirm` raises
`handover already confirmed`; an unknown action still raises `invalid handover action`.

**MINOR-3 — FIXED IN CODE, INEFFECTIVE.** `migration:55` excludes
`new.escalation_owner_employee_id`, which is exactly the recipient the pre-existing sweep
notifies (`process_operational_escalations` sets `escalation_owner_employee_id=v_owner` in
the same `update` and notifies `v_owner`), so the de-duplication logic is right. It can never
run, though — see N1.

**MINOR-4 — VERIFIED-FIXED.** The `on conflict … do update set` list (`migration:93-96`)
no longer contains `aktiv`, `paused_at` or `deleted_at`, so „Bearbeiten" cannot revive a
paused or deleted rule, and a dedicated `resume_rule` action plus „Fortsetzen" button exist
(`route.ts:53`, `operations-client.tsx:28`).

**MINOR-5 — VERIFIED-FIXED.** `.is('deleted_at', null)` added at
`app/(neo)/neo/app/mitarbeiter/page.tsx:58`; that is the only other place querying
`operational_task_templates`.

**MINOR-6 — VERIFIED-FIXED.** `page.tsx:35` sorts unconfirmed first, then newest first,
which now matches the copy.

**MINOR-7 — VERIFIED-FIXED.** No occurrence of „Materialisierung", „materialisier" or
„idempotent" remains under `app/(neo)/neo/app/ablaeufe/aufgaben/`; the replacements read
„Ein zweiter Lauf legt dieselbe Fälligkeit nicht noch einmal an." and „Jetzt erzeugen".

**MINOR-8 — VERIFIED-FIXED.** `route.ts:46-48` resolves deputies from
`department_responsibility_assignments` with `responsibility_role='stellvertretung'` and
`aktiv`, matched on tenant+location+department, replacing `delegated_from_employee_id`.
NOTE: like the in-app trigger it ignores `valid_from`/`valid_until`, so the two paths stay
consistent with each other.

**MINOR-9 — VERIFIED-FIXED.** `escapeHtml` is applied to the title and first name in the
HTML body and the CTA is absolute via `NEXT_PUBLIC_APP_URL` with a production fallback
(`route.ts:14,50,54`).

**MINOR-10 — VERIFIED-FIXED.** Confirmed on the snapshot: `pg_proc.provolatile = 's'` for
`recurring_template_due_at(jsonb,date,timestamptz)`.

**MINOR-11 — NOT-FIXED (partial).** Preselection is now gated
(`operations-client.tsx:15`: `tasks.length<=20 ? tasks.map(t=>t.id) : []`), which addresses
the dangerous half. But the list itself is still one flat, ungrouped set of up to 200
checkboxes (`:29`), and the copy above it still states unconditionally „Offene Aufgaben sind
vorausgewählt." — which is false for exactly the >20 case the fix introduced. The report's
claim „die UI fordert zur bewussten Auswahl auf" is not implemented; there is no conditional
text.

**MINOR-12 (`recurrenceRule` unvalidated server-side) — VERIFIED-FIXED.**
`route.ts:46` now calls `validateRecurrenceRule` before the RPC and returns
„Wiederholung ist unvollständig oder ungültig." on failure.

*No REFUTED claims were made in the implementer's answer — all 24 items were reported as
FIXED, so there is nothing to accept or reject as a refutation.*

### New findings

#### N1 — MAJOR — the new escalation trigger raises on every fire; the notification path is dead code
`supabase/migrations/20260831120000_…sql:57-58`

`notifications.typ` is the enum `notification_type` (`info|warnung|dringend|erfolg`), but the
trigger inserts `case when new.escalation_level>1 then 'dringend' else 'warnung' end` with no
cast. A CASE whose branches are all string literals resolves to `text`, and there is no
implicit `text → notification_type` coercion. Reproduced against the restored production
schema: escalating one overdue task aborts with
`ERROR: column "typ" is of type notification_type but expression is of type text … CONTEXT:
PL/pgSQL function notify_operational_escalation_recipients() line 13 … SQL statement "update
public.operational_tasks set escalation_level=…, last_escalated_at=p_now …"`. Because the
trigger is `after update of last_escalated_at`, it aborts the sweep's own `UPDATE`, so
`process_operational_escalations` fails as a whole and `/api/cron/operational-escalations`
returns 500 — no escalation, no in-app notification, no e-mail. This also means MINOR-3's
de-duplication and the report's claim „In-App-Benachrichtigungen werden unabhängig davon
geschrieben" cannot hold.
*Fix:* cast explicitly, e.g. `(case … end)::public.notification_type`, and assert the insert
(not just the function text) in `scripts/tests/077_…sql`.

#### N2 — MAJOR — the acknowledging surface shows none of the structured handover content
`app/mitarbeiter/my-operations.tsx:123` and `app/mitarbeiter/page.tsx:150`

The B4 fix gave the recipient the buttons but not the content. The card renders only
`handover.note`, which the creating route fills with
`importantNotes || incidents || 'Übergabe ohne zusätzliche Hinweise'`
(`app/api/operations/tasks/route.ts:69`), and the query selects neither `open_task_ids` nor
`incidents`, `inventory_notes`, `damage_notes` or `cleaning_notes`. Failure mode: the
Folgeschicht presses „Gelesen" and „Bestätigen" on a legally binding record while four of the
five categories and the open-task list are invisible to it — the confirmation attests to
content the confirming person was never shown. Owner point 4 asks for the opposite.
*Fix:* select and render the five categories plus the resolved open tasks in the
Mitarbeiter-App card before the acknowledge buttons.

#### N3 — MINOR — the weekly/monthly-weekday inputs render above the tab bar, detached from the form
`app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:22-23` vs `:24,27`

Both new field blocks are emitted before `<nav>` and outside `tab==='rules'`, gated only on
`showForm`. They therefore appear at the very top of the page, separated from the rest of the
rule form by the tab bar, and stay visible after switching to „Übergabe erstellen" or
„Übergabe-Audit". *Fix:* move them into the `showForm` grid at `:27`.

#### N4 — MINOR — the recurrence option is still labelled „Monatlich · erster Montag"
`operations-client.tsx:11`

Now that ordinal and weekday are selectable (M4), the option label promises a fixed first
Monday. *Fix:* „Monatlich · bestimmter Wochentag".

#### N5 — MINOR — the audit copy still tells the owner to edit the page address
`operations-client.tsx:30`

„Standort und Zeitraum lassen sich über die Seitenadresse filtern." remains although the
date fields from the M5 fix are two lines above it. *Fix:* delete that sentence.

#### N6 — MINOR — handed-over task titles are resolved against currently open tasks only, and rendered as a collapsed run of text under the wrong label
`app/(neo)/neo/app/ablaeufe/aufgaben/page.tsx:30-34` and `operations-client.tsx:30`

`titleByTaskId` is built from the *open* task query (`not status in (erledigt,storniert)`,
`limit 200`), so as soon as a handed-over task is completed the audit prints
„Nicht mehr offene Aufgabe" instead of its title — precisely for the historic handovers an
audit exists to answer. The summary is also injected into `important_notes` rather than
carried in `open_tasks` (which is passed to the client and never used), so it is displayed
under the label „Hinweise", and `Fact` renders it in a plain `<p>` with no
`whitespace-pre-line`, collapsing the `•`-list into a single line. *Fix:* select the titles
for `open_task_ids` directly (no status filter) and render `open_tasks` as its own labelled
list.

#### N7 — MINOR — the cron's scope discovery can be silently truncated
`app/api/cron/operational-escalations/route.ts:19`

The scope query has no explicit range, so it is subject to PostgREST's default row cap
(1000). Past that many active recurring templates the tail of tenant/location pairs is
dropped and those locations are never materialized, with nothing logged. *Fix:* select
distinct scopes via an RPC, or page the query explicitly.

### NOTES

- The same uncast-CASE defect as N1 already exists in the pre-existing
  `process_operational_escalations` (production snapshot: `insert into public.notifications …
  case when v_level=1 then 'warnung' else 'dringend' end`), which fails identically when the
  new trigger is dropped. That is outside this packet's diff, but it means the escalation
  sweep is currently non-functional on the production schema and should be fixed in the same
  pass as N1.
- The status check constraint swap (`migration:163-164`) is a strict superset of the
  snapshot's `('offen','angenommen','abgeschlossen','storniert')`, so no existing row can be
  invalidated by adding `'gelesen'`.
- Every column the new code references exists in the production snapshot
  (`operational_tasks.escalation_owner_employee_id`, the template assignment/evidence/
  escalation columns, the `department_responsibility_assignments` schedule columns, and the
  `notifications` columns) — checked against `information_schema`.
- Idempotency of the materializer is real: the second `materialize(A, L1)` call returned `0`
  on the snapshot, and the partial unique index inference in
  `on conflict(template_id,recurrence_due_at) where …` is accepted by the server.
- The legacy `accept_handover` path now writes `read_at` and `confirmed_at` at the same
  instant. That is acceptable for a one-tap accept, but the audit chain for handovers taken
  through the Neo surface will always show identical read/confirm timestamps.
- Recommendation for the integrator: still do not merge. N1 disables the escalation run
  outright on the production schema, and B3/M6/N2 leave the Folgeschicht acknowledging
  content it cannot see — both touch shared operational tables and surfaces that C-D/C-E/C-F
  build on.

**VERDICT: FAIL** — 0 BLOCKER, 4 MAJOR (B3 residual, M6, N1, N2), 6 MINOR (MINOR-11
residual, N3–N7); 4/4 BLOCKER, 7/9 MAJOR and 11/12 MINOR from round 1 verified fixed.

---

## Re-review round 2

- Diff re-reviewed: `git diff e401d12..HEAD` (fix commit `3554358`, 19 files, +1230/-19)
- Read-only. No source file was modified. Verification again used a private throwaway
  database restored from the production schema snapshot; nothing touched production.

### Verified gate results (run by the reviewer in this worktree)

| Command | Result |
| --- | --- |
| `./node_modules/.bin/vitest run --configLoader native` | **exit 0** — `Test Files 35 passed (35)`, `Tests 230 passed (230)`, duration 12.23s |
| `./node_modules/.bin/tsc --noEmit -p .` | **exit 0**, no diagnostics |
| `bash migration-dryrun.sh supabase/migrations/20260831120000_recurring_tasks_and_handover_ack.sql supabase/migrations/20260831120100_recurring_tasks_escalation_enum_fix.sql` | **exit 0** — `restore: 0 errors, tables=243` / `OK 20260831120000_…` / `OK 20260831120100_…` |
| `scripts/tests/077_recurring_tasks_handovers.sql` (reviewer-run against the restored snapshot **with fixtures**) | **exit 0** — `BEGIN / DO / DO / ROLLBACK` |
| `scripts/tests/077_recurring_tasks_handovers.sql` (same snapshot, **no fixtures**) | **exit 3** — `ERROR: fixture missing: operational task and employee required` (see N11) |

Both migration files new on this branch were dry-run; no `FAIL` line and no retry was needed
this time. Beyond the dry-run I restored the same snapshot into a private database, applied
both migrations and executed the escalation sweep, the escalation trigger and the
acknowledgement state machine against real fixtures.

### Per-finding verdicts

**B3 (round-1 residual, MAJOR) — VERIFIED-FIXED.** `app/(neo)/neo/app/mitarbeiter/page.tsx:61`
now selects `read_at,confirmed_at`, and `responsibility-client.tsx:323` derives the badge from
them (`Bestätigt` / `Gelesen · Bestätigung offen` / `Ungelesen`) and renders
„Lesen & bestätigen" for every **unconfirmed** handover addressed to the actor, not only for
`status='offen'`. Proven on the snapshot that the legacy path survives the `gelesen` state:
`acknowledge(...,'read')` → `gelesen`, a second `read` → `gelesen` (no-op, no exception),
`confirm` → `angenommen` with `confirmed_by`/`accepted_at` set. The dead end is gone.

**M6 — VERIFIED-FIXED.** `app/mitarbeiter/page.tsx:150` selects `open_task_ids` and the four
missing note columns; `:163-171` resolves the handed-over ids against `operational_tasks`
tenant- and location-scoped **without** a status filter and passes `open_tasks` to the client,
which renders them as a labelled „Offene Aufgaben" list above the acknowledge buttons
(`my-operations.tsx:125`). The incoming shift now sees the tasks it confirms.

**N1 (MAJOR) — VERIFIED-FIXED.** `20260831120100_…sql` casts both CASE expressions to
`public.notification_type`, in the new trigger function and in the pre-existing
`process_operational_escalations`. Verified on the restored production schema, not statically:
with an overdue task, `select process_operational_escalations(now(), interval '4 hours')`
returned `1`, the task reached `escalation_level=1` with owner and `last_escalated_at` set, and
the notification rows were written. `pg_get_functiondef` confirms the cast is present in both
installed functions.

**MINOR-3 (round-1 „fixed in code, ineffective") — VERIFIED-FIXED and now effective.** With
assignee = accountable = escalation owner, exactly **one** notification was written (the
sweep's). With assignee ≠ owner, exactly two were written — one „Aufgabe überfällig" for the
assignee from the trigger, one „Überfällige Aufgabe: Verantwortung klären" for the owner from
the sweep. No duplicate.

**N2 — VERIFIED-FIXED (with a regression, see N8).** `my-operations.tsx:125` renders
„Offene Aufgaben", „Besondere Vorkommnisse", „Bestände", „Schäden", „Reinigungsprobleme" and
„Wichtige Hinweise" before the „Gelesen"/„Bestätigen" buttons. The five structured categories
of a C-C handover are visible to the confirming person.

**MINOR-11 (round-1 residual) — VERIFIED-FIXED.** `operations-client.tsx:27` groups the
checkboxes per Bereich (with „Standortweit" for department-less tasks) and the copy is now
conditional: `tasks.length<=20 ? 'Offene Aufgaben sind vorausgewählt.' : 'Wähle die zu
übergebenden Aufgaben bewusst aus.'`, matching the preselection rule at `:15`.

**N3 — VERIFIED-FIXED.** Both field blocks are now inside the `showForm` grid within
`tab==='rules'` (`operations-client.tsx:27`); nothing renders above the `<nav>` any more.

**N4 — VERIFIED-FIXED.** `operations-client.tsx:11` reads „Monatlich · bestimmter Wochentag".

**N5 — VERIFIED-FIXED.** The „Seitenadresse" sentence is gone; the audit intro is now only
„Unbestätigte Übergaben stehen oben." (`operations-client.tsx:30`), which the sort at
`page.tsx:35` actually implements.

**N6 — VERIFIED-FIXED.** `page.tsx:30-34` resolves the titles with a dedicated
`in('id', handedOverTaskIds)` query without a status filter, so completed tasks keep their
title; the result is carried in `open_tasks` and rendered as its own „Offene Aufgaben" list
(`operations-client.tsx:30`). `important_notes` is no longer polluted and appears only under
„Hinweise".

**N7 — VERIFIED-FIXED.** `active_recurring_operational_scopes()` returns
`distinct tenant_id, location_id`; verified on the snapshot as `stable`, `security definer`,
`proacl = supabase_admin=X | service_role=X` (no `authenticated`), and the cron consumes it
(`route.ts:20`). The row set is now bounded by distinct tenant/location pairs instead of by
template count. NOTE: no explicit `.range()` was added, so a PostgREST `db-max-rows` cap would
still apply in principle — it is simply unreachable at this cardinality.

*No REFUTED claims were made in „Fix round 2" either — all nine items were reported as FIXED,
so there is again nothing to accept or reject as a refutation.*

### New findings

#### N8 — MAJOR — the round-2 fix dropped `handover.note`; handovers from the pre-existing creation dialog are now shown to the recipient with no content at all
`app/mitarbeiter/my-operations.tsx:125` (regression vs. `464fadd:app/mitarbeiter/my-operations.tsx:123`)

The card previously rendered `<p className="mt-3 text-sm text-slate-700">{handover.note}</p>`.
The N2/M6 fix replaced that paragraph with the five structured categories and the open-task
list, and `note` is now referenced only in the type declaration (`:19`) — it is rendered
nowhere. The still-live legacy creation path writes **only** `note`: `HandoverEditor`
(`app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:321-322`) posts `create_handover` to
`app/api/operations/responsibility/route.ts`, whose schema (`:57-60`) has `note` as the sole
content field and whose insert (`:325`) sets no `incidents`, `inventory_notes`,
`damage_notes`, `cleaning_notes`, `important_notes` or `open_task_ids`.
Failure mode: a manager hands over via „Übergabe erstellen" with reason `urlaub` and the note
„Kühlhaus-Thermostat defekt, Techniker kommt Dienstag"; the recipient opens the
Mitarbeiter-App and sees only the two names, the Bereich, the reason and the „Ungelesen"
badge — an empty card — then presses „Gelesen" and „Bestätigen". This is exactly the
attest-to-unseen-content failure N2 described, re-created for every handover of the older
path, and it is a regression of behaviour that worked at `52e8936` and `464fadd`.
*Fix:* render `handover.note` again (e.g. as a „Notiz" fact) alongside the five categories.

#### N9 — MINOR — the Neo Mitarbeiter card lets the recipient bindingly confirm while showing only `note`
`app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:323`, query at `app/(neo)/neo/app/mitarbeiter/page.tsx:61`

The B3 fix added „Lesen & bestätigen" to this surface, but the select fetches neither
`open_task_ids` nor the five category columns, and the card renders `<p>{handover.note}</p>`
only. For a C-C handover that `note` is `importantNotes || incidents || 'Übergabe ohne
zusätzliche Hinweise'` (`app/api/operations/tasks/route.ts:69`), so a manager who is the
recipient can confirm here while at most one of six content blocks is on screen. Rated MINOR
rather than MAJOR only because the page is `requireManagerPlus` and the same person has the
complete card in `app/mitarbeiter`. *Fix:* select and render the same fields as the
Mitarbeiter-App card, or link to it instead of offering the button here.

#### N10 — MINOR — the integrator handover still lists one migration and the pre-round-1 test count
`docs/agents/HANDOFF.md:397-399`

The C-C section names only
`supabase/migrations/20260831120000_recurring_tasks_and_handover_ack.sql`, claims
„Vitest 35/35 Dateien und 231/231 Tests grün" (the branch is now at 230) and still says the
SQL smoke „ist mangels Datenbank noch auszuführen". The round-2 migration
`20260831120100_recurring_tasks_escalation_enum_fix.sql` — the one that carries the enum cast
without which the escalation sweep aborts (N1) — is not mentioned. An integrator working from
HANDOFF alone would not know it exists. *Fix:* list both migrations and refresh the numbers.

#### N11 — MINOR — the new escalation assertion in the SQL smoke cannot run on a clean schema and mutates existing rows when it can
`scripts/tests/077_recurring_tasks_handovers.sql:29-45`

The `$escalation_insert_test$` block selects an arbitrary existing task
(`order by t.created_at desc limit 1`) and aborts with
`fixture missing: operational task and employee required` when the database holds none.
Reproduced: against the schema-only production snapshot with both migrations applied, the file
exits **3** on that RAISE; after seeding a tenant/location/employee/task it exits **0**. Where
it does find data — i.e. against production-like data — it `update`s that real task's
`assigned_to`, `accountable_employee_id`, `escalation_level` and `last_escalated_at`
(rolled back at the end, so no lasting change, but it locks and re-escalates a live row).
*Fix:* insert and roll back its own fixture rows instead of reusing whatever is in the table.

### NOTES

- The replacement of `process_operational_escalations(timestamptz,interval)` in
  `20260831120100` is byte-for-byte equivalent to the `20260830154500` definition apart from
  the `::public.notification_type` cast — same cursor predicates, same `for update skip
  locked`, same interval guard, same `audit_log` payloads, same owner selection and links.
  Replacing a foreign packet's function is acceptable here because the defect made this
  packet's own trigger unreachable, but it is a cross-packet edit the integrator should know
  about.
- The C-C migration prefix range is respected: `20260831120100` sits after `20260831120000`
  and before any other packet's 2026-08-31 prefix in the tree.
- `open_task_ids` titles are resolved with an explicit `tenant_id`/`location_id` filter in
  both new resolvers (`app/mitarbeiter/page.tsx:165`,
  `app/(neo)/neo/app/ablaeufe/aufgaben/page.tsx:32`), so a stale id from another scope cannot
  leak a foreign task title; it degrades to the neutral fallback string instead.
- M8's residual stands unchanged: the e-mail lookback (`now - 3 min`) equals the cron cadence,
  so a slightly early run can still double-send. Not re-raised as a finding.
- Recommendation for the integrator: N8 is a one-line render regression, not a design problem —
  but until it is fixed, every handover created through the older „Übergabe erstellen" dialog
  reaches its recipient as an empty card. Fix N8, then this packet is mergeable; N9–N11 can
  follow.

**VERDICT: FAIL** — 0 BLOCKER, 1 MAJOR (N8), 3 MINOR (N9–N11); all 11 items left open by
round 1 (B3 residual, M6, N1, N2, MINOR-3, MINOR-11, N3–N7) verified fixed.

---

## Re-review round 3

- Diff re-reviewed: `git diff e401d12..HEAD` (fix commit `8c86dd5`, 19 files, +1448/-19)
- Read-only. No source file was modified. Verification again used a private throwaway
  database (`claude_rr3`) restored from the production schema snapshot and dropped
  afterwards; nothing touched production.

### Verified gate results (run by the reviewer in this worktree)

| Command | Result |
| --- | --- |
| `./node_modules/.bin/vitest run --configLoader native` | **exit 0** — `Test Files 35 passed (35)`, `Tests 230 passed (230)`, duration 6.82s |
| `./node_modules/.bin/tsc --noEmit -p .` | **exit 0**, no diagnostics |
| `bash migration-dryrun.sh supabase/migrations/20260831120000_recurring_tasks_and_handover_ack.sql supabase/migrations/20260831120100_recurring_tasks_escalation_enum_fix.sql` | **exit 0** — `restore: 0 errors (extension/duplicate noise expected), tables=243` / `OK 20260831120000_recurring_tasks_and_handover_ack.sql` / `OK 20260831120100_recurring_tasks_escalation_enum_fix.sql` |
| `scripts/tests/077_recurring_tasks_handovers.sql` against the restored production schema | **exit 3** — `ERROR: null value in column "name" of relation "tenants" violates not-null constraint … CONTEXT: SQL statement "insert into public.tenants(id) values (v_tenant_id)"` (see N11) |
| `git diff --check e401d12..HEAD` | **exit 0**, no output |
| `./node_modules/.bin/next build` | not run (excluded by the review instruction) |

Both migration files new on this branch (`20260831120000`, `20260831120100`) were dry-run
against the production schema snapshot in one invocation; no `FAIL` line, no retry needed.
**Dry-run verdict: PASS.** The migration files are byte-identical to the round-2 state
(`git diff 3554358..HEAD` touches no `supabase/` file), so the round-1/round-2 database
verifications of the RPCs, grants, scoping and state machine carry over unchanged.

### Per-finding verdicts

**N8 (MAJOR) — VERIFIED-FIXED.** `app/mitarbeiter/my-operations.tsx:125` renders
`{handover.note && <HandoverFact label="Notiz">{handover.note}</HandoverFact>}` as the first
block of the content grid, ahead of „Offene Aufgaben" and the five categories, and
`note` is selected by the query (`app/mitarbeiter/page.tsx:150`). A handover created through
the legacy `HandoverEditor` — which writes `note` and nothing else — is therefore no longer
an empty card for its recipient. The acknowledge buttons still sit below the content grid, so
the read/confirm order is preserved.

**N9 (MINOR) — VERIFIED-FIXED.** `app/(neo)/neo/app/mitarbeiter/page.tsx:61` now selects
`open_task_ids,incidents,inventory_notes,damage_notes,cleaning_notes,important_notes`
alongside `note`, and `responsibility-client.tsx:325` renders all seven blocks through the new
`HandoverDetail` helper (`:350`) before the „Lesen & bestätigen" button. The recipient on this
surface no longer confirms with one of six content blocks visible. `open_task_ids` is
`not null default '{}'::uuid[]` in the snapshot, so the `.length` access is safe for legacy
rows. Residuals raised separately as N12/N13.

**N10 (MINOR) — VERIFIED-FIXED for what it named, but the replacement text introduces a false
claim.** `docs/agents/HANDOFF.md:398-399` now lists both migrations, including
`20260831120100_recurring_tasks_escalation_enum_fix.sql`, and corrects the test count from
231 to 230 (I measured 230/230). The stale „ist mangels Datenbank noch auszuführen" sentence
is gone. However the sentence that replaced it — „`scripts/tests/077_…sql` verwendet
ausschließlich zurückgerollte eigene Fixtures und wurde vom Reviewer gegen den restaurierten
Snapshot erfolgreich ausgeführt" — is not true of the file that is on the branch: the
reviewer-run it refers to was of the round-2 version, and the current version cannot succeed
(N11). See also N14.

**N11 (MINOR) — NOT-FIXED.** The intent of the fix is right — the block no longer selects an
arbitrary live task and no longer mutates production rows — but the fixture inserts omit
columns the production schema declares `not null` with no default, so the file now fails
**unconditionally**, on a clean schema and on a populated one alike. Reproduced against the
restored snapshot with both migrations applied: the first `do $test$` block passes (`BEGIN`,
`DO`), the second aborts at `scripts/tests/077_recurring_tasks_handovers.sql:40` with
`ERROR: null value in column "name" of relation "tenants" violates not-null constraint`,
and psql exits **3** under the file's own `\set ON_ERROR_STOP on`.
The missing columns, read from `information_schema` on the snapshot, are
`tenants.name`, `tenants.slug`, `locations.name`, `employees.vorname`, `employees.nachname`
and `employees.email`. Proof that this is the only remaining defect: a copy of the file in
`/tmp` with exactly those six values supplied runs `BEGIN / DO / DO / ROLLBACK` and exits
**0** on the same database — the escalation assertion itself is sound and does fire the
trigger. Regression note: at `3554358` the file exited 0 whenever fixture data existed; it now
exits 3 in every case, so this packet's only database-level test artifact is red while
HANDOFF.md and „Fix round 3" declare it green („läuft auch auf einem Schema ohne
Datenfixtures" — provably the opposite).
**Severity raised MINOR → MAJOR**, because it is no longer a fixture-hygiene nit but a
non-executable gate combined with a green claim in the integrator handover.
*Fix:* add `name`/`slug`, `name`, and `vorname`/`nachname`/`email` to the three fixture
inserts, then re-run the file and quote its real exit status.

*No REFUTED claims were made in „Fix round 3" either — all four items were reported as FIXED,
so there is again nothing to accept or reject as a refutation.*

### New findings

#### N12 — MINOR — the Neo card resolves handed-over task titles from the page's capped, `storniert`-filtered task list
`app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:325`, query at `app/(neo)/neo/app/mitarbeiter/page.tsx:51-53`

`tasks.find((task) => task.id === id)?.title ?? 'Aufgabe aus der Übergabe'` looks the ids up in
the page's `operational_tasks` result, which is `.not('status','eq','storniert').limit(300)`.
The two other surfaces resolve the ids with a dedicated `in('id', handoverTaskIds)` query and
no status filter (`app/mitarbeiter/page.tsx:163-167`,
`app/(neo)/neo/app/ablaeufe/aufgaben/page.tsx:30-34`) — precisely the N6 fix. Failure mode: a
handed-over task that was later cancelled, or that falls outside the 300-row window on a busy
location, is displayed to the confirming manager as the placeholder „Aufgabe aus der
Übergabe" instead of its title. *Fix:* resolve the titles server-side in `page.tsx` the same
way the other two surfaces do and pass them down.

#### N13 — MINOR — `HandoverDetail` emits unstyled markup into a CSS-module-styled card
`app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:350`

The helper returns `<div><strong>{label}</strong><div>{children}</div></div>` with no
`styles.*` class. `responsibility.module.css` styles only `.handoverCard p`
(`color:#334155;font-size:11.5px;line-height:1.5`), which the old `<p>{handover.note}</p>`
matched and these `div`s do not. Failure mode: inside a card whose other text is 10–11.5px,
the seven content blocks render at the browser default 16px with no gap, and the „Offene
Aufgaben" `<ul>` keeps the default ~40px indent — on the `minmax(330px,1fr)` grid, and at the
`max-width:700px` single-column breakpoint, that is a visibly broken card. *Fix:* add a
`handoverDetail` class (label 10px uppercase, body 11.5px, `list-style` inside) and use it.

#### N14 — MINOR — for C-C handovers the „Notiz" block repeats content already shown below it
`app/mitarbeiter/my-operations.tsx:125` and `app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:325`, source at `app/api/operations/tasks/route.ts:69`

The structured creation route sets `note: input.importantNotes || input.incidents ||
'Übergabe ohne zusätzliche Hinweise'`. Both cards now render `note` *and* `important_notes`
*and* `incidents`. Failure mode: a handover with „Wichtige Hinweise: Kühlhaus defekt" shows
that sentence twice — once under „Notiz", once under „Wichtige Hinweise" — and a handover
with no notes at all shows „Notiz: Übergabe ohne zusätzliche Hinweise" above real content.
The N8 fix is still correct for legacy handovers; only the overlap is untreated. *Fix:* render
„Notiz" only when `note` differs from `important_notes` and `incidents`, or stop
back-filling `note` in the structured route.

#### N15 — MINOR — the integrator handover silently dropped the disclosure that Playwright is not green
`docs/agents/HANDOFF.md:398` (regression vs. `464fadd:docs/agents/HANDOFF.md:398`)

The C-C entry previously read „Playwright ausgeführt, aber mangels `.env`/DB und nach lokalem
Server-Reset nicht grün (6 passed, 4 skipped, 22 connection failures)." The N10 fix removed
that clause entirely rather than updating it, and the new sentence lists only green gates.
Failure mode: an integrator reading HANDOFF alone concludes the E2E suite is green for this
packet; the packet's own spec still self-skips without `E2E_AUTH_STATE`
(`tests/e2e/operations-recurring-handover.spec.ts`). Refreshing numbers must not remove a
known-red disclosure. *Fix:* restore the Playwright sentence with the current status.

### NOTES

- The round-3 diff touches no `supabase/migrations/**` file, no API route and no
  authentication/role gate. All security-relevant verdicts from rounds 1 and 2 — tenant and
  location scoping of `materialize_recurring_operational_tasks`, the service-only grants on it
  and on `active_recurring_operational_scopes()`, the department-lead check on
  `create_handover`, the recipient-or-management check in
  `acknowledge_responsibility_handover`, and the explicit `tenant_id`/`location_id` filters on
  every service-client read — therefore stand unchanged and were not re-litigated.
- The two Neo/Mitarbeiter queries widened in round 3 add only columns of
  `responsibility_handovers`, a table both queries already filter on `tenant_id` **and**
  `location_id`; no new data crosses a scope boundary.
- M8's residual stands unchanged (e-mail lookback equals the cron cadence, so a slightly
  early run can double-send). Still not re-raised as a finding.
- Recommendation for the integrator: the product surfaces are in good shape — N8 closed the
  last content-visibility gap and every finding from rounds 1 and 2 is now verified fixed. The
  one thing blocking a clean pass is evidence integrity: `scripts/tests/077_…sql` is red on
  the production schema while HANDOFF.md and the report call it green. That is a six-value
  edit to three `insert` statements plus an honest re-run; N12–N15 can follow.

**VERDICT: FAIL** — 0 BLOCKER, 1 MAJOR (N11, upgraded from MINOR), 4 MINOR (N12–N15); N8, N9
and N10 verified fixed, and all 20 items closed in rounds 1–2 remain fixed.

## Re-review round 4

- Diff re-reviewed: `git diff e401d12..HEAD` (fix commit `87ae5f9`, 20 files, +1663/-20)
- Read-only. No source file was modified. The SQL smoke was verified on a private throwaway
  database (`claude_rr4`) restored from the production schema snapshot and dropped again
  afterwards; nothing touched production.

### Verified gate results (run by the reviewer in this worktree)

| Command | Result |
| --- | --- |
| `./node_modules/.bin/vitest run --configLoader native` | **exit 0** — `Test Files 35 passed (35)`, `Tests 230 passed (230)`, duration 10.88s |
| `./node_modules/.bin/tsc --noEmit -p .` | **exit 0**, no diagnostics |
| `bash migration-dryrun.sh supabase/migrations/20260831120000_recurring_tasks_and_handover_ack.sql supabase/migrations/20260831120100_recurring_tasks_escalation_enum_fix.sql` | **exit 0** — `restore: 0 errors (extension/duplicate noise expected), tables=243` / `OK 20260831120000_recurring_tasks_and_handover_ack.sql` / `OK 20260831120100_recurring_tasks_escalation_enum_fix.sql` |
| `scripts/tests/077_recurring_tasks_handovers.sql` against the restored production schema with both migrations applied | **exit 0** — `BEGIN / DO / DO / ROLLBACK` |
| `git diff --check e401d12..HEAD` | **exit 0**, no output |
| `./node_modules/.bin/next build` | not run (excluded by the review instruction) |

`git diff --name-only --diff-filter=A e401d12..HEAD -- supabase/migrations` lists exactly the
two files dry-run above; both are new on this branch and both applied cleanly in one
invocation. No `FAIL` line, no retry needed. **Dry-run verdict: PASS.**

The migration files are byte-identical to the round-2 state (`git show --stat 87ae5f9` touches
no `supabase/migrations/**` file, no API route, and no authentication or role gate), so every
security-relevant verdict from rounds 1–3 — tenant/location scoping of
`materialize_recurring_operational_tasks`, the service-only grants on it and on
`active_recurring_operational_scopes()`, the department-lead check on `create_handover`, the
recipient-or-management check in `acknowledge_responsibility_handover`, and the explicit
`tenant_id`/`location_id` filters on every service-client read — carries over unchanged and
was not re-litigated.

### Per-finding verdicts

**N11 (MAJOR) — VERIFIED-FIXED.** `scripts/tests/077_recurring_tasks_handovers.sql:40-45` now
supplies all six `not null`-without-default columns the production schema requires
(`tenants.name`, `tenants.slug`, `locations.name`, `employees.vorname`, `employees.nachname`,
`employees.email`). Executed by me against a freshly restored production schema snapshot with
both C-C migrations applied: the file emits `BEGIN / DO / DO / ROLLBACK` and psql exits **0**
under its own `\set ON_ERROR_STOP on`. The assertion is not vacuous — line 55 raises unless
the escalation trigger actually inserted an enum-typed row into `public.notifications`
between the `v_before` and `v_after` counts, and exit 0 therefore proves the trigger fired.
The fixtures use namespaced constant ids (`cc000000-…`), read no pre-existing operational
rows, and are rolled back, so the file is now green on a clean schema and non-mutating on a
populated one — the exact combination N11 asked for. The corresponding claim in
`docs/agents/HANDOFF.md:397` („lief in Fix-Runde 4 gegen den restaurierten
Produktionsschema-Snapshot mit Exit 0") matches what I measured, so the evidence-integrity
problem that carried the round-3 FAIL is closed.

**N12 (MINOR) — VERIFIED-FIXED.** `app/(neo)/neo/app/mitarbeiter/page.tsx:77-86` collects the
distinct `open_task_ids` across all loaded handovers and resolves their titles in a dedicated
`operational_tasks` query with **no** `status` filter and **no** `limit(300)`, then hands the
resolved `open_tasks` down to the client; `responsibility-client.tsx:326` renders
`handover.open_tasks` instead of looking ids up in the page's capped task list. This is now
byte-for-byte the same shape the other two surfaces use (`app/mitarbeiter/page.tsx:163-170`,
`app/(neo)/neo/app/ablaeufe/aufgaben/page.tsx:30-37`), so a cancelled or older handed-over
task keeps its title on all three. Scope is preserved: the new read is a service-role query
with explicit `.eq('tenant_id', actor.tenant_id).eq('location_id', locationId)` plus
`.in('id', handoverTaskIds)`, and it is skipped entirely when no handover carries task ids,
so no new data crosses a tenant or location boundary and no query is issued on the common
path. `handovers` is a server prop that is never mirrored into client state — `mutation()`
ends in `router.refresh()` (`responsibility-client.tsx:135`) — so `open_tasks` is guaranteed
present at every render and the new `handover.open_tasks.length` access cannot throw.

**N13 (MINOR) — VERIFIED-FIXED.** `HandoverDetail` now carries `styles.handoverDetail`
(`responsibility-client.tsx:352`) and `responsibility.module.css:33` defines it:
`font-size:11.5px;line-height:1.5;color:#334155` for the body, a 10px uppercase 800-weight
label, `gap:4px` plus `margin-top:10px` between blocks, and `ul{margin:0;padding-left:18px}`
with `li+li{margin-top:3px}`. The seven content blocks therefore no longer render at the
browser default 16px with a ~40px list indent inside an 10–11.5px card, on the
`minmax(330px,1fr)` grid or at the `max-width:700px` single-column breakpoint. One residual is
raised separately as N16.

**N14 (MINOR) — VERIFIED-FIXED.** Both recipient surfaces gate the „Notiz" block behind
`showLegacyNote()` (`app/mitarbeiter/my-operations.tsx:131`,
`app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:355`), which requires `note` to be
non-empty *and* different from `important_notes`, from `incidents`, and from the literal
`'Übergabe ohne zusätzliche Hinweise'`. Checked against the producer at
`app/api/operations/tasks/route.ts:69`, where `note` is `input.importantNotes ||
input.incidents || 'Übergabe ohne zusätzliche Hinweise'` and the structured columns are
written from the same trimmed zod values: importantNotes present → `note === important_notes`
→ hidden; importantNotes empty and incidents present → `note === incidents` → hidden; both
empty → `note` equals the sentinel → hidden. All three duplication cases from N14 are covered.
The N8 guarantee survives: the legacy `HandoverEditor` path
(`app/api/operations/responsibility/route.ts:325`) writes a user-supplied `note` (zod
`min(2)`) with `incidents`/`important_notes` null, so `showLegacyNote()` returns true and a
legacy handover is still fully visible before its recipient confirms.

**N15 (MINOR) — VERIFIED-FIXED.** `docs/agents/HANDOFF.md:396` restores the disclosure as its
own bullet: „Playwright ist nicht grün belegt: ohne `E2E_AUTH_STATE` überspringt die
packet-eigene Spec ihre vier authentifizierten Abläufe; der letzte Gesamtlauf endete … mit 6
bestandenen, 4 übersprungenen und 22 Verbindungsfehlern." That matches the spec on the branch
(`tests/e2e/operations-recurring-handover.spec.ts` self-skips without `E2E_AUTH_STATE`), and
the refreshed migration/test numbers from the N10 fix are kept. An integrator reading HANDOFF
alone can no longer conclude the E2E suite is green for this packet.

*„Fix round 4" reports all five items as FIXED and makes no REFUTED claim, so there is again
nothing to accept or reject as a refutation.*

### Regression check

`git show --stat 87ae5f9` limits the round-4 fix to five source files plus three documents.
I re-read each change:

- No migration, RPC, grant, RLS policy, API route, role gate or scope filter was touched.
- The two new server-side reads (`app/(neo)/neo/app/mitarbeiter/page.tsx:78-80`) are
  tenant- and location-scoped and conditional on `handoverTaskIds.length`.
- `.handoverDetail` is a new selector; it does not override `.handoverCard p`, `.handoverMeta`
  or the `max-width:700px` block, so no other card content shifts.
- The `open_tasks` field is now produced by all three handover pages and consumed by all three
  clients (`app/mitarbeiter/my-operations.tsx:70`, `:125`;
  `app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx:10`;
  `responsibility-client.tsx:54`, `:326`) — no surface reads a field its page does not send.
- `vitest`, `tsc`, `git diff --check` and the migration dry-run are all green (table above).

No regression found.

### New findings

#### N16 — MINOR — the Neo manager card collapses line breaks in handover content that the employee card preserves
`app/(neo)/neo/app/mitarbeiter/responsibility.module.css:33` and `app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:352`

The five structured fields and the legacy note are all entered in `textarea`s
(`app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx` — „`textarea value={handover[key]}`";
`HandoverEditor` — „`textarea rows={4} value={form.note}`"), so multi-line content is normal
input. The employee surface renders it with `whitespace-pre-line`
(`app/mitarbeiter/my-operations.tsx:130`, `HandoverFact`); the new `.handoverDetail` rule sets
`display:grid;gap:4px;color;font-size;line-height` but no `white-space`, so the Neo card falls
back to `normal`. Failure mode: „Bestände" written as one item per line („Kaffeebohnen leer /
Milch 2 Kartons / Backmischung nachbestellen") renders as one run-on sentence for the manager
who is about to press „Lesen & bestätigen", while the same handover renders as three lines for
the recipient in the Mitarbeiter app — two views of the same binding content that do not
agree. This is not a regression (the previous `.handoverCard p` rule had no `white-space`
either), but it is untreated by the N13 fix, which claims readable rendering „für Desktop und
Mobile". *Fix:* add `white-space:pre-line` to `.handoverDetail`, matching `HandoverFact`.

#### N17 — NOTE — the „no additional notes" sentinel is a bare literal in three files
`app/api/operations/tasks/route.ts:69`, `app/mitarbeiter/my-operations.tsx:131`, `app/(neo)/neo/app/mitarbeiter/responsibility-client.tsx:356`

The N14 fix works, but its correctness depends on the string
`'Übergabe ohne zusätzliche Hinweise'` being identical in the one place that writes it and the
two places that suppress it, and `showLegacyNote` itself is duplicated verbatim in the two
clients. Editing the producer's wording alone silently reinstates the N14 double-render on
both surfaces with no test to catch it — `tests/operations/recurring-tasks-handovers.test.ts`
does not cover `showLegacyNote`. Not a defect today; worth one shared constant (and one unit
test) the next time this area is touched.

### NOTES

- M8's residual is unchanged (the escalation e-mail lookback equals the cron cadence, so a
  slightly early run can still double-send). Carried forward, not re-raised as a finding.
- The report's „Fix round 4" gate line quotes `--configLoader runner`; `--configLoader native`
  also passes here with the same 35/35 files and 230/230 tests. Harmless inaccuracy, same as
  in earlier rounds.
- `next build` was excluded by the review instruction and is therefore taken on the report's
  word; every other number the report and HANDOFF.md state for this packet — 230/230 tests,
  `tsc` exit 0, dry-run exit 0, SQL smoke exit 0, Playwright not green — I reproduced or
  confirmed against the branch myself.
- Recommendation for the integrator: the packet is releasable. N16 is a one-declaration CSS
  addition and N17 is hygiene; neither blocks, and both can ride along with the next change in
  this area.

**VERDICT: PASS-WITH-MINORS** — 0 BLOCKER, 0 MAJOR, 1 new MINOR (N16), 1 new NOTE (N17); N11
(the round-3 MAJOR) and N12–N15 all verified fixed, and all 23 items closed in rounds 1–3
remain fixed.
