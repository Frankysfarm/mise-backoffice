# Claude review — Packet C-A (Bewerbungstests, Schulungen, Onboarding)

- Worktree: `/home/openclaw/factory/workspaces/codex-builder/runs/mise-neo/wt-c-a`
- Branch: `vollausbau/c-a`, base `e401d12`, reviewed range `e401d12..d60cc5e`
- Reviewed commits: `ca8a8f3` (implementation), `d60cc5e` (evidence docs)
- Mode: read-only. No source file was modified by this review.

## Verified gates (run by the reviewer in this worktree)

```
./node_modules/.bin/vitest run --configLoader native
  →  Test Files  37 passed (37)
     Tests      229 passed (229)
     Duration   15.04s
     exit code 0

./node_modules/.bin/tsc --noEmit -p .
  →  no diagnostics, exit code 0
```

`next build` was not run (explicitly out of scope for this review). The report's
claims of "tsc Exit 0" and "37 Dateien, 229 Tests" reproduce exactly; the report's
note that `--configLoader runner` was needed is not required here — the default
`native` loader also passes. Test/typecheck claims in
`docs/agents/reports/2026-08-31-c-a.md` are honest.

Verification limit: the base schema (`employees`, `audit_log`, `tenants`,
`training_modules`/`training_progress` fusion policies) is not present in
`supabase/migrations/`; it starts at `20260820065751`. Column existence and the
pre-existing `training_progress` RLS policies could therefore not be checked
against the repository. Findings that depend on them are marked below.

---

## BLOCKER

### BLOCKER 1 — `revoke all on table training_modules from anon,authenticated` breaks two existing, untouched features
`supabase/migrations/20260831100944_application_assessments_training_onboarding.sql:439`

The revoke removes `select`/`insert` for the `authenticated` role, but two code
paths still reach `training_modules` as `authenticated`, and neither was migrated
in this packet:

- `app/(admin)/training/ai-create/page.tsx:114` — a `'use client'` component that
  inserts a generated module through the **browser** client
  (`createClient` from `@/lib/supabase/client`). After this migration the insert
  returns `permission denied for table training_modules`, surfaced to the owner
  via `toastError('Speichern fehlgeschlagen', error.message)`. The entire
  "AI Training erstellen" flow — reachable from the Schulungen page header — dies.
- `app/(admin)/employees/[id]/page.tsx:35` — a server component using the
  **user-scoped** client (`createClient` from `@/lib/supabase/server`, line 24)
  with the embedded resource
  `training_progress.select('*,module:training_modules(titel,kategorie)')`.
  PostgREST requires `select` on the embedded table for the requesting role, so
  the Schulungen block on every employee detail page starts erroring.

Fix: keep `grant select on public.training_modules to authenticated` (RLS still
scopes rows) and port `ai-create`'s save to the new `/api/training/modules` route
inside this packet, exactly as `app/(admin)/training/new/page.tsx` was ported.

### BLOCKER 2 — `process_overdue_trainings` collides with the pre-existing `operational_tasks_source_uidx` and 500s the whole daily cron
`supabase/migrations/20260831100944_application_assessments_training_onboarding.sql:686-688`

The insert writes `source_type='training_overdue'`, `source_id=progress.id::text`,
but the `on conflict` target names only the new partial index
`operational_tasks_training_progress_uq` (`… where training_progress_id is not
null and status<>'storniert'`). `operational_tasks` already carries a **full**
unique index `operational_tasks_source_uidx on (tenant_id, source_type, source_id)`
(`supabase/migrations/20260828174510_unified_operations_and_table_ordering.sql:335`)
with no partial predicate.

Reproducible failure: a manager cancels the auto-created overdue-training task
(`status='storniert'` is reachable via `app/api/operations/responsibility/route.ts:53`).
The next cron tick no longer matches the partial index, falls through to a plain
insert, and violates `operational_tasks_source_uidx` → `unique_violation` →
`process_overdue_trainings` raises → `app/api/cron/operational-escalations/route.ts:36`
returns `{ok:false}` / HTTP 500. The daily Tagesklarheit run then reports failure
on every subsequent tick with no self-healing path.

Fix: drop the `status<>'storniert'` predicate from the partial index (or add
`on conflict (tenant_id,source_type,source_id) do nothing` as well) so both unique
indexes are covered by a conflict clause.

---

## MAJOR

### MAJOR 3 — Managers can read, rewrite and delete another location's Bewerbungstest
`app/(neo)/neo/app/tests/page.tsx:11`, `app/api/application-assessments/[id]/route.ts:9`, `:23-28`, migration `:520`

All three paths scope by `tenant_id` only. `save_application_assessment_template`
checks the manager's location against the **incoming** `p_location_id`
(migration `:487`), never against the location of the template being edited
(`where t.id=p_id and t.tenant_id=p_tenant_id`, line 520). A `manager` at
location A therefore sees location B's tests in the list, can open them, and can
save them — which forcibly relocates the template to location A — or `DELETE`
them outright with no location predicate at all. This is the tenant/location
scope invariant the reference `save_shift_operational_template` pattern exists to
enforce.

Fix: add `and (t.location_id is null or t.location_id = v_actor.location_id)` to
the edit lookup for `rolle='manager'`, and filter the list/GET/DELETE by
`actor.location_id` for managers.

### MAJOR 4 — Raw Postgres/English error text is rendered in owner- and applicant-facing UI
`app/api/application-assessments/route.ts:29`, `app/api/application-assessments/public/[token]/route.ts:33`, `:35`, `app/api/applications/[id]/review/route.ts:106`

Every one of these returns `error.message` from the RPC verbatim, and the UI
prints it: `app/(neo)/neo/app/tests/assessment-manager.tsx:21`, `:24`, `:25`
(`setMessage(data.error)` → red box) and `app/bewerbungstest/[token]/test.tsx:8`
(`setError(result.error)` → shown to the applicant). A German-speaking owner sees
`assessment does not match candidate department`, `question 3 is invalid`,
`manager is outside assessment location`; an applicant whose link expired mid-test
sees `assessment session is not active`. This directly contradicts owner order §1
("Die Bedienung darf keine technischen Kenntnisse erfordern"), §7 ("Technische
Daten … dürfen an keiner Stelle in der normalen Benutzeroberfläche erscheinen")
and coordination rule 6.

Fix: map the known RPC exception strings to German sentences in the route and
return a single generic German fallback for anything unmapped.

### MAJOR 5 — Migration grants `insert/update/delete` on `training_progress` to `authenticated` with no policy in this repo
`supabase/migrations/20260831100944_application_assessments_training_onboarding.sql:441`

Every read/write path this packet adds goes through `createServiceClient()`
(`app/mitarbeiter/schulungen/page.tsx:8`, `app/api/training/my/[id]/route.ts:9`,
`app/api/training/assign/route.ts:12`). The only remaining `authenticated` writer
is the browser insert at `app/(admin)/training/ai-create/page.tsx:135`. Granting
`insert,update,delete` to `authenticated` means the new quiz gate is only as
strong as a `training_progress` RLS policy that lives outside this repository —
if the fusion policy allows self-row updates, an employee can set
`status='bestanden', abgeschlossen=true` straight from the browser console and
skip every Wissenstest, and `training_attempts` (service-role only) will show no
attempt.

Fix: narrow to `grant select on public.training_progress to authenticated` and
route the `ai-create` assignment through `/api/training/assign`; if the write
grant must stay, add the self/manager policy explicitly in this migration so it
is reviewable.

### MAJOR 6 — Non-atomic quiz-key replacement can leave a module that everyone auto-passes at 100 %
`app/api/training/modules/route.ts:25-29`

Line 25 deletes all `training_module_targets` and `training_quiz_keys` for the
module; line 28 re-inserts them; line 29 reports the error. There is no
transaction. If the key insert fails (constraint, network, the
`points between 1 and 100` check), the module row is already saved and its answer
keys are gone. `app/api/training/my/[id]/route.ts:12` then computes
`max === 0 → score = 100 → passed = true` and writes `status='bestanden'` for
every employee who opens it. The same window exists for any employee submitting
between the DELETE and the INSERT.

Fix: move save+targets+keys into one `security definer` RPC (as the assessment
side already does), and treat "module has quiz blocks but zero keys" as
not-scoreable rather than as a 100 % pass.

### MAJOR 7 — A test targeted at more than one Bereich or Stelle can never be assigned from the Bewerbung
`app/(admin)/applications/[id]/page.tsx:102`

The filter is
`!(template.targets ?? []).some(t => t.target_type === 'department' && t.department_id !== application.department_id)`.
With targets `[Küche, Service]` and a candidate in Küche, the `Service` row makes
`.some(...)` true and the template is dropped. Identical bug on the position
branch. So exactly the case owner order §1 asks for — "unterschiedliche Tests für
verschiedene Stellen und Bereiche" — is the case the picker hides. It also
disagrees with `assign_application_assessment` (migration `:744-755`), which uses
"if any department target exists, one must match".

Fix: `const deps = targets.filter(t => t.target_type === 'department'); deps.length === 0 || deps.some(t => t.department_id === application.department_id)` and the same for positions.

### MAJOR 8 — Training description is rendered as raw HTML to every employee
`app/mitarbeiter/schulungen/[id]/runner.tsx:8`

`dangerouslySetInnerHTML={{ __html: progress.module.beschreibung }}` — but the
new editor collects `beschreibung` from a plain `<Textarea>`
(`app/(admin)/training/[id]/editor.tsx:21`) and `lib/training/domain.ts:16`
validates it only as `z.string().trim().max(5000)`. Any manager-or-above (or
anything that reaches `training_modules.beschreibung`) gets stored-XSS into every
assigned employee's session; nothing in the pipeline escapes or sanitises.

Fix: render it as text, e.g. `<p className="whitespace-pre-wrap">{progress.module.beschreibung}</p>`.

### MAJOR 9 — Two parallel scoring/status implementations; the unit tests exercise the dead one
`lib/application-assessments/domain.ts:45`, `lib/training/domain.ts:36`

`scoreAssessment` is imported only by `tests/application-assessments/domain.test.ts:2`
— no route calls it. The scoring that actually runs lives in SQL
(`complete_application_assessment`, migration `:632-641`). `trainingStatus` is
likewise only imported by `tests/training/domain.test.ts:2`, while the identical
rule is re-implemented inline at `app/mitarbeiter/schulungen/page.tsx:9` and
`app/(admin)/training/page.tsx:22`. This is the "keine parallelen Systeme"
violation (coordination rule 5) and it makes the report's "Domain-…belege" claim
misleading: the passing domain tests cover a code path that never executes in
production, so neither the SQL scorer nor the overdue rule is actually tested.

Fix: delete `scoreAssessment`, call `trainingStatus` from both render sites, and
test the SQL scorer against a real instance before integration.

### MAJOR 10 — `process_overdue_trainings` violates `operational_tasks.location_id NOT NULL` for employees without a location
`supabase/migrations/20260831100944_application_assessments_training_onboarding.sql:686`

The insert supplies `employee.location_id`, but `operational_tasks.location_id` is
`not null` (`20260828174510_…sql:299`) and `employees.location_id` is treated as
nullable throughout this same migration (`assign_matching_onboarding_trainings`
line 668 guards with `module.location_id is null or module.location_id =
v_employee.location_id`). One location-less employee with an overdue mandatory
training aborts the whole function and 500s the daily cron.

Fix: skip employees with `location_id is null` (or fall back to the module's
location) and log the skip.

---

## MINOR

### MINOR 11 — Nav rule: three edits to `shell.tsx`, not one
`app/(neo)/neo/app/shell.tsx:31`, `:49`, `:67`

Coordination rule 2 allows "exactly ONE line to the existing nav array … and
nothing else in that file". The packet also adds a `META` entry and extends
`isOperations`. Functionally correct and small, but it is the file every other
parallel packet is also told not to touch, so each extra hunk is a merge conflict.
Fix: integrator should confirm no other packet edits `:49`/`:67`.

### MINOR 12 — Second Playwright config duplicates the root one
`tests/e2e/playwright.c-a.config.ts:1`

Same `testDir`, same two projects (`chromium`, `Pixel 5`) as
`playwright.config.ts:12-44`; only the port (34279 vs 3200) and
`reuseExistingServer` differ. A parallel system per rule 5.
Fix: delete it and pass `--config playwright.config.ts` with `PORT` overridden.

### MINOR 13 — E2E covers one screen of a seven-step acceptance chain, with the server stubbed
`tests/e2e/application-training-flow.spec.ts:4-7`

Both GET and POST are `page.route`-intercepted with hand-written JSON, so the spec
proves the applicant screen renders and submits — nothing about the API, the RPCs,
scoring, the result in the Bewerbung, the hire, the onboarding auto-assignment, or
the employee's training list. The packet's "Done means" names all seven steps. The
report discloses the interception honestly; `docs/agents/HANDOFF.md:397` does not
("gezieltes Playwright Desktop + Mobile 2/2 grün").
Fix: add the scope caveat to the HANDOFF line so the integrator does not read it
as end-to-end coverage.

### MINOR 14 — `create unique index … training_progress_employee_module_uq` is not safe on a populated database
`supabase/migrations/20260831100944_application_assessments_training_onboarding.sql:376`

`if not exists` guards re-creation, not pre-existing duplicates. The legacy bulk
assignment at `app/(admin)/training/ai-create/page.tsx:135` inserts
`(employee_id, module_id)` rows with no conflict guard, so duplicates are
plausible in production; the index build then aborts the whole migration. The same
class applies to `assessment_templates_scope_slug_uq` (`:36`).
Fix: add a `delete from … using …` de-duplication step immediately before each
new unique index.

### MINOR 15 — Cross-shell hard-coded links bypass `operationsBasePath`
`app/(admin)/applications/page.tsx:57`, `app/(admin)/applications/[id]/assessment-assignment.tsx:7`

Both hard-code `/neo/app/tests` although the surrounding file resolves its links
through `operationsBasePath('/applications', '/neo/app/bewerbungen')`
(`app/(admin)/applications/[id]/page.tsx:19`). An owner working in the mise-os
admin shell is thrown into the Neo shell.
Fix: add a `tests` entry to `operationsBasePath` and use it in both places.

### MINOR 16 — Raw status slugs rendered as user-visible badge labels
`app/mitarbeiter/schulungen/page.tsx:10`, `app/(admin)/training/[id]/editor.tsx:26`

Only `ueberfaellig` is mapped to `überfällig`; `offen`, `begonnen` and `bestanden`
are printed as the lowercase database values.
Fix: use one label map (`{offen:'Offen', begonnen:'Begonnen', bestanden:'Bestanden', ueberfaellig:'Überfällig'}`).

### MINOR 17 — Managers cannot use the "Alle Standorte" option the UI offers them
`app/(neo)/neo/app/tests/assessment-manager.tsx:27`, migration `:487`

The select offers `<option value="">Alle Standorte</option>`, but
`save_application_assessment_template` raises whenever `rolle='manager'` and
`p_location_id is distinct from v_actor.location_id` — which `null` always is. The
manager gets the English exception from MAJOR 4 and no explanation.
Fix: hide the "Alle Standorte" option for managers (or allow `p_location_id is null`
to default to the manager's own location).

### MINOR 18 — Blanket type suppression in the new owner-facing editor
`app/(admin)/training/[id]/editor.tsx:12`, `:23`

`type Block = any` plus a file-level-looking `{/* @ts-expect-error */}` above the
whole content-block render. `tsc` passes, but nothing in this editor is
type-checked, including the block discriminants the API's Zod schema depends on.
Fix: type `Block` from `lib/training/domain.ts`'s `contentBlockSchema` and drop
the suppression.

---

## NOTE

### NOTE 19 — RLS pattern deviates from the mandated reference
New tables receive only `packet_ca_service_all … to service_role` plus revoked
grants (migration `:406-424`), rather than the tenant+location `authenticated`
policies the reference `save_shift_operational_template` migration uses
(`20260830113000_…sql:95-217`). Server-only access is a defensible, stricter
choice and the report documents it — but it is not the pattern coordination
rule 1 names, so the integrator should confirm this is an accepted deviation.

### NOTE 20 — `submit_application_assessment_response` authorises on session id alone
Migration `:824`. Unlike `start_application_assessment` and
`complete_application_assessment`, it takes no `p_token_hash`. Not exploitable
today: execute is granted to `service_role` only (`:872`) and
`app/api/application-assessments/public/[token]/route.ts:29` resolves the session
from the token hash first. Worth tightening for symmetry.

### NOTE 21 — Automatic rejection leaves no `audit_log` trail
Migration `:645` — a failing test with `failAction='reject'` sets
`employees.status='abgelehnt'` and `next_stage` can flip `wartet_zuteilung` →
`in_probe`, both without a `public.audit_log` row (only `assessment_audit_logs`)
and without notifying the manager. The behaviour is what owner order §1 asks for;
the missing auditability of an automated, destructive status change is the gap.
The UI default is `manual_review`, which limits blast radius.

### NOTE 22 — Coordination compliance that does hold
Migration timestamp `20260831100944` is inside C-A's reserved
`202608311[0-1]…` range and it is the only migration added. The file uses explicit
`begin;`/`commit;`, `create table if not exists`, `alter table … add column if not
exists`, `create index if not exists`, `drop policy if exists` before `create
policy`, and `security definer` + `set search_path=public,pg_temp` on every RPC.
New code stayed inside the packet's areas; no existing export was renamed; the
Neo `schulungen`/`bewerbungen` pages re-export the admin pages, so the new header
link appears in both shells without duplicating a page. Answer keys are correctly
kept out of the browser: `training_quiz_keys` is service-role only,
`app/api/training/modules/route.ts:15` strips `correctOptionIds` from
`training_modules.inhalt`, and the applicant payload
(`assessment_session_items.candidate_payload_json`) carries no scoring config.
Multi-select comparison is order-safe on both sides
(`app/api/application-assessments/route.ts:27` and `public/[token]/route.ts:31`
both `.sort()` before the jsonb equality at migration `:634`).

---

## VERDICT

VERDICT: FAIL — 2 BLOCKER, 8 MAJOR, 8 MINOR, 4 NOTE. Not integrable until
BLOCKER 1 (revoked `training_modules` grant breaks `ai-create` and the employee
detail page) and BLOCKER 2 (`process_overdue_trainings` unique-index collision
500s the daily cron) are fixed; MAJOR 3, 5, 7 and 8 must also be resolved before
release because they concern location scope, browser write privileges on
completion records, a core acceptance criterion, and stored XSS.

---

# Re-review round 1

- Re-reviewed range: `e401d12..8e7b03d` (fix commits `6199590`, `8e7b03d`).
- Implementer answers: `docs/agents/reports/2026-08-31-c-a.md` § `Fix round 1`.
- Mode: read-only. No source file was modified by this re-review.

## Verified gates (run by the reviewer in this worktree)

```
./node_modules/.bin/vitest run --configLoader native
  →  RUN v4.1.10 /…/runs/mise-neo/wt-c-a
     Test Files  37 passed (37)
     Tests      228 passed (228)
     exit code 0

./node_modules/.bin/tsc --noEmit -p .
  →  no diagnostics, exit code 0
```

Note on method: a first vitest run wrote to a shared `/tmp` log that a parallel
packet agent overwrote (the log showed root `…/wt-c-e`). The numbers above come
from a re-run with a private log path and are confirmed to be this worktree.

## Migration dry-run against the production schema snapshot

Only migration touched on this branch: `20260831100944_application_assessments_training_onboarding.sql`
(the file already existed at `e401d12` as an interrupted-run draft; this branch
completes it). The shared `dryrun` database of
`migration-dryrun.sh` is raced by parallel packet agents, so the run below was
repeated in an isolated database `dryrun_ca` on the same container/snapshot.

```
restore: 0 errors, tables=243
OK   20260831100944_application_assessments_training_onboarding.sql       (exit 0)
```

DDL apply: **PASS**. Post-apply runtime check on the same database:

```
dryrun_ca=# select public.process_overdue_trainings();
ERROR:  there is no unique or exclusion constraint matching the ON CONFLICT specification
CONTEXT:  SQL statement "insert into public.operational_tasks(… ) … on conflict(tenant_id,source_type,source_id) do nothing"
PL/pgSQL function process_overdue_trainings(timestamp with time zone) line 6 at SQL statement
```

See BLOCKER 2 below.

## Per-finding verdicts

### BLOCKER 1 — NOT-FIXED (partially)

The grant half is fixed and verified in the dry-run database:
`training_modules` now has `SELECT` for `authenticated`
(migration `:450`), so `app/(admin)/employees/[id]/page.tsx:35`
(`training_progress` → embedded `training_modules`) works again.

The second half — porting `ai-create`'s save — is done but with a payload the
receiving schema rejects. `app/(admin)/training/ai-create/page.tsx:114` posts
`blocks: result.inhalt.lessons` straight from `/api/ai/generate-training`, whose
`SYSTEM_PROMPT` (`app/api/ai/generate-training/route.ts:14-28`) emits
`{type:'info', title, body}` and `{type:'quiz', options: string[], correct: number}`.
`trainingModuleSchema.blocks` (`lib/training/domain.ts:29` → `:3-13`) is a
discriminated union over `text|image|video|document|quiz` with
`options: {id,label}[]` + `correctOptionIds/points/mustPass`. Verified by parsing
the exact AI payload against that schema with the repo's zod:

```
invalid_union_discriminator at [0,"type"] — Expected 'text'|'image'|'video'|'document'|'quiz'
invalid_type at [1,"options",0..3] — Expected object, received string
```

`app/api/training/modules/route.ts:9` therefore returns HTTP 400 and
`ai-create` shows `toastError('Speichern fehlgeschlagen', 'Bitte Schulung und
Inhalte vollständig ausfüllen.')` for **every** generated module. The "AI Training
erstellen" flow reachable from the Schulungen header is still dead — the same
feature BLOCKER 1 was raised about, now failing at validation instead of at
`permission denied`.

Fix: map the generator output to the canonical block shape (`info`→`text`;
`options: string[]` + `correct: number` → `{id,label}[]` + `correctOptionIds`,
`points`, `mustPass`) either in `/api/ai/generate-training` or in `saveModule()`,
and cover it with a unit test on the mapper.

### BLOCKER 2 — NOT-FIXED; the fix makes the failure unconditional

`migration :733` now reads `on conflict(tenant_id,source_type,source_id) do nothing`
with the partial-index predicate dropped from
`operational_tasks_training_progress_uq` (`:418-420`). But the arbiter it targets,
`operational_tasks_source_uidx`, **is itself partial**
(`20260828174510_…sql:335-337`, `where source_type is not null and source_id is not null`).
Postgres cannot infer a partial index unless the `ON CONFLICT` clause carries an
`index_predicate` that implies it. The repo's own reference does exactly that
(`20260830113000_shift_linked_operational_tasks.sql:525-526`:
`on conflict(tenant_id,source_type,source_id) where source_type is not null and source_id is not null`);
the new function omits it.

Reproduced against the production schema snapshot with this migration applied,
on an **empty** `training_progress` (no overdue rows, no cancelled task needed):

```
select public.process_overdue_trainings();
ERROR:  there is no unique or exclusion constraint matching the ON CONFLICT specification
```

and the isolated control, which succeeds:

```
… on conflict(tenant_id,source_type,source_id)
  where source_type is not null and source_id is not null do nothing;   -- INSERT 0 0
```

Impact is strictly worse than before the fix: previously the daily run broke only
after a manager cancelled an auto-created task; now
`app/api/cron/operational-escalations/route.ts` returns `{ok:false}`/HTTP 500 on
**every** tick from the first deploy, because the statement fails at plan time
regardless of row count. The dry-run's `exit 0` does not cover this — plpgsql
bodies are not planned at `CREATE FUNCTION` time.

Fix: append `where source_type is not null and source_id is not null` to the
conflict target, and keep the `training_progress_id` index non-partial as it now is.

### MAJOR 3 — VERIFIED-FIXED

`app/(neo)/neo/app/tests/page.tsx:11`, `app/api/application-assessments/route.ts:14`
and `[id]/route.ts:6-8` (used by both GET `:14` and DELETE `:28`) scope managers to
`location_id.is.null,location_id.eq.<own>`; the DELETE/archive paths are gated by
that same lookup. The edit branch of `save_application_assessment_template`
(migration `:565`) now carries
`and (v_actor.rolle<>'manager' or t.location_id is null or t.location_id=v_actor.location_id)`,
so a manager can no longer relocate another location's template. See new MINOR 26
for the `location_id is null` manager edge case.

### MAJOR 4 — NOT-FIXED (partially)

`lib/application-assessments/errors.ts` plus the mapped call sites
(`app/api/application-assessments/route.ts:16`, `:32`; `[id]/route.ts:33`, `:36`;
`public/[token]/route.ts:33`, `:36`; `applications/[id]/review/route.ts:107`)
cover the applicant flow and the template CRUD. But
`app/api/application-assessments/assign/route.ts:16` still returns
`NextResponse.json({ error: error.message })` verbatim, and that is precisely the
route whose RPC raises the English strings quoted in the original finding:
`assessment does not match candidate department` (migration `:794`),
`assessment does not match candidate position` (`:800`),
`manager is outside candidate location` (`:787`),
`assessment template is outside candidate location` (`:784`),
`candidate already has an open assessment` (`:806`),
`assessment has no published questions` (`:811`).
`app/(admin)/applications/[id]/assessment-assignment.tsx:8` renders it directly:
`toastError('Test konnte nicht zugewiesen werden', data.error)`. An owner clicking
"Test zuweisen" still gets English Postgres exception text.

Fix: wrap line 16 in `assessmentErrorMessage(...)` and add the six `assign_…`
strings above to the map.

### MAJOR 5 — VERIFIED-FIXED

Confirmed against the dry-run database:
`training_progress` → `authenticated` holds `SELECT` only;
`training_modules` → `authenticated` holds `SELECT` only; all write privileges are
`service_role`. Grepping `app/`, `components/`, `lib/` finds no remaining browser
or `authenticated`-client write to either table — the six remaining call sites are
all reads. The quiz gate can no longer be bypassed from the browser console.

### MAJOR 6 — VERIFIED-FIXED

`app/api/training/modules/route.ts:19` now calls the single `save_training_module`
RPC (migration `:461-479`), which does module upsert + target/key replace inside one
server-side statement, so the delete/insert window is gone. The 100 % auto-pass is
closed at `app/api/training/my/[id]/route.ts:15-16`: `quizCount > 0 && !keys?.length`
returns HTTP 409 instead of scoring.

### MAJOR 7 — VERIFIED-FIXED

`app/(admin)/applications/[id]/page.tsx:103` now uses
`departments.length === 0 || departments.some(...)` and the same shape for
positions, matching `assign_application_assessment` (migration `:794`/`:800`). A
template targeted at Küche + Service is now offered for a Küche candidate.

### MAJOR 8 — VERIFIED-FIXED

`app/mitarbeiter/schulungen/[id]/runner.tsx:20` renders
`<p className="mt-2 whitespace-pre-wrap …">{progress.module.beschreibung}</p>`;
`dangerouslySetInnerHTML` is gone (commit `8e7b03d`). No other
`dangerouslySetInnerHTML` was introduced in this packet.

### MAJOR 9 — VERIFIED-FIXED

`scoreAssessment` is removed from `lib/application-assessments/domain.ts` and its
dead test with it. `trainingStatus` is now called from both production render
sites: `app/mitarbeiter/schulungen/page.tsx:10` and
`app/(admin)/training/page.tsx:24`. The "SQL scorer untested" part remains an open
pre-integration item, as the report states.

### MAJOR 10 — VERIFIED-FIXED (currently moot)

Migration `:732` adds `and employee.location_id is not null`, so a location-less
employee no longer violates `operational_tasks.location_id NOT NULL`. The guard is
correct but unreachable while BLOCKER 2 aborts the same statement.

### MINOR 11 — NOT-FIXED (SKIPPED, rationale accepted)

`shell.tsx` was not touched again in round 1. Reverting the two extra hunks now
would break navigation/metadata for no safety gain; the integrator conflict note
stands as originally written.

### MINOR 12 — VERIFIED-FIXED

`tests/e2e/playwright.c-a.config.ts` is deleted.

### MINOR 13 — VERIFIED-FIXED

`docs/agents/HANDOFF.md:399` now reads "gezieltes, API-simuliertes Playwright für
den Bewerberbildschirm … (kein Ende-zu-Ende-/Datenbankbeleg)". See new MINOR 27 for
the stale test count on that same line.

### MINOR 14 — VERIFIED-FIXED as written (but see new MAJOR 24 / MINOR 25)

Deterministic de-duplication now precedes both new unique indexes
(migration `:36-41` for `assessment_templates`, `:382-384` for `training_progress`).
The de-duplication *rule* itself introduces two new findings below.

### MINOR 15 — NOT-FIXED (regression) → escalated to new MAJOR 23

Both call sites now resolve the link through
`operationsBasePath('/application-tests', '/neo/app/tests')`
(`app/(admin)/applications/page.tsx:30`, `[id]/page.tsx:20`), but the legacy path
`/application-tests` does not exist. See MAJOR 23.

### MINOR 16 — NOT-FIXED (SKIPPED, acknowledged)

`trainingStatusLabels` was added to `lib/training/domain.ts:37` but is used
nowhere. `app/mitarbeiter/schulungen/page.tsx:11` and
`app/(admin)/training/[id]/editor.tsx:26` still print
`{status === 'ueberfaellig' ? 'überfällig' : status}`, i.e. raw `offen` /
`begonnen` / `bestanden` slugs to employees and owners. Open, plus two dead
imports (new MINOR 28).

### MINOR 17 — NOT-FIXED

Neither remedy was applied. `app/(neo)/neo/app/tests/assessment-manager.tsx:27`
still renders `<option value="">Alle Standorte</option>` for managers, and the
server rule is unchanged (migration `:531-533`:
`if v_actor.rolle='manager' and p_location_id is distinct from v_actor.location_id then raise`).
The only change is the initial state at `:17` (`locationId: managerLocationId ?? ''`),
and that default is discarded by the primary path: the "Test anlegen" button at
`:26` and the post-save reset at `:24` both call `setForm({ ...blank, … })`, and
`blank.locationId` is `''` (`:14`). A manager creating a new test therefore still
starts on "Alle Standorte" and still gets a rejection — now in German
("Dieser Test gehört zu einem anderen Standort."), which is an improvement, but
the offered option still cannot succeed.

### MINOR 18 — VERIFIED-FIXED

`app/(admin)/training/[id]/editor.tsx:11-13` types `Block` as
`TrainingContentBlock`; the blanket `@ts-expect-error` is gone and the remaining
comment at `:23` is prose only. `tsc` is clean.

### NOTE 19 — REFUTATION-ACCEPTED

Service-role-only access is stricter than browser RLS and the canonical tables are
preserved. Recorded as an accepted deviation from the reference pattern; nothing
further required from the implementer.

### NOTE 20 — open (SKIPPED, accepted)

Still not exploitable: `submit_application_assessment_response` is `service_role`
only (migration `:914`) and `public/[token]/route.ts:27` resolves the session by
token hash first. Left as a symmetry improvement.

### NOTE 21 — open (SKIPPED)

Automatic `abgelehnt` / `in_probe` transitions (migration `:698-699`) still write
only `assessment_audit_logs`, not `public.audit_log`. Unchanged, and out of the
round's scope as the implementer states.

### NOTE 22 — still holds

Re-checked: single migration in the reserved timestamp window, explicit
`begin`/`commit`, idempotent DDL, `security definer` + `set search_path` on every
new RPC (including `save_training_module`, `:459`). Answer keys remain out of the
browser: `training_quiz_keys` is service-role only and
`app/api/training/modules/route.ts:15` still strips `correctOptionIds`, `points`
and `mustPass` from `training_modules.inhalt`.

## New findings

### NEW MAJOR 23 — "Bewerbungstests verwalten" is a 404 in the mise-os admin shell
`app/(admin)/applications/page.tsx:30`, `:58`; `app/(admin)/applications/[id]/page.tsx:20`; `app/(admin)/applications/[id]/assessment-assignment.tsx:7`

The MINOR 15 fix routes both links through
`operationsBasePath('/application-tests', '/neo/app/tests')`.
`operationsBasePath` (`lib/routing/operations-base-path.ts:5`) returns the **legacy**
argument for any pathname that is not under the Neo path — so on `/applications`
it returns `/application-tests`. There is no `app/(admin)/application-tests/`
directory, no `app/application-tests/`, and no rewrite or redirect in
`next.config.js:11-15`. An owner in the admin shell who clicks "Bewerbungstests
verwalten", or the "Test anlegen" link shown when no test matches a candidate,
lands on a 404 — the entry point to the entire packet feature. The previous
hard-coded `/neo/app/tests` was cross-shell but at least resolved.

Fix: point the legacy argument at a real route (`/neo/app/tests`, or add an
`app/(admin)/application-tests/page.tsx` that re-exports the Neo page the way
`schulungen`/`bewerbungen` already do).

### NEW MAJOR 24 — the `training_progress` de-duplication silently deletes passed/certified rows
`supabase/migrations/20260831100944_…sql:382-384`

```sql
delete from public.training_progress duplicate
using public.training_progress keeper
where duplicate.id>keeper.id and duplicate.employee_id=keeper.employee_id and duplicate.module_id=keeper.module_id;
```

The survivor is chosen by lowest `id` (a random uuid), not by state. The realistic
duplicate shape in production is exactly the one MINOR 14 described — the legacy
`ai-create` bulk assignment re-inserting `(employee_id, module_id)` — so the pair is
typically one stale `offen` row and one later row that may be `bestanden` with
`testergebnis`, `passed_at` and `abgeschlossen=true`. Whichever uuid sorts lower
wins, so a completed mandatory-training record can be destroyed. `training_attempts`
rows for the deleted progress cascade away with it (FK `on delete cascade`,
verified on the snapshot). The delete emits no `raise notice`, writes no
`audit_log`, and is irreversible after `commit`.

Fix: order the de-duplication by state, e.g. keep
`(abgeschlossen desc, passed_at desc nulls last, fortschritt_prozent desc, id)`
per `(employee_id, module_id)`, and `raise notice` the number of rows removed so
the deploy log records it.

### NEW MINOR 25 — the `assessment_templates` de-duplication can abort the migration
`supabase/migrations/20260831100944_…sql:36-41`

`assessment_template_versions` cascades from `assessment_templates`, but
`assessment_sessions.template_version_id` references it with `NO ACTION`
(verified on the snapshot: `assessment_sessions → assessment_template_versions = 'a'`).
If a duplicate-slug template has any published version with a session, the delete
raises a foreign-key violation and rolls back the whole migration — the same class
of deploy abort MINOR 14 asked to prevent, moved one step earlier.

Fix: restrict the de-duplication to templates with no sessions, or archive
(`status='RETIRED'` + slug suffix) instead of deleting.

### NEW MINOR 26 — a manager without a location gets a malformed PostgREST filter
`app/(neo)/neo/app/tests/page.tsx:11`, `app/api/application-assessments/route.ts:14`, `app/api/application-assessments/[id]/route.ts:7`

All three interpolate `location_id.eq.${actor.location_id}`. `employees.location_id`
is nullable and this packet treats it as nullable elsewhere (migration `:732`), so a
manager with no location produces `location_id.eq.null`, which PostgREST sends as
`= 'null'::uuid` → `invalid input syntax for type uuid`. The list page then renders
"Noch keine Bewerbungstests" and the API returns the generic German error, with no
indication that the account is misconfigured. Not a scope leak — the failure mode is
closed — but it is silent.

Fix: build the filter as `location_id.is.null` alone when `actor.location_id` is null.

### NEW MINOR 27 — HANDOFF still reports the pre-fix test count
`docs/agents/HANDOFF.md:399`

The line reads "Vitest 37 Dateien/229 Tests grün". After the MAJOR 9 fix removed the
dead `scoreAssessment` test the suite is 37 files / **228** tests, as both the
report's Fix-round-1 gate block and my run above show.

### NEW MINOR 28 — dead imports of the new label map
`app/(admin)/training/[id]/editor.tsx:11`, `app/mitarbeiter/schulungen/page.tsx:6`

`trainingStatusLabels` is imported in both files and used in neither (see MINOR 16).
Either finish MINOR 16 or drop the imports; leaving them suggests the labels are in
use when they are not.

### NEW NOTE 29 — training-assignment failures are reported as Bewerbungstest failures
`app/api/applications/[id]/review/route.ts:107`

`assessmentErrorMessage(trainingError.message)` is applied to
`assign_matching_onboarding_trainings`, whose exceptions
(`actor may not assign trainings`, `employee is outside tenant`, migration `:709`/`:711`)
are not in the map, so the owner is told
"Der Bewerbungstest konnte nicht verarbeitet werden" after a *hire* whose onboarding
assignment failed. Non-technical and German, so §7 holds; the wording just points at
the wrong feature.

### NEW NOTE 30 — legacy AI modules still ship their answer index to the employee browser
`app/mitarbeiter/schulungen/[id]/page.tsx:5`

The page selects `module:training_modules(…,inhalt)` wholesale. Modules created by
the pre-packet `ai-create` path store `{type:'quiz', options: string[], correct: 0}`
inside `inhalt`, so `correct` reaches the client for that legacy data. Not
exploitable for scoring — `app/api/training/my/[id]/route.ts:15-16` returns 409 for
a quiz module with no `training_quiz_keys` rows — and new saves are stripped at
`app/api/training/modules/route.ts:15`. Worth a one-off backfill that strips
`correct` from existing `inhalt` when the legacy modules are migrated.

## VERDICT

VERDICT: FAIL — 2 BLOCKER open (1 NOT-FIXED, 2 NOT-FIXED and now unconditional),
3 MAJOR open (4 partial, 23 new, 24 new), 7 MAJOR verified fixed, 7 MINOR open
(11, 15→23, 16, 17, 25, 26, 27, 28), 4 MINOR verified fixed, 1 refutation accepted
(19), 4 NOTE open (20, 21, 29, 30). Gates pass (vitest 37/228 exit 0, tsc exit 0)
and the migration applies cleanly to the production schema snapshot, but
`process_overdue_trainings` raises on every invocation against that same schema,
so the daily operational cron is broken from the first deploy. Not integrable.
