# Claude Review — Packet C-F (Visueller Ablauf-Editor + geführte Checkliste)

- Branch: `vollausbau/c-f`, Basis `e401d12`, Kopf `91e40b9`
- Diff: `git diff e401d12..HEAD` — 13 Dateien, +416/−77
- Rolle: unabhängiges, read-only Review. Es wurde keine Quelldatei verändert.

## VERDICT

**FAIL** — 3 BLOCKER, 5 MAJOR, 6 MINOR, 5 NOTE.

## Verifizierte Test-/Typecheck-Ergebnisse (selbst ausgeführt)

```
$ ./node_modules/.bin/vitest run --configLoader native
 Test Files  35 passed (35)
      Tests  227 passed (227)
   Duration  7.13s
 EXIT=0

$ ./node_modules/.bin/tsc --noEmit -p .
 (keine Ausgabe)
 EXIT=0
```

Beide Angaben aus `docs/agents/reports/2026-08-31-c-f.md` sind korrekt und ehrlich
(227 = 222 Bestand + 5 neue in `tests/ablaeufe/procedure-schema.test.ts`).
`next build` wurde auftragsgemäß NICHT ausgeführt; die Behauptung „228/228" im
Report ist von mir daher nicht verifiziert.

Zusätzlich habe ich die Migration real ausgeführt: Wegwerf-Container
`postgres:16` mit Stub-Schema (`tenants`, `locations`, `departments`,
`shift_guides`, `operational_tasks`, `current_tenant_id()`,
`can_access_operational_location()`, `can_manage_operational_location()`,
Rolle `authenticated`) und drei realistischen Legacy-Leitfäden. Ergebnisse siehe
BLOCKER-1 und MAJOR-1. Der Container wurde wieder entfernt.

---

## BLOCKER

**BLOCKER-1 — Migration bricht bei Legacy-Schritten ab, die reine Strings sind**
`supabase/migrations/20260831150000_visual_shift_guide_editor.sql:59`
Verifiziert, nicht vermutet: mit einem Leitfaden der Form
`{"categories":[{"name":"Check","steps":["Kasse zaehlen","Toilette pruefen"]}]}`
bricht die Migration mit `ERROR: cannot delete from scalar` ab und rollt die
komplette Transaktion zurück — inklusive Spalten, Index, RLS und Policies. Der
Ausdruck `(s.value - array[...])` setzt ein jsonb-Objekt voraus.
Dass dieser Fall real erwartet wird, zeigt `lib/ablaeufe/schema.ts:52`, wo der
TS-Normalizer String-Schritte ausdrücklich als `{ title: step }` abfängt — die
SQL-Variante tut das nicht. Der Report bestätigt, dass die Migration nie gegen
eine DB lief; genau dieser Pfad ist damit ungetestet in die Deploy-Kette gegangen.
*Fix:* `case when jsonb_typeof(s.value)='object' then s.value - array[...] else '{}'::jsonb end`
verwenden und `s.value->>'title'` für Skalare auf `s.value #>> '{}'` zurückfallen lassen.

**BLOCKER-2 — Editor löscht beim Speichern still alle Kategorien außer der ersten**
`app/(admin)/shift-guides/[id]/editor.tsx:19-20`
`const steps = form.content.categories[0]?.steps ?? []` und
`categories: [{ ...current.content.categories[0], steps: next }]` reduzieren den
Inhalt auf ein einziges Kategorien-Objekt. Sobald der Owner einen Schritt
hinzufügt, löscht, ändert oder sortiert, wird beim Speichern der gesamte Rest
des Ablaufs überschrieben. Der kanonische Speicher ist laut Audit
`inhalt.categories[].steps[]` (Mehrzahl); mein Fixture „Barista – Öffnung" mit
den Kategorien Vorbereitung/Maschine/Abschluss überlebt die Migration korrekt mit
`cats = 3` und würde beim ersten Editor-Speichern auf 1 Kategorie mit 2 Schritten
zusammenfallen. Datenverlust ohne Warnung, ohne Undo; die Versionszählung
(`version+1`) verdeckt ihn zusätzlich. Kategorien ab der zweiten sind im Editor
außerdem überhaupt nicht sichtbar, der Owner sieht den Verlust also nicht kommen.
*Fix:* alle Kategorien rendern und beim Patchen nur die betroffene Kategorie
ersetzen (`categories.map((c,i) => i===catIndex ? {...c, steps:next} : c)`).

**BLOCKER-3 — Roher JSON-Editor im Abläufe-Bereich bleibt bestehen**
`app/(admin)/checkups/[id]/editor.tsx:22,28,69` (erreichbar über
`app/(neo)/neo/app/ablaeufe/kontrollen/[id]/page.tsx`, das diese Seite re-exportiert)
Das Packet verlangt wörtlich: „Replace every place where raw JSON is rendered in
the normal UI with the guided view (grep for JSON.stringify / <pre> in the
ablaeufe area)", und benennt `kontrollen` in der Audit-Liste. Dort steht weiterhin
`fragen: JSON.stringify(tpl.fragen ?? { tasks: [] }, null, 2)`, ein Label
„Aufgaben (JSON: tasks[] mit id, title, description, requiresPhoto, estMin)" und
die Fehlermeldung „Aufgaben-JSON ungültig". Owner-Auftrag Punkt 7: „Technische
Daten oder JSON-Code dürfen an keiner Stelle in der normalen Benutzeroberfläche
erscheinen." Die Beschwerde des Owners ist einen Klick neben dem reparierten
Bildschirm unverändert reproduzierbar. Gespeichert wird dort zudem weiterhin per
Client-`supabase.update()` ohne serverseitige Validierung.
*Fix:* `checkup_templates.fragen` auf dasselbe `procedureContentSchema` und
dieselbe Editor-Komponente heben (kein zweiter Editor), sonst bleibt das
Akzeptanzkriterium unerfüllt.

---

## MAJOR

**MAJOR-1 — Leitfäden ohne `department_id` verlieren jeden Scope und verschwinden nach dem RLS-Einschalten**
`supabase/migrations/20260831150000_visual_shift_guide_editor.sql:10-14,23,30-32`
Der Backfill zieht `tenant_id`/`location_id` ausschließlich über
`from public.departments d where d.id=g.department_id`. Danach wird auf derselben
Tabelle RLS aktiviert, und beide Policies fordern `tenant_id=public.current_tenant_id()`.
Verifiziert im Container: ein standortweiter Leitfaden ohne Bereich behält
`tenant_id`/`location_id` NULL, und `set role authenticated; select titel from
public.shift_guides;` liefert ihn nicht mehr zurück — er ist für jeden
angemeldeten Nutzer unsichtbar, in keiner Liste mehr auffindbar und über
`app/api/ablaeufe/executions/route.ts:19` auch nicht mehr startbar. „Standortweit"
ist im neuen Editor eine ausdrücklich angebotene Option
(`editor.tsx:36`, `empty="Standortweit"`), der Fall ist also nicht exotisch.
*Fix:* Fallback-Backfill über den einzigen Tenant bzw. den Standort ergänzen und
die Migration abbrechen lassen, solange nach dem Backfill noch
`tenant_id is null`-Zeilen existieren (`raise exception`), statt sie stumm zu verstecken.

**MAJOR-2 — Pflichtnachweise sind serverseitig fälschbar**
`app/api/ablaeufe/executions/route.ts:9,27`
`evidence: z.record(z.unknown())` kommt ungeprüft vom Client, und
`canCompleteProcedure` (`lib/ablaeufe/schema.ts:84`) prüft nur
`Boolean(evidence[step.id])`. Ein POST mit
`{"action":"finish","taskId":"…","completed":{"step-1-2":true},"evidence":{"step-1-2":true},"values":{}}`
schließt einen Pflichtschritt „Foto" ab, ohne dass je eine Datei hochgeladen
wurde; die Zeilen in `operational_task_evidence` werden nie gegengeprüft. Das
Gating existiert damit faktisch nur im Browser. Bei Hygiene- und
Temperaturkontrollen (Owner-Auftrag Punkt 7) sind das fälschbare
Compliance-Nachweise.
*Fix:* im `finish`-Zweig `operational_task_evidence` für die Task-ID laden und je
Pflichtschritt mit `evidence='photo'` eine reale Nachweis-Zeile verlangen;
`confirmation`/`value` serverseitig als `unterschrift`/`messwert`-Evidence schreiben.

**MAJOR-3 — Abschluss umgeht den kanonischen Statuspfad und die Vier-Augen-Kontrolle (paralleles System)**
`app/api/ablaeufe/executions/route.ts:21,29`
Der Abschluss erfolgt als direkter Service-Role-`update({status:'erledigt'})`.
Der kanonische Weg ist `update_operational_task_as_actor`
(`supabase/migrations/20260830183000_predeploy_operational_hardening.sql:20-79`,
benutzt in `app/api/operations/responsibility/route.ts:300`). Umgangen werden
dadurch: die DB-seitige Pflichtnachweisprüfung, `reviewed_by`/`reviewed_at`, das
`set_config('app.audit_employee_id',…)` und die Freigabe-Kaskade auf
`operational_task_evidence.verification_status` (bleibt dauerhaft `offen`).
Verschärfend setzt `start` `assigned_to = accountable_employee_id =
controller_employee_id = actor.id`: der Mitarbeiter ist sein eigener Prüfer, was
die Kontrolle aus `20260830183000:42-51` („actor may not review or close task")
konstruktiv aushebelt. Owner-Auftrag Punkt 4 verlangt ausdrücklich
Nachvollziehbarkeit, wer erstellt, gelesen und bestätigt hat; Koordinationsregel 5
verbietet parallele Systeme.
*Fix:* Abschluss über `update_operational_task_as_actor` fahren und
`accountable_/controller_employee_id` auf die Bereichs-/Schichtleitung setzen
statt auf den Ausführenden.

**MAJOR-4 — `departmentId`/`locationId` werden ohne Tenant-/Standortprüfung geschrieben**
`app/api/ablaeufe/guides/[id]/route.ts:11-12,25-29`
Beide Werte werden lediglich als UUID validiert. Der Standort-Check greift nur
für `manager` (`actor.rolle === 'manager' && actor.location_id !== locationId`);
`backoffice` und `admin` können jede beliebige UUID setzen, auch die eines
fremden Tenants. Für `department_id` findet überhaupt keine Prüfung statt, für
keine Rolle. Der Schreibvorgang läuft mit Service-Role, RLS greift also nicht.
Ergebnis: Zeile mit eigenem `tenant_id`, aber fremdem `location_id`/`department_id`.
Der Rest des Repos macht das anders — vgl. `departmentInScope(...)` in
`app/api/operations/responsibility/route.ts:313`.
*Fix:* beide IDs vor dem Schreiben gegen `locations`/`departments` des eigenen
Tenants (und für Manager gegen den eigenen Standort) auflösen, sonst 403.

**MAJOR-5 — Die geführte Checkliste hat für Mitarbeitende keinen erreichbaren Einstieg**
`app/(neo)/neo/app/shell.tsx:31` (`managerOnly: true`), `app/(neo)/neo/app/ablaeufe/page.tsx:7` (`requireManagerPlus()`)
`app/(admin)/shift-guides/page.tsx:11` wurde auf `requirePosAccess()` gelockert und
setzt für Nicht-Manager einen „Starten"-Link — aber die Navigationsgruppe
„TEAM & ABLÄUFE" ist `managerOnly`, und die Hub-Seite `/neo/app/ablaeufe` verlangt
weiterhin `requireManagerPlus()`. Ein `mitarbeiter` erreicht die Liste also nur
durch manuelles Eintippen der URL; im Mitarbeiter-Bereich `app/mitarbeiter/`
wurde kein Einstieg ergänzt. Im `(admin)`-Routengruppen-Layout
(`app/(admin)/layout.tsx:18`) ist die Lockerung sogar vollständig wirkungslos,
weil das Layout außerhalb von `/pos` `requireManagerPlus()` erzwingt.
Damit ist der Klickpfad im Report („Mobile — Mitarbeiter: Als Mitarbeiter
desselben Standorts `Neo → Abläufe → Öffnung & Schließung → Starten`") für die
angegebene Rolle nicht durchführbar — die Prüfanleitung ist insoweit unzutreffend.
*Fix:* gemäß Koordinationsregel 2 genau eine Nav-Zeile für Mitarbeitende ergänzen
oder eine Kachel in `app/mitarbeiter/page.tsx` setzen und den Guard der Hub-Seite
entsprechend angleichen.

---

## MINOR

**MINOR-1 — Messwertfeld lässt sich nicht mehr leeren**
`app/(neo)/neo/app/ablaeufe/schichtleitfaeden/[id]/ausfuehren/guided-procedure.tsx:20`
`setValues({...values, ...(value===undefined?{}:{[step.id]:value})})` entfernt den
alten Wert beim Leeren des Feldes nicht, während `value={values[step.id]??''}`
ihn sofort wieder anzeigt. Wer sich vertippt, bekommt die alte Zahl zurück.
*Fix:* beim leeren Feld den Schlüssel löschen (`const next={...values}; delete next[step.id]`).

**MINOR-2 — Englische Zod-Standardmeldungen erscheinen in der deutschen Owner-UI**
`app/api/ablaeufe/guides/[id]/route.ts:9,21`
`parsed.error.issues[0]?.message` gibt bei leerem Ablaufnamen die
Default-Meldung von `z.string().min(2)` aus („String must contain at least 2
character(s)"). Der Editor validiert vorab nur den Inhalt, nicht den Titel.
*Fix:* deutsche `message` an allen Feldern des Request-Schemas setzen.

**MINOR-3 — Nach „Ablauf kopieren" wird hart nach `/neo/...` umgeleitet**
`app/(admin)/shift-guides/[id]/editor.tsx:25`
Dieselbe Komponente läuft auch unter `/shift-guides/[id]`; die Seite ermittelt
ihren Pfad korrekt über `operationsBasePath`, der Redirect ignoriert das.
*Fix:* `basePath` als Prop durchreichen und für den Push verwenden.

**MINOR-4 — Technische Rohwerte in der Ablauf-Liste**
`app/(admin)/shift-guides/page.tsx:16,43,48`
Die Spalte „Phase" zeigt weiterhin die englischen Rohwerte `opening`/`closing`/
`middle` als Badge, die Version erscheint als `v3` in `font-mono`. Das neu
mitgeladene `ablauf_typ` wird nirgends gerendert — die deutschsprachigen
Ablaufarten aus dem Editor tauchen in der Liste also gar nicht auf.
*Fix:* `ablauf_typ` über die vorhandene `TYPES`-Tabelle deutsch anzeigen und die
Phase-Spalte entfernen.

**MINOR-5 — Cross-Location-Ausführung endet in einer irreführenden Fehlermeldung**
`app/(neo)/neo/app/ablaeufe/schichtleitfaeden/[id]/ausfuehren/page.tsx:11` vs. `app/api/ablaeufe/executions/route.ts:13,19`
Die Seite lässt `backoffice`/`admin` Abläufe fremder Standorte öffnen, `start`
lehnt sie danach mit 404 ab; bei fehlendem `location_id` meldet die Route
„Nicht angemeldet." (401), obwohl der Nutzer angemeldet ist.
*Fix:* Seite und Route auf dieselbe Scope-Regel bringen und für „kein Standort
hinterlegt" eine eigene, verständliche Meldung ausgeben.

**MINOR-6 — Zeilenformat weicht stark vom Umfeld ab**
`guided-procedure.tsx` (23 Zeilen / 6261 Zeichen, bis ~270 Zeichen pro Zeile),
`editor.tsx`, `executions/route.ts`
Mehrere Anweisungen pro Zeile und JSX-Blöcke in einer Zeile machen künftige Diffs
und Zeilenverweise praktisch unbrauchbar; die ersetzten Dateien waren normal umbrochen.
*Fix:* vor dem Merge einmal durch den Repo-Formatter schicken.

---

## NOTE

**NOTE-1** — `shift_guides` hat in `supabase/migrations/` keine Basisdefinition
(die Tabelle stammt aus dem externen Baseline-Schema). Meine Ausführung lief
gegen ein selbst gebautes Stub-Schema; `phase`, `titel`, `aktiv`, `version`,
`position_typ` habe ich als vorhanden angenommen, weil der bestehende Code sie
liest. Vor dem Merge trotzdem gegen einen echten Supabase-Branch anwenden.

**NOTE-2** — Idempotenz der Migration ist bestätigt: zweiter Durchlauf meldet
`UPDATE 0` für die Normalisierung, `schemaVersion` bleibt `1`, Kategorienzahl
unverändert. Legacy-Felder (`name`) bleiben erhalten, `title` wird ergänzt.

**NOTE-3** — Koordinationsregeln eingehalten: Migrationspräfix `20260831150000`
liegt im reservierten C-F-Bereich; `app/(neo)/neo/app/shell.tsx` wurde nicht
angefasst; neuer Code liegt in `lib/ablaeufe/`, `app/api/ablaeufe/`,
`tests/ablaeufe/`. `docs/agents/HANDOFF.md` ist eine von allen Paketen geteilte
Datei — der angehängte Block (ohne Leerzeile vor der neuen Überschrift) wird beim
parallelen Merge Konflikte erzeugen.

**NOTE-4** — Der Playwright-Spec ist ehrlich mit `test.skip(true, …)` deaktiviert
(`tests/e2e/ablaeufe-visual-editor.spec.ts:4`), und der Report behauptet an keiner
Stelle einen grünen Browserlauf. Die EROFS-Begründungen für Vitest ohne
`--configLoader native` und für den Playwright-Webserver sind plausibel und
korrekt dokumentiert. Der Spec deckt zudem nicht ab, was das Packet fordert
(drei Schritte, Pflicht+Foto, Temperatur mit Bereich, Abschluss).

**NOTE-5** — Positiv: Der Foto-Nachweis nutzt korrekt den bestehenden,
scope-geprüften Endpunkt `app/api/operations/responsibility/evidence/route.ts`
statt eines eigenen Uploads; die Zod-Validierung des Schrittinhalts läuft
serverseitig; die Ausführungsseite scopet den Service-Role-Read manuell auf
Tenant und Standort; die 5 neuen Unit-Tests treffen genau die Kernlogik
(Normalisierung, Sortierung, Pflicht-Gating, Bereichsprüfung).

---

# Re-review round 1

- Kopf jetzt `a3a80bb` (`fix(ablaeufe): resolve review findings`), Basis unverändert `e401d12`
- Diff `git diff e401d12..HEAD` — 19 Dateien, +1171/−143
- Erneut read-only: keine Quelldatei wurde verändert.

## VERDICT

**FAIL** — Migration-Dry-Run gegen den Produktionsschema-Snapshot schlägt fehl;
zusätzlich 3 neue BLOCKER, 3 neue MAJOR, 4 neue MINOR.

## Gates (selbst ausgeführt)

```
$ ./node_modules/.bin/vitest run --configLoader native
 Test Files  35 passed (35)
      Tests  227 passed (227)
   Duration  8.47s
 EXIT=0

$ ./node_modules/.bin/tsc --noEmit -p .
 (keine Ausgabe)
 EXIT=0

$ bash .../migration-dryrun.sh supabase/migrations/20260831150000_visual_shift_guide_editor.sql
 restore: 0 errors (extension/duplicate noise expected), tables=243
 FAIL 20260831150000_visual_shift_guide_editor.sql
 ERROR:  function min(uuid) does not exist
 EXIT=1
```

Die einzige auf diesem Branch neue Migrationsdatei ist
`20260831150000_visual_shift_guide_editor.sql`. **Der Dry-Run ist FAIL → BLOCKER.**

Zur Abgrenzung habe ich eine Diagnosekopie unter `/tmp/patched-mig.sql` mit
`min(id)` → `(array_agg(id))[1]` erzeugt (Quelldatei unverändert). Diese Kopie
läuft sauber durch (`OK patched-mig.sql`); der Rest der Migration ist also
korrekt. Alle folgenden Datenprüfungen liefen gegen diese Kopie.

## Status der Befunde aus Runde 0

Der Implementierer hat keinen Befund als REFUTED markiert — alle 14 wurden als
FIXED gemeldet. Es gibt daher keine REFUTATION-ACCEPTED/REJECTED-Einträge.

- **BLOCKER-1 — VERIFIED-FIXED.** Real nachgestellt: Leitfaden mit
  `"steps":["Kasse zaehlen","Toilette pruefen"]` normalisiert jetzt fehlerfrei
  (`UPDATE 1`), beide Schritte erhalten `step-1-1`/`step-1-2` und ihren Text;
  eine zweite Kategorie mit Objekt-Schritt und Fremdfeld `extra` bleibt inklusive
  `extra` erhalten. `migration:77-79` (`jsonb_typeof(...)='object'`) und
  `:71` (`s.value #>> '{}'`) greifen wie beschrieben.
- **BLOCKER-2 — VERIFIED-FIXED.** `editor.tsx:119-128` rendert alle Kategorien,
  `updateCategory` (`:60-63`) ersetzt per `map` nur die betroffene Kategorie,
  `onDelete`/`onMove` arbeiten auf der vollen Liste. Kategorie-Namen sind
  editierbar (`:154`), Löschen der letzten Kategorie ist gesperrt (`:157`).
  Der Drei-Kategorien-Fixture überlebt einen Speichervorgang vollständig.
- **BLOCKER-3 — VERIFIED-FIXED (mit Regression, siehe NEW-BLOCKER-3).**
  `app/(admin)/checkups/[id]/editor.tsx` enthält kein `JSON.stringify`, kein
  `<pre>` und kein „JSON"-Label mehr; es wird dieselbe exportierte
  `CategoryEditor`-Komponente verwendet, Speichern läuft über
  `app/api/ablaeufe/checkups/[id]/route.ts` mit `procedureContentSchema`.
  `grep -n "JSON.stringify" app/(admin)/checkups app/(admin)/shift-guides` ist leer.
- **MAJOR-1 — VERIFIED-FIXED (Logik), aber durch NEW-BLOCKER-1 unerreichbar.**
  Gegen die Diagnosekopie verifiziert: bei genau einem Tenant und einem Standort
  wird ein bereichsloser Leitfaden korrekt auf `tenant_id`/`location_id`
  zurückgefüllt (`UPDATE 1`, `UPDATE 1`); nach Anlegen eines zweiten Standorts
  und einer weiteren scope-losen Zeile bricht die Migration wie gefordert mit
  `ERROR: shift_guides contains legacy rows whose tenant/location scope is
  ambiguous` ab. Kein stilles Verstecken mehr.
- **MAJOR-2 — VERIFIED-FIXED (Restlücke siehe NEW-MINOR-1).**
  `executions/route.ts:91-102` lädt `operational_task_evidence` für die Task-ID,
  Tenant und `evidence_type='foto'` und verlangt je Pflichtfoto-Schritt eine
  reale Zeilen-ID; `:104-115` schreibt `unterschrift`/`messwert` serverseitig.
  Ein `evidence: {"step-1-2": true}` für einen Foto-Pflichtschritt wird jetzt mit
  409 „Foto für … fehlt." abgelehnt.
- **MAJOR-3 — VERIFIED-FIXED.** `executions/route.ts:123-132` fährt den Abschluss
  über `update_operational_task_as_actor` mit `wartet_auf_pruefung`; damit greifen
  die DB-seitige Evidence-Prüfung (`20260830183000:52-58`), `set_config
  ('app.audit_employee_id',…)` und die spätere Freigabekaskade. Ausführender und
  Prüfer sind getrennt: `accountable_/controller_employee_id = reviewerId` mit
  `.neq('employee_id', actor.id)` bzw. `.neq('id', actor.id)` (`:45`, `:51`).
- **MAJOR-4 — VERIFIED-FIXED.** `guides/[id]/route.ts:27-33` löst `locationId`
  gegen `locations` des Actor-Tenants auf und prüft `departmentId` gegen Tenant
  **und** den gewählten Standort; Manager bleiben über `:26` auf ihren Standort
  beschränkt. Auch der Fallback `existing.location_id` läuft durch dieselbe Prüfung.
- **MAJOR-5 — VERIFIED-FIXED.** `app/mitarbeiter/page.tsx:324-331` setzt genau
  eine sichtbare Kachel „Öffnung, Schließung & Abläufe" auf
  `/neo/app/ablaeufe/schichtleitfaeden`. Das Neo-Layout
  (`app/(neo)/neo/app/layout.tsx:17-18`) verlangt nur einen Tenant, die Zielseite
  re-exportiert die auf `requirePosAccess()` gelockerte Liste. Der Klickpfad ist
  für `mitarbeiter` damit tatsächlich durchführbar; die `managerOnly`-Nav-Gruppe
  wurde korrekt nicht angefasst (Koordinationsregel 2 eingehalten).
- **MINOR-1 — VERIFIED-FIXED.** `guided-procedure.tsx:79-83` löscht den Schlüssel
  bei leerem Feld und setzt den Nachweis auf `null`.
- **MINOR-2 — VERIFIED-FIXED.** Alle Felder in `guides/[id]/route.ts:9-13` und
  `checkups/[id]/route.ts:8-16` tragen deutsche Meldungen; der Client prüft den
  Titel vorab (`editor.tsx:71-74`).
- **MINOR-3 — VERIFIED-FIXED.** `basePath` wird als Prop durchgereicht
  (`page.tsx:22`) und für den Redirect verwendet (`editor.tsx:98`).
- **MINOR-4 — VERIFIED-FIXED.** `shift-guides/page.tsx:47` zeigt die deutsche
  Ablaufart über `TYPE_LABELS`; Phase-Spalte und `v<version>` sind entfernt.
- **MINOR-5 — VERIFIED-FIXED.** Seite (`ausfuehren/page.tsx:10-12`) und Route
  (`executions/route.ts:21,31`) verlangen für alle Rollen denselben
  Actor-Standort; die Route liefert für „kein Standort hinterlegt" eine eigene
  409-Meldung. (Rest: die Seite antwortet in diesem Fall weiterhin mit
  `notFound()` statt einer erklärenden Meldung — siehe NEW-NOTE-2.)
- **MINOR-6 — NOT-FIXED (teilweise).** Imports und Kontrollfluss sind umbrochen,
  die JSX-Blöcke nicht. Gemessen:
  `app/(admin)/shift-guides/[id]/editor.tsx` 21 Zeilen > 160 Zeichen, längste
  Zeile **802** Zeichen (`:162`) — länger als alles im Ausgangszustand;
  `guided-procedure.tsx` 9 Zeilen > 160, längste **576** (`:65`);
  `checkups/[id]/editor.tsx` 8 Zeilen > 160, längste **375** (`:79`).
  Nur `executions/route.ts` (max. 202) ist im Rahmen. Zeilenverweise auf den
  Editor bleiben damit unbrauchbar.

## Neue Befunde

### BLOCKER

**NEW-BLOCKER-1 — Migration ist gegen das Produktionsschema nicht anwendbar**
`supabase/migrations/20260831150000_visual_shift_guide_editor.sql:19,24`
Der Dry-Run gegen den Prod-Schema-Snapshot (`supabase/postgres:15.8.1.085`)
bricht sofort ab: `ERROR: function min(uuid) does not exist`. Die
`min()`/`max()`-Aggregate für `uuid` existieren in dieser PostgreSQL-Version
nicht. Da alles in einer Transaktion liegt, wird **nichts** angewendet — weder
Spalten noch Index, RLS, Policies oder Normalisierung. Der MAJOR-1-Fix hat
diesen Fehler eingeführt (die Runde-0-Fassung enthielt keine Aggregate).
*Fix:* `(array_agg(id))[1]` oder `(select id from public.tenants order by id limit 1)`
mit separatem `count(*)=1`-Guard. Mit dieser Ersetzung läuft die Migration
sauber durch (`OK patched-mig.sql`) — der Rest ist in Ordnung.

**NEW-BLOCKER-2 — Jeder Ablauf außer „Öffnung"/„Schließung" lässt sich nicht speichern**
`app/api/ablaeufe/guides/[id]/route.ts:35`, `app/api/ablaeufe/checkups/[id]/route.ts:9,38`
`phase` ist im Produktionsschema der Enum `shift_guide_phase` mit den Labels
`opening`, `midday`, `closing` — **kein** `middle`. Die Route schreibt aber für
alle sechs übrigen Ablaufarten (`cleaning`, `control`, `production`, `handover`,
`hygiene_temperature`, `other`) hart `'middle'`. Real nachgestellt gegen den
Snapshot:
```
update public.shift_guides set phase='middle', ablauf_typ='cleaning' where id=…;
ERROR:  invalid input value for enum shift_guide_phase: "middle"
```
Der Owner bekommt „Ablauf konnte nicht gespeichert werden." (500) und verliert
seine gesamte Editor-Eingabe. Dieselbe Route-Konstante trifft
`checkup_templates.phase` (identischer Enum): die Auswahl „Tagsüber"
(`checkups/[id]/editor.tsx:79`, Wert `middle`) macht jedes Speichern einer
Kontrollliste unmöglich. Damit ist das zentrale Akzeptanzkriterium („Ablauf
visuell aufbauen und speichern") für 6 von 8 Ablaufarten nicht erfüllt.
*Fix:* `midday` statt `middle` schreiben (und den Zod-Enum der Checkup-Route
entsprechend ändern), oder `phase` beim neuen `ablauf_typ`-Modell gar nicht mehr
setzen.

**NEW-BLOCKER-3 — Gespeicherte Kontrolllisten werden für Sessions und HACCP-PDF leer**
`app/(admin)/checkups/[id]/editor.tsx:13-33` + `app/api/ablaeufe/checkups/[id]/route.ts:45`
Der BLOCKER-3-Fix ändert das Speicherformat von `fragen = {tasks:[…]}` auf
`fragen = {schemaVersion:1, categories:[{steps:[…]}]}`. Kein einziger Leser wurde
angepasst und keine Migration konvertiert Bestandsdaten:
- `app/(admin)/checkups/sessions/[id]/page.tsx:28-29` → `tasks = []`: die
  Session-Auswertung zeigt „0 Aufgaben", die Liste „nicht erledigt" ist leer und
  Fotobeschriftungen fallen auf die rohe `task_id` zurück (`:41`).
- `app/api/pdf/haccp/route.ts:80` → `tasksTotal = 0` im HACCP-Monatsnachweis.
- `app/(admin)/checkups/page.tsx:81` → Spalte „Aufgaben" zeigt 0.
Sobald der Owner eine bestehende Hygiene-/Temperaturkontrolle einmal im neuen
Editor speichert, verliert der behördlich relevante Nachweis seine Sollstruktur —
still, ohne Fehlermeldung. Das trifft genau Owner-Auftrag Punkt 7 und ist eine
durch den Fix eingeführte Regression.
*Fix:* entweder alle drei Leser (plus `checkup_completions.task_id`-Zuordnung)
auf `categories[].steps[]` heben und die Bestandsdaten per Migration
konvertieren, oder beim Speichern zusätzlich eine abgeleitete `tasks[]`-Sicht
mitschreiben.

### MAJOR

**NEW-MAJOR-1 — Legacy-Feldwerte lassen die Migration weiterhin komplett scheitern**
`supabase/migrations/20260831150000_visual_shift_guide_editor.sql:73`
`coalesce(nullif(s.value->>'required','')::boolean, nullif(s.value->>'pflicht','')::boolean, true)`
castet ungeprüft. Real nachgestellt: ein einziger Schritt
`{"title":"A","required":"ja"}` bricht die Normalisierung und damit die gesamte
Transaktion ab:
```
ERROR:  invalid input syntax for type boolean: "ja"
```
Das ist derselbe Fehlermodus wie BLOCKER-1, nur für Werte statt Typen; der Fix
hat ihn nur für skalare Schritte geschlossen. `"1"`/`"true"` casten, `"ja"`,
`"nein"`, `"yes"` nicht — bei einem deutschsprachigen Legacy-Feld `pflicht` ist
das nicht exotisch. Da der Snapshot schema-only ist, kann ich nicht
verifizieren, ob solche Zeilen in Produktion existieren; die Migration hat aber
keine Absicherung.
*Fix:* `case when s.value->>'required' in ('true','t','1','ja','yes') then true
when s.value->>'required' in ('false','f','0','nein','no') then false else … end`
statt eines direkten Casts.

**NEW-MAJOR-2 — Manager speichern Kontrolllisten ins Leere, bekommen aber „gespeichert"**
`app/api/ablaeufe/checkups/[id]/route.ts:22,26,36-47`
Die Route lässt `manager` ausdrücklich zu (`:22`), schreibt aber mit dem
Benutzer-Client (`createClient()`), und die einzige Write-Policy im
Produktionsschema ist
`checkup_templates_write_bo … ALL using is_backoffice() with check is_backoffice()`
mit `is_backoffice() = has_role(['backoffice','admin'])`. Ein Manager-Update
trifft damit 0 Zeilen, PostgREST meldet **keinen** Fehler, die Route antwortet
200 und die UI zeigt „Kontrolliste wurde gespeichert." Der Owner glaubt, seine
Kontrollschritte seien gesichert; sie sind es nicht.
*Fix:* entweder die Policy auf Standort-Manager ausweiten oder in der Route die
betroffene Zeilenzahl prüfen (`.select()` + leeres Ergebnis → 403) und `manager`
aus der Rollenliste entfernen, solange die Policy es nicht erlaubt.

**NEW-MAJOR-3 — Kontrolllisten sind über die neue Route mandantenübergreifend überschreibbar**
`app/api/ablaeufe/checkups/[id]/route.ts:27-34`
`checkup_templates` hat im Produktionsschema **kein** `tenant_id`, die
Read-Policy ist `checkup_templates_read_all … using (uid() is not null)`, und die
Route lädt `existing` allein über die ID ohne jede Scope-Bedingung. Wird
`departmentId` als `null` gesendet, findet überhaupt keine Prüfung statt.
Ein `backoffice`/`admin` aus Tenant A kann damit die Kontrollliste von Tenant B
per POST auf deren ID vollständig überschreiben (Titel, Bereich, Intervall,
Eskalation, Inhalt). Der Rest des Repos scopet solche Writes explizit
(`app/api/ablaeufe/guides/[id]/route.ts:23,27`). Die Lücke existierte in
Ansätzen schon im alten Client-Editor, wird durch die neue serverseitige Route
aber als „rollen- und RLS-gebunden" ausgewiesen, ohne es zu sein.
*Fix:* `checkup_templates` über `location_id` (bzw. eine ergänzte `tenant_id`)
gegen den Actor-Tenant auflösen, bevor geschrieben wird.

### MINOR

**NEW-MINOR-1 — Ein einziges Foto erfüllt beliebig viele Foto-Pflichtschritte**
`app/api/ablaeufe/executions/route.ts:92-101`
Geprüft wird nur Mengenzugehörigkeit (`uploadedPhotoIds.has(evidenceId)`), nicht
Eindeutigkeit je Schritt. Ein Client kann dieselbe Evidence-ID für alle
Foto-Pflichtschritte senden; die Nachweiszeile trägt keine `stepId`
(`evidence/route.ts:67-70`), die Zuordnung lebt nur in `procedure_results`.
*Fix:* je Pflichtschritt eine eigene, noch nicht verwendete Evidence-ID
verlangen und die `stepId` beim Upload mitschreiben.

**NEW-MINOR-2 — Der erste Editor-Speichervorgang verwirft genau die Legacy-Felder, die die Migration bewahrt**
`lib/ablaeufe/schema.ts:55-65`, `app/(admin)/checkups/[id]/editor.tsx:22-30`
Die SQL-Normalisierung erhält unbekannte Schrittfelder ausdrücklich
(`migration:77-79`, verifiziert: `extra: "keep"` überlebt).
`normalizeProcedureContent` baut jeden Schritt dagegen aus einer festen
Feldliste neu, und `procedureContentSchema` strippt beim Speichern den Rest —
beim ersten Öffnen und Speichern im Editor gehen sie verloren, u. a. das im
alten Checkup-Label dokumentierte `estMin`. Der Report behauptet
„Unbekannte Legacy-Felder bleiben erhalten"; das gilt nur für die Migration.

**NEW-MINOR-3 — Ohne zweite Person lässt sich kein Ablauf mehr starten**
`app/api/ablaeufe/executions/route.ts:48-56`
Der MAJOR-3-Fix bricht `start` mit 409 ab, wenn kein von `actor.id`
verschiedener Hauptverantwortlicher bzw. Standort-Manager existiert. An kleinen
Standorten bzw. für den Manager, der selbst öffnet, ist die geführte Checkliste
damit gar nicht mehr benutzbar.
*Fix:* auf Standort-/Tenant-Leitung außerhalb des Standorts ausweichen oder die
Aufgabe ohne Controller in `wartet_auf_pruefung` stellen.

**NEW-MINOR-4 — Roher JSON-Text kann als Schritttitel in der UI landen**
`supabase/migrations/20260831150000_visual_shift_guide_editor.sql:71`
Der neue Fallback `s.value #>> '{}'` steht vor `'Schritt '||s.ordinality` und
liefert für ein Objekt ohne `title`/`text`/`name` dessen JSON-Serialisierung.
Verifiziert: `{"aufgabe":"Kaffeemaschine spuelen"}` wird zu
`title = '{"aufgabe": "Kaffeemaschine spuelen"}'` und erscheint so im Editor und
in der Mitarbeiter-Checkliste — genau das, was Owner-Auftrag Punkt 7 verbietet.
*Fix:* den Fallback auf `jsonb_typeof(s.value)='string'` beschränken.

### NOTE

**NEW-NOTE-1** — Der MAJOR-1-Fix ist bewusst laut: bei mehreren Standorten und
mindestens einem Leitfaden ohne Bereich bricht die Migration ab und der Deploy
schlägt fehl. Das ist richtig herum, muss aber als Deploy-Voraussetzung
eingeplant werden (Scope vorher setzen). Gegen den schema-only Snapshot ist
nicht prüfbar, ob Pontstraße solche Zeilen hat.

**NEW-NOTE-2** — `ausfuehren/page.tsx:10` beantwortet „kein Standort hinterlegt"
weiterhin mit `notFound()`; die verständliche Meldung existiert nur in der API.
Für den neu ergänzten Mitarbeiter-Einstieg endet dieser Fall in einer 404-Seite.

**NEW-NOTE-3** — Ich habe `next build` erneut nicht ausgeführt; die Angabe
„228/228" im Report bleibt unverifiziert. Der Konflikthinweis aus NOTE-3 der
Runde 0 zu `docs/agents/HANDOFF.md` (gemeinsam genutzte Datei) besteht
unverändert.

## Zählung Re-review 1

- Runde 0: 13 von 14 Befunden VERIFIED-FIXED, 1 NOT-FIXED (MINOR-6, teilweise).
- Neu: **3 BLOCKER, 3 MAJOR, 4 MINOR, 3 NOTE.**
- Gates: vitest EXIT=0 (227/227), tsc EXIT=0, Migration-Dry-Run **EXIT=1 (FAIL)**.

**VERDICT: FAIL**

---

# Re-review round 2

- Kopf jetzt `8e648e0` (`fix(ablaeufe): resolve round 2 review findings`), Basis unverändert `e401d12`
- Diff `git diff e401d12..HEAD` — 21 Dateien, +2900/−200
- Erneut read-only: keine Quelldatei wurde verändert. Alle DB-Prüfungen liefen in
  Wegwerf-Datenbanken aus dem Produktionsschema-Snapshot; sie wurden anschließend
  wieder gelöscht.

## Gates und Dry-Run (selbst ausgeführt)

```
$ ./node_modules/.bin/vitest run --configLoader native
 Test Files  35 passed (35)
      Tests  228 passed (228)
   Duration  6.03s
 EXIT=0

$ ./node_modules/.bin/tsc --noEmit -p .
 (keine Ausgabe)
 EXIT=0

$ bash .../migration-dryrun.sh supabase/migrations/20260831150000_visual_shift_guide_editor.sql
 restore: 0 errors (extension/duplicate noise expected), tables=243
 OK   20260831150000_visual_shift_guide_editor.sql
 EXIT=0
```

Die einzige auf diesem Branch neue Migrationsdatei ist weiterhin
`20260831150000_visual_shift_guide_editor.sql`
(`20260831100944_application_assessments_training_onboarding.sql` liegt bereits
auf der Basis). **Der Dry-Run ist jetzt PASS** — der Dry-Run-BLOCKER aus Runde 1
ist damit erledigt. `next build` habe ich auftragsgemäß erneut nicht ausgeführt.

## Status der Befunde aus Re-review 1

Der Implementierer hat auch in Runde 2 keinen Befund als REFUTED markiert — alle
elf offenen Punkte wurden als FIXED gemeldet. Es gibt daher wieder keine
REFUTATION-ACCEPTED/REJECTED-Einträge.

- **NEW-BLOCKER-1 — VERIFIED-FIXED.** `migration:19,24,62,67` verwenden
  `(array_agg(id))[1]` statt `min(uuid)`. Der verpflichtende Dry-Run gegen den
  Produktionsschema-Snapshot läuft mit `OK` und Exit 0 durch (siehe oben).
- **NEW-BLOCKER-2 — VERIFIED-FIXED.** Gegen den Snapshot verifiziert: der Enum
  `public.shift_guide_phase` hat exakt die Werte `opening`, `midday`, `closing`;
  `checkup_templates.phase` und `shift_guides.phase` nutzen beide diesen Typ.
  `guides/[id]/route.ts:113-118` schreibt jetzt `midday`,
  `checkups/[id]/route.ts:13` akzeptiert `midday`. Ein realer
  `update … set phase='midday', ablauf_typ='cleaning'` läuft durch (`UPDATE 1`).
  `grep -rn "'middle'"` findet in `app/`/`lib/` nur noch die eine
  Kompatibilitätszeile `checkups/[id]/editor.tsx:122`.
- **NEW-BLOCKER-3 — VERIFIED-FIXED.** `checkups/[id]/route.ts:79-82` schreibt
  neben `schemaVersion`/`categories` die abgeleitete `tasks`-Sicht
  (`procedureTasks`, `lib/ablaeufe/schema.ts:151-161`). Die drei beanstandeten
  Leser bleiben damit funktionsfähig: `checkups/sessions/[id]/page.tsx:28`,
  `api/pdf/haccp/route.ts:80`, `checkups/page.tsx:81` lesen `fragen.tasks` mit
  `id`/`title`. Entscheidend für die Fotozuordnung: die Legacy-Konvertierung
  `checkups/[id]/editor.tsx:38` behält `item.id` bei, `normalizeProcedureContent`
  (`schema.ts:117-120`) ebenso — vorhandene `checkup_completions.task_id`
  bleiben also zuordenbar. `requiresPhoto` wird korrekt aus `evidence==='photo'`
  zurückgerechnet.
- **NEW-MAJOR-1 — VERIFIED-FIXED.** Real gegen den Snapshot nachgestellt: ein
  Leitfaden mit `{"title":"Kuehlung","required":"ja"}` und einem zweiten Schritt
  `{"text":"Boden","pflicht":"nein","evidence_type":"foto"}` normalisiert
  fehlerfrei zu `required=true` bzw. `required=false, evidence="photo"`
  (`migration:109-113`). Die Transaktion bricht nicht mehr ab.
- **NEW-MAJOR-2 — VERIFIED-FIXED.** `checkups/[id]/route.ts:43,83-104` nutzt den
  Service-Client und verlangt über `.select('id').maybeSingle()` eine real
  aktualisierte Zeile; ohne Treffer gibt es 500 statt eines stillen „gespeichert".
  Der Schreibpfad umgeht die backoffice-only RLS-Policy bewusst und ersetzt sie
  durch Rollen-, Tenant- und Standortprüfung in der Route — dasselbe Muster wie
  `guides/[id]/route.ts`.
- **NEW-MAJOR-3 — VERIFIED-FIXED.** `migration:51-79` ergänzt
  `checkup_templates.tenant_id` (plus Scope-Index); `checkups/[id]/route.ts:48`
  lädt und `:97` aktualisiert ausschließlich mit `.eq('tenant_id', actor.tenant_id)`,
  Manager zusätzlich gegen `existing.location_id` (`:55-63`), `departmentId`
  gegen Tenant **und** Template-Standort (`:64-77`). Ein Tenant-A-Backoffice
  erhält für eine Tenant-B-ID jetzt 404. Hinweis: `location_id` existierte in
  `checkup_templates` bereits produktiv, `add column if not exists` ist insoweit
  ein No-op — verifiziert am Snapshot.
- **NEW-MINOR-1 — VERIFIED-FIXED.** `evidence/route.ts:22,44-49,155-158` nimmt
  `stepId` entgegen, validiert ihn und legt ihn in `content.stepId` ab; der
  Client sendet ihn (`guided-procedure.tsx:56`).
  `executions/route.ts:197-217` verlangt je Pflichtfoto-Schritt eine reale
  Evidence-Zeile mit exakt passender `content.stepId` **und** über
  `uniquePhotoIds.size !== requiredPhotoSteps.length` die Eindeutigkeit. Die
  Mehrfachverwendung einer ID wird abgelehnt.
- **NEW-MINOR-2 — VERIFIED-FIXED.** `schema.ts:27,49,56` sind `passthrough`,
  `:116` spreadet `...value` vor den kanonischen Overrides. Der neue Unit-Test
  (`tests/ablaeufe/procedure-schema.test.ts:33-47`) belegt den Erhalt von
  `estMin` und `extra` inklusive Checkup-Projektion; die SQL-Seite habe ich real
  gegengeprüft (`extra: "keep"`, `estMin: 4` überleben die Migration).
- **NEW-MINOR-3 — VERIFIED-FIXED.** `executions/route.ts:95-106` fällt auf eine
  unabhängige Tenant-Leitung zurück; ohne jeden Kandidaten startet die Aufgabe
  mit `accountable_employee_id = actor.id` und `controller_employee_id = null`
  (`:137-138`) statt mit 409 abzubrechen. Zur Nebenwirkung siehe NEU-MINOR-2.
- **NEW-MINOR-4 — VERIFIED-FIXED.** `migration:105` beschränkt den skalaren
  Fallback auf `jsonb_typeof(s.value)='string'`. Real nachgestellt: aus
  `{"aufgabe":"Maschine spuelen"}` wird `title = "Schritt 3"` — kein
  serialisierter JSON-Text mehr; der Skalar `"Kasse zaehlen"` behält seinen Text.
- **MINOR-6 (Runde 0/1, zuletzt NOT-FIXED) — VERIFIED-FIXED.** Gemessen:
  `shift-guides/[id]/editor.tsx` 573 Zeilen, 0 Zeilen > 160, längste 122;
  `guided-procedure.tsx` 271/0/143; `checkups/[id]/editor.tsx` 287/0/112;
  `executions/route.ts` 303/0/91.

## Neue Befunde

### MAJOR

**NEU-MAJOR-1 — Migration bricht bei Kontrolllisten ab, deren Tenant eindeutig ableitbar ist**
`supabase/migrations/20260831150000_visual_shift_guide_editor.sql:55-76`
Der `tenant_id`-Backfill für `checkup_templates` kennt nur zwei Quellen:
`departments` über `department_id` und den Einzeltenant-Fallback. Die im
Produktionsschema **bereits vorhandene** Spalte `checkup_templates.location_id`
(FK auf `locations`, verifiziert am Snapshot) wird nicht als Quelle genutzt,
obwohl `locations.tenant_id` existiert und den Tenant eindeutig bestimmt.
Real nachgestellt gegen den Snapshot: zwei Tenants, zwei Standorte, ein
Leitfaden mit Bereich (backfillt sauber) und eine Kontrollliste mit gesetztem
`location_id`, aber ohne `department_id`:
```
ERROR:  checkup_templates contains legacy rows whose tenant/location scope is ambiguous
MIG_EXIT=3
```
Der Scope ist hier gerade **nicht** mehrdeutig — er steht in der Zeile. Da alles
in einer Transaktion liegt, schlägt der komplette Deploy fehl (Spalten, Index,
RLS, Policies, Normalisierung für `shift_guides` inklusive). Der schema-only
Dry-Run kann das nicht zeigen, weil er keine Daten enthält; genau deshalb ist der
grüne Dry-Run hier kein Freibrief. „Standortweite" Kontrolllisten ohne Bereich
sind der Normalfall — der neue Editor bietet „Standortweit" ausdrücklich an
(`checkups/[id]/editor.tsx:149`).
*Fix:* vor dem Guard ergänzen:
`update public.checkup_templates t set tenant_id=l.tenant_id from public.locations l where l.id=t.location_id and t.tenant_id is null;`

### MINOR

**NEU-MINOR-1 — Bereichslose Legacy-Leitfäden behalten die Ablaufart „Sonstiges"**
`supabase/migrations/20260831150000_visual_shift_guide_editor.sql:10-14`
Die Zuordnung `phase → ablauf_typ` sitzt im selben `UPDATE … from public.departments d
where d.id=g.department_id`. Zeilen ohne `department_id` — genau die, die der
MAJOR-1-Fix aus Runde 0 überhaupt erst sichtbar hält — durchlaufen dieses
Statement nie und behalten den Spaltendefault `other`. Real nachgestellt:
ein Leitfaden mit `phase='opening'` und `department_id is null` steht nach der
Migration auf `ablauf_typ='other'`. Folge: die Liste zeigt „Sonstiges"
(`shift-guides/page.tsx:47`) und der Editor stellt „Sonstiges" vor
(`shift-guides/[id]/editor.tsx:71-72`, `guide.ablauf_typ` ist wegen `not null
default 'other'` nie `null`, der `phase`-Fallback greift also nicht). Wer den
Ablauf einmal speichert, schreibt die falsche Art fest.
*Fix:* die `ablauf_typ`-Zuordnung in ein eigenes, join-freies `update` ziehen.

**NEU-MINOR-2 — Fallback ohne Prüfer hebt die Vier-Augen-Kontrolle wieder auf**
`app/api/ablaeufe/executions/route.ts:137-138`
Der Fix für NEW-MINOR-3 setzt bei fehlendem unabhängigen Prüfer
`accountable_employee_id = actor.id` und `controller_employee_id = null`.
`update_operational_task_as_actor` erlaubt den Abschluss, wenn
`v_actor.id in (v_task.accountable_employee_id, v_task.controller_employee_id)`
(`20260830183000_predeploy_operational_hardening.sql:41-46`) — der Ausführende
kann seine eigene Ausführung also freigeben und über die Kaskade auch die
eigenen Nachweise auf `akzeptiert` setzen. Das ist der Fehlermodus, gegen den
MAJOR-3 aus Runde 0 gerichtet war, nur auf den Sonderfall verengt. Weder UI noch
`procedure_results` markieren, dass diese Ausführung ohne unabhängige Kontrolle
lief.
*Fix:* Fallback-Ausführungen kennzeichnen (z. B. Flag in `procedure_results` oder
`review_note`), damit die Auditkette den fehlenden zweiten Blick ausweist.

**NEU-MINOR-3 — Tote `middle`-Kompatibilität mit englischer Fehlermeldung**
`app/(admin)/checkups/[id]/editor.tsx:122`
`value={form.phase === "middle" ? "midday" : form.phase}` verändert nur die
Anzeige des Selects, nicht `form.phase`. Bliebe der Wert je `middle`, ginge er
unverändert an die Route und scheiterte dort am Zod-Enum
(`checkups/[id]/route.ts:13`) mit der englischen Default-Meldung — also erneut
MINOR-2 aus Runde 0. Praktisch unerreichbar, weil `shift_guide_phase` den Wert
`middle` gar nicht kennt (am Snapshot verifiziert); die Zeile ist damit toter
Code, der eine englische Meldung offenhält.
*Fix:* entweder `form.phase` beim Initialisieren normalisieren oder die Zeile
entfernen.

### NOTE

**NEU-NOTE-1** — `app/(neo)/neo/app/ablaeufe/schichtleitfaeden/[id]/ausfuehren/page.tsx`
ist weiterhin im Einzeiler-Stil (mehrere Anweisungen pro Zeile). Die Datei
gehörte nicht zu den drei in MINOR-6 benannten, der Report behauptet aber, „die
betroffenen C-F-Routen und das Schema" seien formatiert worden; für diese Datei
trifft das nicht zu.

**NEU-NOTE-2** — Die Read-Policy `checkup_templates_read_all … using (uid() is
not null)` bleibt unverändert: jede angemeldete Person liest alle
Kontrolllisten aller Tenants (`checkups/page.tsx:18-20` filtert nicht). Das ist
Bestand, nicht durch C-F eingeführt; die neue `tenant_id`-Spalte macht die
Verschärfung aber jetzt trivial und sollte in einem Folgepacket erfolgen.

**NEU-NOTE-3** — `migration:110-111` verwendet
`lower(coalesce(s.value->>'required', s.value->>'pflicht',''))`. Ein leerer
String in `required` verdeckt ein gültiges `pflicht` (Runde 1 nutzte hier
`nullif`). Auswirkung ist auf `pflicht:"nein"` bei gleichzeitig leerem
`required` begrenzt und endet im sicheren Default `true`.

**NEU-NOTE-4** — `.gitignore` erhält `/node_modules` zusätzlich zum bereits
vorhandenen `node_modules/`; die Zeile ist wirkungsgleich und gehört nicht zum
Packet-Scope.

**NEU-NOTE-5** — `docs/agents/HANDOFF.md:395` hängt weiterhin ohne Leerzeile vor
der neuen Überschrift an (Konfliktrisiko beim parallelen Merge, vgl. NOTE-3 aus
Runde 0) und nennt „Vitest 227/227", während die Suite jetzt 228 Tests hat.

**NEU-NOTE-6** — `next build` wurde auch in dieser Runde nicht ausgeführt; die
Angabe „228/228 statische Seiten" bleibt von mir unverifiziert. Der
Playwright-Spec ist weiterhin ehrlich `skip`.

## Zählung Re-review 2

- Aus Re-review 1: **11 von 11 VERIFIED-FIXED**, 0 NOT-FIXED, 0 REFUTATIONEN
  (der Implementierer hat keinen Befund bestritten).
- Neu in dieser Runde: **0 BLOCKER, 1 MAJOR, 3 MINOR, 6 NOTE.**
- Gates: vitest EXIT=0 (228/228), tsc EXIT=0, Migration-Dry-Run **EXIT=0 (OK)**.

**VERDICT: FAIL** — kein Blocker mehr, aber NEU-MAJOR-1 lässt die Migration bei
einer real möglichen Bestandsdatenlage die gesamte Transaktion abbrechen.

---

# Re-review round 3

- Kopf jetzt `3d97c41` (`fix(ablaeufe): resolve round 3 review findings`), Basis unverändert `e401d12`
- Diff `git diff e401d12..HEAD` — 20 Dateien, +3187/−200
- Erneut read-only: keine Quelldatei wurde verändert. Alle Datenprüfungen liefen in
  Wegwerf-Datenbanken aus dem Produktionsschema-Snapshot
  (`supabase/postgres:15.8.1.085`); Datenbanken und Container wurden anschließend
  gelöscht.

## Gates und Dry-Run (selbst ausgeführt)

```
$ ./node_modules/.bin/vitest run --configLoader native
 Test Files  35 passed (35)
      Tests  228 passed (228)
   Duration  6.19s
 EXIT=0

$ ./node_modules/.bin/tsc --noEmit -p .
 (keine Ausgabe)
 EXIT=0

$ bash .../migration-dryrun.sh supabase/migrations/20260831150000_visual_shift_guide_editor.sql
 restore: 0 errors (extension/duplicate noise expected), tables=243
 OK   20260831150000_visual_shift_guide_editor.sql
 EXIT=0

$ git diff --check e401d12..HEAD
 EXIT=0
```

Die einzige auf diesem Branch neue Migrationsdatei ist weiterhin
`20260831150000_visual_shift_guide_editor.sql` (`git diff --name-status
e401d12..HEAD -- supabase/migrations/` liefert genau diesen einen `A`-Eintrag;
`20260831100944_application_assessments_training_onboarding.sql` liegt bereits auf
der Basis). **Der Dry-Run ist PASS.** `next build` habe ich auftragsgemäß erneut
nicht ausgeführt.

## Status der Befunde aus Re-review 2

Der Implementierer hat auch in Runde 3 keinen Befund bestritten; zehn Punkte sind
als FIXED gemeldet, `NEU-NOTE-2` ausdrücklich als SKIPPED. Es gibt daher wieder
keine REFUTATION-ACCEPTED/REJECTED-Einträge im engeren Sinn; `NEU-NOTE-2` wird als
begründete Zurückstellung geführt.

- **NEU-MAJOR-1 — VERIFIED-FIXED.** `migration:62-66` füllt `checkup_templates.tenant_id`
  jetzt vor dem Guard aus `locations.tenant_id` über die produktiv vorhandene
  `location_id`. Genau der Runde-2-Fixture real gegen den Snapshot nachgestellt
  (zwei Tenants, zwei Standorte, ein Leitfaden mit Bereich, eine Kontrollliste mit
  `location_id` und `department_id is null`): Die Migration läuft jetzt mit
  `MIG_EXIT=0` durch statt mit `ERROR: checkup_templates contains legacy rows
  whose tenant/location scope is ambiguous` abzubrechen. Die Kontrollliste erhält
  `tenant=2222…` / `loc=aaaa…0002`, der Leitfaden bleibt korrekt bei Tenant A. Die
  Reihenfolge stimmt: Spalte anlegen → Standort-Ableitung → Bereichs-Ableitung →
  Einzeltenant-/Einzelstandort-Fallback → Guard.
- **NEU-MINOR-1 — VERIFIED-FIXED.** `migration:15-21` zieht die Zuordnung
  `phase → ablauf_typ` in ein eigenes, join-freies `update`. Real nachgestellt mit
  einem Tenant/einem Standort und vier bereichslosen Leitfäden: `opening → opening`,
  `closing → closing`, `midday → other`, `phase is null → other`. Die Ablaufart
  bleibt für standortweite Legacy-Leitfäden korrekt. Nebenwirkung siehe NEU3-NOTE-2.
- **NEU-MINOR-2 — VERIFIED-FIXED (mit Einschränkung, siehe NEU3-MINOR-1).**
  `executions/route.ts:272-276` schreibt bei fehlendem Prüfer
  `independentReviewRequired: false` und `reviewNotice: "Ohne unabhängige prüfende
  Person eingereicht"` in `procedure_results`; das war eine der beiden von mir
  genannten zulässigen Varianten und persistiert am Datensatz. Die zusätzlich
  behauptete `review_note`-Kennzeichnung funktioniert dagegen nicht — dazu
  NEU3-MINOR-1.
- **NEU-MINOR-3 — VERIFIED-FIXED.** `checkups/[id]/editor.tsx:62` normalisiert
  `middle → midday` einmalig bei der Formularinitialisierung; `:122` sendet nur noch
  `form.phase`. Der Weg zur englischen Zod-Standardmeldung ist damit zu. Das Select
  hat weiterhin eine passende Leeroption `<option value="">Nicht festgelegt</option>`
  (`:127`), `phase: ""` bleibt also darstellbar und wird vom Route-Enum akzeptiert
  (`checkups/[id]/route.ts:13`). `grep -rn "'middle'\|\"middle\""` über `app/`/`lib/`
  findet in C-F-Code nur noch diese eine Normalisierungszeile (die übrigen Treffer
  sind `textAnchor="middle"` in unbeteiligten SVGs).
- **NEU-NOTE-1 — VERIFIED-FIXED.**
  `ablaeufe/schichtleitfaeden/[id]/ausfuehren/page.tsx` ist auf 42 Zeilen umbrochen,
  0 Zeilen > 160 Zeichen, längste 66. Ein Whitespace-normalisierter Vergleich gegen
  `8e648e0` zeigt außer dem inlinen der `query`-Zwischenvariable und
  Anführungszeichenstil **keine** semantische Änderung: dieselben Filter
  (`id`, `tenant_id`, `location_id`, `aktiv=true`), dieselben `notFound()`-Pfade.
- **NEU-NOTE-2 — ZURÜCKSTELLUNG AKZEPTIERT (SKIPPED).** Die Verschärfung von
  `checkup_templates_read_all` war von mir selbst ausdrücklich als Bestandsproblem
  und Folgepacket-Empfehlung formuliert. Eine RLS-Änderung ohne isolierte
  Policy-Tests in einer Fix-Runde zu vermeiden, ist die richtige Entscheidung.
  Der Punkt bleibt offen und gehört in ein Folgepacket.
- **NEU-NOTE-3 — VERIFIED-FIXED.** `migration:123-124` verwendet
  `coalesce(nullif(...,''),nullif(...,''),'')`. Real nachgestellt:
  `{"required":"","pflicht":"nein"} → required=false`,
  `{"required":"","pflicht":""} → true`, `{"pflicht":"ja"} → true`,
  `{"required":"vielleicht"} → true` (sicherer Default). Die übrigen
  Normalisierungsgarantien früherer Runden sind unverändert gültig: Skalar-Schritt
  behält seinen Text (`step-1-5 = "Skalar-Schritt"`), Objekt ohne Titel wird
  `"Schritt 6"` statt serialisiertem JSON, Fremdfelder (`estMin`, `aufgabe`,
  `extra`) überleben.
- **NEU-NOTE-4 — VERIFIED-FIXED.** `/node_modules` ist aus `.gitignore` entfernt,
  `node_modules/` (Zeile 2) bleibt.
- **NEU-NOTE-5 — VERIFIED-FIXED.** `docs/agents/HANDOFF.md:395` hat jetzt eine
  Leerzeile vor der C-F-Überschrift; die Gate-Zeile nennt „Vitest 228/228".
- **NEU-NOTE-6 — NICHT VERIFIZIERT (unverändert).** `next build` gehörte erneut
  nicht zu meinem Auftrag; die Angabe „228/228 statische Seiten" bleibt von mir
  ungeprüft. Der Playwright-Spec ist weiterhin ehrlich `skip`.

## Neue Befunde

### MINOR

**NEU3-MINOR-1 — Die `review_note`-Kennzeichnung wird von der RPC stillschweigend verworfen**
`app/api/ablaeufe/executions/route.ts:295-298`
Die Route übergibt bei fehlendem Prüfer
`p_review_note: "Ohne unabhängige prüfende Person eingereicht"` zusammen mit
`p_status: "wartet_auf_pruefung"`. `update_operational_task_as_actor` schreibt
`review_note` aber ausschließlich für Abschlussstatus:
`review_note=case when p_status in ('erledigt','nicht_bestanden','storniert')
then nullif(p_review_note,'') else review_note end`
(`supabase/migrations/20260830183000_predeploy_operational_hardening.sql:68`).
Real gegen den Snapshot nachgestellt (Alleinarbeiter, `controller_employee_id
is null`, `accountable_employee_id = actor`):

```
select ... from update_operational_task_as_actor(…,'wartet_auf_pruefung','Ohne unabhängige prüfende Person eingereicht');
 status              | review_note
 wartet_auf_pruefung | <NULL>
-- gespeicherte Zeile ebenfalls review_note = NULL
-- danach Selbstabschluss durch denselben Actor:
select ... from update_operational_task_as_actor(…,'erledigt',null);
 erledigt | <NULL> | reviewed_by = eeeeeeee-…-0001   (= der Ausführende)
```

Der Report behauptet in „Fix round 3", die Ausführung werde „mit derselben Warnung
als kanonische `review_note` gekennzeichnet". Das ist am Produktionsschema
widerlegt: das Feld bleibt `NULL` und wird beim späteren Abschluss ohnehin
überschrieben. Wirksam ist nur die `procedure_results`-Hälfte des Fixes. Da
`review_note` das einzige der beiden Felder ist, das überhaupt eine Leseseite hat
(`app/(neo)/neo/app/mitarbeiter/page.tsx`, `responsibility-client.tsx`), bleibt der
fehlende zweite Blick für jede Person in der Oberfläche unsichtbar.
*Fix:* entweder `review_note` beim Einreichen direkt über den Service-Client
setzen (analog zum `procedure_results`-Update zwei Zeilen darüber) oder die
Behauptung im Report streichen und ausschließlich `procedure_results` als
Auditmarker führen — dann aber mit einer Leseseite.

### NOTE

**NEU3-NOTE-1** — `procedure_results` wird ausschließlich geschrieben
(`executions/route.ts:280`) und von keiner Stelle im Repo gelesen:
`grep -rn "procedure_results\|independentReviewRequired\|reviewNotice"` über
`app/`, `lib/`, `tests/` liefert nur die drei Schreibzeilen. Der Auditmarker
existiert damit nur als per SQL abfragbarer Spaltenwert; weder die prüfende
Leitung noch die Bestätigungsseite sehen ihn. Zusammen mit NEU3-MINOR-1 heißt
das: die Vier-Augen-Lücke ist dokumentiert, aber nicht sichtbar.

**NEU3-NOTE-2** — Das neue join-freie `update` (`migration:17-21`) läuft ohne
`where`-Klausel über die gesamte Tabelle. Beim Erstlauf ist das korrekt (die
Spalte wird in derselben Migration mit `default 'other'` angelegt). Ein zweiter
Lauf überschreibt jedoch nachträglich gesetzte Werte: real nachgestellt — ein auf
`hygiene_temperature` geänderter Leitfaden mit `phase='opening'` steht nach dem
erneuten Anwenden wieder auf `opening`. Die Runde-2-Fassung war hier durch
`where (g.tenant_id is null or g.location_id is null)` geschützt; die in Runde 0
bestätigte Idempotenz gilt für dieses Statement also nicht mehr. Unter dem
Supabase-Migrationstracking wird die Datei nicht erneut angewendet, deshalb NOTE
und nicht MINOR. Die Normalisierung selbst (`migration:137`) bleibt über
`schemaVersion<>'1'` idempotent — im zweiten Lauf verifiziert unverändert.

**NEU3-NOTE-3** — `independentReviewRequired: task.controller_employee_id !== null`
(`executions/route.ts:272`) ist gegenläufig benannt: der Wert ist gerade dann
`false`, wenn keine unabhängige Prüfung stattfinden kann, also wenn sie am
dringendsten fehlt. Ein späterer Auswerter, der auf
`independentReviewRequired === true` filtert, findet genau die unkritischen Fälle.
`reviewNotice` ist eindeutig; der Feldname sollte `independentReviewAvailable`
o. ä. heißen.

**NEU3-NOTE-4** — Der Konflikthinweis zu `docs/agents/HANDOFF.md` ist mit der
ergänzten Leerzeile entschärft, die Datei bleibt aber von allen Paketen geteilt.
Beim parallelen Merge ist weiterhin mit einem Textkonflikt am Dateiende zu rechnen.

## Regressionsprüfung Runde 3

Der Fix-Commit `3d97c41` berührt sieben Dateien. Geprüft und ohne Regression:

- `.gitignore` — `node_modules/` bleibt wirksam.
- `checkups/[id]/editor.tsx` — nur die zwei `middle`-Zeilen; Leeroption und
  Route-Enum passen weiterhin zusammen.
- `executions/route.ts` — `controller_employee_id` zusätzlich selektiert; der
  bestehende Pflichtfoto-, Eindeutigkeits- und Evidence-Pfad (`:178-266`) ist
  unverändert, ebenso Start-Gating und Prüferermittlung (`:67-106`).
- `ausfuehren/page.tsx` — semantisch identisch (siehe NEU-NOTE-1).
- `migration` — voller Datendurchlauf gegen den Snapshot reproduziert alle
  Garantien der Runden 0–2 (Skalar-Schritte, Fremdfelder, kein JSON-Titel,
  `ja/nein`-Werte, Scope-Guard, Policies, Index). Kein Rückschritt.
- `HANDOFF.md`, `reports/2026-08-31-c-f.md` — nur Dokumentation.

Formatierung der berührten Dateien bleibt im Rahmen: `ausfuehren/page.tsx` 42/0/66,
`executions/route.ts` 311/0/91, `checkups/[id]/editor.tsx` 287/0/112
(Zeilen / Zeilen > 160 / längste Zeile).

## Zählung Re-review 3

- Aus Re-review 2: **9 VERIFIED-FIXED**, 0 NOT-FIXED, 1 begründet zurückgestellt
  (`NEU-NOTE-2`), 1 von mir nicht verifizierbar (`NEU-NOTE-6`, `next build`).
  0 REFUTATIONEN — der Implementierer hat erneut keinen Befund bestritten.
- Neu in dieser Runde: **0 BLOCKER, 0 MAJOR, 1 MINOR, 4 NOTE.**
- Gates: vitest EXIT=0 (228/228), tsc EXIT=0, Migration-Dry-Run **EXIT=0 (OK)**,
  `git diff --check` EXIT=0.

**VERDICT: PASS-WITH-MINORS** — kein Blocker und kein Major offen. Der
NEU-MAJOR-1-Deploy-Abbruch aus Runde 2 ist am Produktionsschema mit Daten
gegengeprüft behoben. Offen bleiben NEU3-MINOR-1 (verworfene `review_note`, dazu
eine sachlich falsche Zeile im Report) sowie vier NOTEs; keiner dieser Punkte
gefährdet Deploy oder Datenbestand.
