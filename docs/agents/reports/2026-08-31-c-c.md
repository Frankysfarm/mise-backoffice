# Packet C-C — Wiederkehrende Aufgaben und Übergaben

## AUDIT

### Vorhanden

- `operational_task_templates`, `operational_tasks` und `operational_task_evidence` bilden bereits ein gemeinsames Aufgabenmodell; Einmalaufgaben und schichtgebundene Vorlagen wurden bereits in Neo und der Mitarbeiter-App angezeigt.
- `materialize_shift_operational_tasks` erzeugt Schichtaufgaben idempotent über `source_type/source_id`; Probeschichten, abgesagte Schichten und fortgeschrittene Aufgaben sind abgesichert.
- `process_operational_escalations` läuft über `/api/cron/operational-escalations`, schreibt Audit-Log und In-App-Benachrichtigungen.
- `responsibility_handovers` erfasste Absender, Empfänger, Zeitraum, Freitext und einen einstufigen Status `offen → angenommen`.
- Bereichsverantwortung, Stellvertretung, Standort-Scope, Task-Nachweise und die Mitarbeiteransicht waren wiederverwendbar.

### Versteckt / schwer auffindbar

- Schichtabläufe und Übergaben lagen im umfangreichen Bereich „Mitarbeiter & Bereiche“; die Abläufe-Startseite verlinkte operative Aufgaben dorthin.
- `recurrence_rule` war bereits als JSON-Spalte vorhanden, hatte aber weder verständlichen Editor noch Kalendermaterialisierung.
- Übergaben hatten ein generisches `evidence`-Feld, aber keine getrennte Lese- und Bestätigungskette.

### Fehlend

- Kalenderregeln täglich, Wochentage, wöchentlich, Monatsdatum, erster Wochentag, individueller Tages-/Wochenrhythmus; Vorschau, Bearbeiten, Pausieren, Löschen.
- Zuweisungsziele Mitarbeiter, Rolle, Bereich, Schicht und Standort.
- Idempotente Kalendermaterialisierung in Berliner Betriebszeit inklusive Monatsende/DST.
- Strukturierte Übergabekategorien, offene Aufgaben, getrennte „Gelesen“-/„Bestätigt“-Zeitpunkte und Management-Audit.
- E-Mail-Versand im bestehenden Eskalationslauf und Benachrichtigung der Stellvertretung.

## CHANGES

- `app/(neo)/neo/app/ablaeufe/page.tsx`: Operative Karte führt zur neuen gemeinsamen Aufgaben-/Übergabeverwaltung.
- `app/(neo)/neo/app/ablaeufe/aufgaben/page.tsx`: tenant- und standortbegrenzte Serverabfragen für Regeln, offene Aufgaben und Audit.
- `app/(neo)/neo/app/ablaeufe/aufgaben/operations-client.tsx`: mobiler deutscher Editor, Vorschau, Regel-Lifecycle, strukturierte Schichtübergabe und Auditkette.
- `app/api/operations/tasks/route.ts`: validierte Manager-/Mitarbeiteraktionen mit Tenant-/Standortprüfung.
- `lib/operations/recurrence.ts`: deterministische Berlin-Vorschau und Validierung.
- `lib/operations/handover-state.ts`: testbare Ack-Zustandsmaschine.
- `app/api/cron/operational-escalations/route.ts`: Materialisierung der nächsten 14 Tage und E-Mail-Hook über den vorhandenen Resend-Sender.
- `tests/operations/recurring-tasks-handovers.test.ts`, `scripts/tests/077_recurring_tasks_handovers.sql`, `tests/e2e/operations-recurring-handover.spec.ts`: Unit-, isolierte SQL- und Persona-Browserabdeckung.

## DB CHANGES

Migration: `20260831120000_recurring_tasks_and_handover_ack.sql` (additiv, explizites `begin/commit`).

- `operational_task_templates`: `target_type`, `target_role`, `recurrence_anchor`, `paused_at`, `deleted_at`; Target-Constraint und Due-Index.
- `operational_tasks`: `recurrence_due_at` plus partieller Unique-Index `(template_id, recurrence_due_at)` als Idempotenzschlüssel.
- `responsibility_handovers`: `open_task_ids`, getrennte Texte für Vorkommnisse/Bestände/Schäden/Reinigung/Hinweise sowie `read_at/read_by` und `confirmed_at/confirmed_by`; Status `gelesen` ergänzt und Audit-Index angelegt.
- RPCs: `save_recurring_operational_template` (Manager, Tenant/Standort/Referenzen geprüft), `materialize_recurring_operational_tasks` (nur `service_role`), `acknowledge_responsibility_handover` (Empfänger oder standortberechtigtes Management).
- Funktionen: `recurring_template_due_at`; Trigger `operational_tasks_escalation_notify` benachrichtigt Verantwortliche und aktive Stellvertretung.
- RLS: bestehende `can_manage_operational_location`-/Handover-Policies bleiben maßgeblich; privilegierte Materialisierung ist nicht für `authenticated` ausführbar.
- Rollback: neue Trigger/Funktionen/Indizes entfernen, neue Spalten erst nach Sicherung der darin entstandenen Regeln/Ack-Daten entfernen; bestehende Tasks und Handovers bleiben durch die additive Migration erhalten.

## TESTS RUN

- `./node_modules/.bin/vitest run --configLoader runner tests/operations/recurring-tasks-handovers.test.ts` — 1 Datei, 9 Tests, **grün**. `--configLoader runner` ist nötig, weil der geteilte `node_modules`-Symlink read-only ist und Vites Standardloader sonst vor Teststart in `node_modules/.vite-temp` schreiben will.
- `./node_modules/.bin/vitest run --configLoader runner` — 35 Dateien, 231 Tests, **grün**.
- `./node_modules/.bin/tsc --noEmit -p .` — Exit 0.
- `./node_modules/.bin/tsc --noEmit -p tsconfig.delivery-hardening.json` — Exit 0.
- `./node_modules/.bin/next build` — Exit 0; 228 statische Seiten erzeugt, Route `/neo/app/ablaeufe/aufgaben` enthalten. Bestehende repositoryweite Lint-Warnungen bleiben Warnungen.
- `git diff --check` — Exit 0.
- `./node_modules/.bin/playwright test` — ausgeführt, **nicht grün**: erster Versuch scheiterte vor Tests am read-only Standalone-`node_modules`-Symlink. Wiederholung gegen den bereits laufenden Server: 6 bestanden, 4 C-C-Tests ohne `E2E_AUTH_STATE` erwartungsgemäß übersprungen, 22 bestehende Smoke-Tests nach Server-Reset mit `ECONNRESET/ECONNREFUSED` fehlgeschlagen. Es gibt auf diesem Host laut Koordination keine `.env` und keine Datenbank; daher keine falsche Green-Behauptung.
- `scripts/tests/077_recurring_tasks_handovers.sql` wurde geschrieben, aber mangels lokaler Datenbank nicht ausgeführt.

## OPEN ITEMS

- Vor Integration Migration in einer isolierten Supabase-Testdatenbank anwenden und `scripts/tests/077_recurring_tasks_handovers.sql` ausführen; anschließend authentifizierten Playwright-Lauf mit Manager- und Folgeschicht-Storage-State auf Desktop und Pixel-5-Profil durchführen.
- E-Mail wird nur versendet, wenn `RESEND_API_KEY`/`MAIL_FROM` gesetzt sind; ohne Key meldet der bestehende Sender den Versand ehrlich als übersprungen. In-App-Benachrichtigungen werden unabhängig davon geschrieben.
- Das vorhandene Handover-`evidence`-Array bleibt erhalten; ein eigener Foto-Uploader für Übergaben wurde nicht parallel zum bestehenden aufgabenbezogenen Evidenzspeicher aufgebaut. Ein Storage-/RLS-konformer gemeinsamer Evidence-Ausbau sollte integratorseitig entschieden werden.

## HOW TO VERIFY MANUALLY

### Desktop — Unternehmer/Filialleitung

1. Neo → **Listen & Abläufe** → **Operative Aufgaben & Übergaben**.
2. Standort wählen, **Neue Regel** öffnen, jede Wiederholungsart prüfen; Vorschau kontrollieren, Mitarbeiter/Rolle/Bereich/Schicht/Standort wählen und speichern.
3. Regel bearbeiten, pausieren und löschen; **Jetzt erzeugen** zweimal drücken und in den offenen Aufgaben prüfen, dass dieselbe Fälligkeit nur einmal existiert.
4. **Übergabe erstellen** öffnen; offene Aufgaben auswählen und die fünf Kategorien ausfüllen; an eine andere aktive Person senden.
5. Als Empfänger öffnen: erst **Gelesen**, danach **Bestätigt**. Im **Übergabe-Audit** müssen Ersteller, Leser, Bestätiger und beide Zeitpunkte sichtbar sein.

### Mobile — Folgeschicht/Mitarbeiter

1. Pixel-5/390px öffnen; die drei Tabs müssen horizontal bedienbar und Formulare einspaltig sein.
2. Eine unbestätigte Übergabe öffnen; gelbe Hervorhebung und „Ungelesen“ prüfen.
3. **Gelesen** antippen; erneutes Öffnen darf den Zeitpunkt nicht überschreiben. Danach **Bestätigt** antippen und grünen Auditstatus prüfen.
4. Mitarbeiter-App → „Mein Bereich heute“: materialisierte Aufgabe mit Fälligkeit, Status und Verantwortlichkeit prüfen; überfällige Aufgabe muss In-App-Benachrichtigung und bei konfiguriertem Resend eine E-Mail an Verantwortliche/Stellvertretung auslösen.

## Fix round 1

### Findings

- B1 — **FIXED**: Materialisierung verlangt jetzt `p_tenant_id` und `p_location_id`; Cursor, Manager-Route und Cron-Aufrufe sind entsprechend standort- und tenantbegrenzt.
- B2 — **FIXED**: SQL-Due-Funktion verwendet `recurrence_anchor`; persistierter Zwei-Tage-Rhythmus ist im SQL-Smoke-Test abgedeckt.
- B3 — **FIXED**: Der Alt-Endpunkt bestätigt nur noch über `acknowledge_responsibility_handover`; beide Mitarbeiterabfragen enthalten `gelesen`.
- B4 — **FIXED**: Die bestehende Mitarbeiter-App bietet dem Empfänger getrennte Aktionen „Gelesen“ und „Bestätigen“ samt hervorgehobenem offenen Zustand.
- M1 — **FIXED**: Strukturierte Übergaben erfordern Managerrechte oder eine aktuell gültige Bereichsleitung.
- M2 — **FIXED**: Pausieren und Löschen stornieren offene zukünftige Instanzen der Regel.
- M3 — **FIXED**: Wochentage erscheinen als deutsche Kurz-/Langnamen statt Zahlen.
- M4 — **FIXED**: Wöchentliche Regeln bieten einen Wochentag; Monatswochentage bieten Ordinal und Wochentag.
- M5 — **FIXED**: Das Audit besitzt bedienbare Von-/Bis-Datumsfelder.
- M6 — **FIXED**: Offene Aufgaben werden serverseitig zu Titeln aufgelöst und im Audit unter „Offene Aufgaben“ ausgegeben.
- M7 — **FIXED**: Rohfehler werden serverseitig protokolliert; der Client erhält eine feste deutsche Meldung.
- M8 — **FIXED**: Der E-Mail-Lookback entspricht jetzt dem Drei-Minuten-Cronintervall.
- M9 — **FIXED**: Die ungenutzte TypeScript-Zustandsmaschine wurde entfernt; der SQL-Smoke-Test prüft die ausgelieferte RPC-Definition und Read-before-confirm-Regel.
- MINOR-1 (Winterzeit) — **FIXED**: Datumsgrenzen werden mit der Berlin-Zeitzone statt festem `+02:00` erzeugt.
- MINOR-2 (wiederholtes Lesen) — **FIXED**: `read` ist bei vorhandenem `read_at` ein No-op.
- MINOR-3 (doppelte In-App-Nachricht) — **FIXED**: Der Trigger schließt den bereits vom Sweep versorgten Eskalationsverantwortlichen aus.
- MINOR-4 (Bearbeiten reaktiviert Regeln) — **FIXED**: Speichern verändert Aktiv-/Pause-/Löschstatus nicht mehr; „Fortsetzen“ ist eine eigene Aktion.
- MINOR-5 (gelöschte Schichtvorlagen) — **FIXED**: Die bestehende Schichtvorlagenabfrage filtert `deleted_at is null`.
- MINOR-6 (Audit-Reihenfolge) — **FIXED**: Unbestätigte Übergaben werden vor bestätigten, jeweils neueste zuerst sortiert.
- MINOR-7 (Technikjargon) — **FIXED**: Materialisierungs-/Idempotenzformulierungen wurden durch verständliche deutsche Texte ersetzt.
- MINOR-8 (falsche Vertretung per Mail) — **FIXED**: E-Mail-Empfänger werden aus aktiven `stellvertretung`-Bereichszuordnungen ermittelt.
- MINOR-9 (Mail-HTML/Link) — **FIXED**: Namen und Titel werden HTML-escaped; CTA verwendet eine absolute App-URL mit Produktionsfallback.
- MINOR-10 (`immutable`) — **FIXED**: `recurring_template_due_at` ist jetzt `stable`.
- MINOR-11 (200 Aufgaben vorausgewählt) — **FIXED**: Ab mehr als 20 offenen Aufgaben wird nichts vorausgewählt und die UI fordert zur bewussten Auswahl auf; die bestehende Bereichszuordnung bleibt erhalten.

### Exact gate results

- `./node_modules/.bin/vitest run --configLoader runner` — **Exit 0**; 35 Dateien, 230 Tests bestanden.
- `./node_modules/.bin/tsc --noEmit -p .` — **Exit 0**, keine Diagnosen.
- `./node_modules/.bin/next build` — erster Lauf **Exit 1** vor der Kompilierung wegen eines alten `.next/standalone/node_modules`-Symlinks auf das read-only geteilte `node_modules` (`EROFS`). Nach Entfernen ausschließlich dieses generierten Symlinks zweimal erneut ausgeführt: jeweils `Compiled successfully` und Typ-/Lintphase abgeschlossen; der Prozess wurde auf diesem Host danach ohne Next-Diagnose durch die Laufzeit beendet (kein `BUILD_ID`, effektiver Gate-Status **nicht grün**). Repositoryweite bestehende Lint-Warnungen blieben Warnungen.
- `git diff --check` — **Exit 0**.

Der isolierte SQL-Smoke-Test bleibt mangels lokaler Datenbank unausgeführt. Die Supabase-Änderungen wurden deshalb statisch geprüft und mit ausführbaren Assertions in `scripts/tests/077_recurring_tasks_handovers.sql` ergänzt.
