# Packet C-INT — Vollausbau-Integration 2026-08-31

## AUDIT

- Ausgangspunkt `3dc87de` auf `vollausbau/integration`; sechs Paketbranches in der vorgeschriebenen Reihenfolge mit je einem Merge-Commit integriert: K-B, C-A, C-F, C-C, C-D, C-E.
- Nach jedem Merge lief `./node_modules/.bin/tsc --noEmit -p .` mit Exit 0.
- Bestehende Neo-Shell, Supabase Auth, Tenants, Locations, Employees, Aufgaben, Benachrichtigungen und Reporting wurden weiterverwendet. Keine zweite Anmeldung, kein paralleles Mitarbeiter- oder Aufgabenmodell.
- Alle neuesten Claude-Re-Reviews gelesen: 0 offene BLOCKER/MAJOR, 15 offene MINORs; alle 15 behoben, kein MINOR übersprungen.
- Unabhängiger Integrationsreview: `PASS-WITH-MINORS`, 0 BLOCKER/MAJOR. Beide neuen Migration-MINORs wurden vor Commit geschlossen: Legacy-Ablauftypen werden nur aus dem Defaultwert normalisiert; ein bereits in einem anderen Schema installiertes `pgcrypto` wird nach `extensions` verschoben.

## CHANGES

- Merge-Konflikte: `HANDOFF.md` additiv zusammengeführt; Cron kombiniert Training, wiederkehrende Aufgaben und Eskalationsmail; Neo-Abläufe-Karte behält Rollen-Gate und führt zur vollständigen Aufgaben-/Übergabeseite.
- K-B: mutationssicher bei Offline/4xx, keine unhandled rejection, kein No-op-Organigramm-Audit; unnötige Avatar-Storage-Policies entfernt.
- C-A: korrekter Neo-Header, Operations-Entitlement für `/application-tests`, shelltreuer Zurück-Link, `extensions.pgcrypto` wird selbst hergestellt.
- C-F/C-C: Warnung für fehlende unabhängige Prüfung wird persistiert; Übergaben behalten Zeilenumbrüche.
- C-D: E-Mail- und In-App-Verfügbarkeitserinnerungen werden innerhalb 12 Stunden dedupliziert; erneute echte Erinnerungsrunde bleibt möglich und Zähler wird angezeigt.
- C-E: NULL-sichere Inventurhistorie, Test prüft effektive Korrekturmigration, Bestellvorschläge werden ohne zweite kollidierende Gruppierung erzeugt.
- Kohärenz: eine Neo-Navigation; Bewerbungstest → Einstellung → Profil/Schulung, Dienstplan → Ablauf/Aufgabe → Übergabe und Lagerplatz → Inventur → Bestellvorschlag bleiben über kanonische Datensätze verbunden. Alle neuen Oberflächen sind aus der Neo-Shell in höchstens zwei Klicks erreichbar.
- Dokumentation: Migration-Runbook und konkrete Desktop/Mobile-Persona-Abnahme für Bewerber, Mitarbeiter, Schichtleitung, Filialleitung und Admin.

## DB CHANGES

- Acht geordnete additive Migrationen seit `87320087`; Details/Rollback: `docs/agents/deploy/2026-08-31-migrations.md`.
- Jede Datei enthält explizit `begin;` und `commit;`; Korrekturmigrationen ersetzen nur identische RPC-Signaturen.
- Gemeinsamer Snapshot-Dry-Run: 243 Tabellen, Restore 0 Fehler, alle acht Dateien `OK`, Exit 0. Der exakte Output steht im Runbook.
- Keine Produktionsmutation und kein Deploy durchgeführt.

## TESTS RUN

- Per-Merge-Typecheck: 6/6 Exit 0.
- Fokus-Vitest nach MINOR-Fixes: 8 Dateien, 57 Tests bestanden.
- Migration-Dry-Run: 8/8 `OK`, Exit 0.
- Playwright ohne Live-DB: `--grep-invert 'reaches the database'` → 32 bestanden, 8 übersprungen, Exit 0 (Desktop Chrome + Pixel 5).
- Ready-not-run: Ablauf-Editor (expliziter DB/Auth-Skip), Dienstplan-Loop (`E2E_AUTH_STATE`), wiederkehrende Aufgaben/Übergabe (`E2E_AUTH_STATE`) und Delivery-Health (Live-DB). Der Browser-Helfer `agent-browser` war nicht installiert; Playwright selbst lieferte die Browser-Evidence.
- Finales Vitest: 42 Dateien, 271 Tests bestanden, Exit 0.
- Finaler Typecheck: keine Diagnosen, Exit 0.
- Finaler Next-Build: kompiliert, Typ-/Lintphase abgeschlossen, 242/242 statische Seiten erzeugt, Exit 0. Bestehende ESLint-Warnungen bleiben nicht-blockierend; keine neue Warnung aus den Integrator-Fixes.
- Finales `git diff --check`: keine Ausgabe, Exit 0.

## OPEN ITEMS

- Vor Deployment: Migrationen auf isolierter Supabase-Staginginstanz anwenden; authentifizierte Persona-Specs mit Manager-/Mitarbeiter-Storage-State ausführen; Resend-Zustellung und Benachrichtigungsworker prüfen.
- Bewusste Produkthinweise aus Reviews: Umlagerung bewegt wegen eines Lagerplatzes pro Produkt den Gesamtbestand; QR-Labels führen absichtlich in die mobile Neo-Oberfläche; keine Produktion wurde berührt.

## HOW TO VERIFY MANUALLY

- Vollständige Klickwege, erwartete Inhalte, Leer-/Fehler-/Berechtigungszustände und Desktop/Mobile-Prüfung: `docs/agents/reports/2026-08-31-persona-e2e.md`.
- Migration und Rollback: `docs/agents/deploy/2026-08-31-migrations.md`.

## Fix round 1 — Claude Final Review

- M1/M3: Neue Korrekturmigration `20260831160000_vollausbau_review_corrections.sql`; Dienstplanvorschläge akzeptieren `aktiv`, `in_training` und `in_probe`. Assessment-Mehrfachantworten werden serverseitig als sortierte, deduplizierte ID-Mengen bewertet. Die API dedupliziert/sortiert zusätzlich, und die Template-Validierung weist doppelte richtige IDs zurück. `20260831100944` blieb unverändert.
- M2: Runbook korrigiert die Additiv-Aussage. Es nennt den `training_progress`-DELETE zur Deduplizierung sowie das Überschreiben von `assessment_templates.status/slug`, verlangt ein verifiziertes Backup und beschreibt die datenbezogene Wiederherstellung. Der bereits reviewte DELETE wurde nicht nachträglich geändert.
- M4: Runbook enthält eine mit dem Vor-Migrationsschema ausführbare Produktionsabfrage, welche die Singleton-Fallbacks nachbildet. Beide Resultsets müssen vor dem Deploy leer sein; andernfalls werden die betroffenen Guides/Templates vorab eindeutig gescoped.
- Sichere MINOR-Fixes: aktiver Status für Schulungszuweiser; RLS für `checkup_templates`; echte Umlagerungsmengen im Audit; QR-Zählung persistiert den gezählten Bestand; nur echte Absagen werden `cancelled`; Veröffentlichungen gehen nur an aktive Workflow-Mitarbeiter und enthalten einen HTML-Fallback. Legacy-Ablauf-JSON und das gespiegelt benötigte Feld `gültig_monate` wurden wegen Daten-/Kompatibilitätsrisiko dokumentiert statt massenhaft umgeschrieben. API-Grenzen bleiben für deutsche, begrenzte Fehlermeldungen verantwortlich.
- Gate-Evidence: `./node_modules/.bin/vitest run` → 42 Dateien / 276 Tests, Exit 0. `./node_modules/.bin/tsc --noEmit -p .` → keine Diagnosen, Exit 0. `./node_modules/.bin/next build` → kompiliert, 242/242 statische Seiten, Exit 0; nur vorbestehende Warnungen. `bash …/migration-dryrun.sh supabase/migrations/20260831*.sql` → 9/9 OK, Exit 0. Der exakt angeforderte breitere Glob `2026083*.sql` wurde ebenfalls ausgeführt; die neun Fix-Round-Dateien sind OK, aber der Gesamtbefehl endet wegen des vorbestehenden Snapshot-Konflikts in `20260830154500_daily_clarity_automation.sql` (`process_operational_escalations already exists`) mit Exit 1. `git diff --check` → Exit 0.
