# Packet C-D — Dienstplan Vollausbau

## AUDIT

### Existed

- `/neo/app/dienstplan` reused the established `shifts` week board, manual creation/editing, drag-and-drop, PDF, swap requests and ArbZG validation.
- `/neo/app/dienstplan/templates` exposed CRUD for individual `shift_templates` (name, time, position, break, type, department, location), but not a complete week template with weekday/headcount.
- `employee_availability` and `availability_exceptions` already represented recurring availability, preferred periods, vacation and sickness.
- `weekly_shift_assignment_suggestions`, `generate_weekly_shift_assignment_suggestions` and `confirm_weekly_shift_assignment_suggestion` already provided a deterministic, explainable assistant. It considered regular/exception availability, department/responsibility fit, overlap and assigned weekly hours, and never assigned silently.
- `/mitarbeiter` already displayed the employee's assigned shifts.
- `notifications`, `email_outbox` and `/api/email/process-outbox` were the existing in-app/e-mail delivery mechanism.

### Hidden or unclear

- Individual shift templates were reachable through “Vorlagen”, but their relationship to a complete planning week was unclear.
- The weekly assistant existed, but had no explicit availability deadline, planning-round status or publish boundary.
- ArbZG warnings existed on the board, while qualification, absence and contract-hour conflicts did not share one structured conflict result.

### Missing

- Complete week-template CRUD/copy/delete, weekday/time-window headcount and idempotent application.
- Planning deadline, reminder action, employee states “kann / möchte / kann nicht” and concrete-shift application.
- Explicit draft → published workflow, publication/change records and affected-person notifications.
- A pure conflict function covering every packet-mandated conflict kind.

## CHANGES

- `app/(admin)/schedule/page.tsx`, `planning-controls.tsx`: planning-round controls for template application, deadline, reminders and publication.
- `app/(admin)/schedule/templates/page.tsx`, `week-template-manager.tsx`: owner-friendly complete week-template editor with multiple demand windows, copy and delete; existing individual shift blocks remain available.
- `app/api/scheduling/planner/route.ts`: authenticated manager/employee orchestration with tenant/location checks and service-role RPC calls.
- `app/mitarbeiter/page.tsx`, `availability-loop.tsx`: mobile-first open-shift view and the three availability/application states.
- `lib/scheduling/planner.ts`: pure idempotency, availability normalization, proposal scoring, structured conflict detection and affected-employee calculation.
- `tests/scheduling/*.test.ts`: 15 unit/contract cases.
- `tests/e2e/dienstplan-planning-loop.spec.ts`: authenticated desktop/mobile Playwright path, ready for a test backend/auth state.
- `supabase/migrations/20260831130000_schedule_planning_loop.sql`: additive data model and protected workflows.

## DB CHANGES

- New tables: `schedule_templates`, `schedule_template_slots`, `schedule_weeks`, `shift_availability_responses`, `schedule_publication_changes`.
- New `shifts.schedule_template_instance_key` plus a tenant/location unique partial index for idempotent application.
- RPCs: `manage_schedule_template`, `apply_schedule_template`, `publish_schedule_week` (service-role only).
- The existing `generate_weekly_shift_assignment_suggestions` is preserved as `_v1` and wrapped, not duplicated: the wrapper enforces the deadline and incorporates concrete “kann / möchte / kann nicht” responses.
- Trigger `shifts_published_schedule_change` records post-publication assignment/time/status changes and notifies only old/new affected employees.
- RLS: service-role write policies; authenticated manager reads use `current_tenant_id()` plus `can_manage_operational_location`; employee reads use tenant plus `current_employee_id()`/location access.
- E-mail is honestly queued through the existing `email_outbox`; actual sending remains the existing configured outbox processor/tenant Resend setup.
- Rollback: drop the new trigger/function/RPCs/tables and partial index, restore the `_v1` suggestion function name, then drop `shifts.schedule_template_instance_key`. Existing shifts and legacy `shift_templates` remain intact.

## TESTS RUN

- `./node_modules/.bin/vitest run tests/scheduling/planner.test.ts` — runner could not start because the prescribed shared `node_modules` symlink is read-only at `node_modules/.vite-temp` (EROFS). No green claim for this invocation.
- `./node_modules/.bin/vitest run --configLoader runner tests/scheduling/planner.test.ts tests/scheduling/planning-contract.test.ts` — exit 0; 2 files, 15 tests passed.
- `./node_modules/.bin/vitest run --configLoader runner` — exit 0; 36 files, 237 tests passed.
- `./node_modules/.bin/tsc --noEmit -p .` — exit 0.
- `corepack pnpm typecheck:delivery` — exit 0; environment warned that installed Node is v24.19.0 although repository requests Node 22.
- `NEXT_TELEMETRY_DISABLED=1 ./node_modules/.bin/next build` — exit 0; compiled, typechecked and generated 227 routes. Existing lint warnings remain non-fatal.
- `git diff --check` — exit 0.
- `./node_modules/.bin/playwright test tests/e2e/dienstplan-planning-loop.spec.ts` — not green: first attempt hit read-only standalone symlink cleanup; after moving the disposable `.next` artifact, the server could not reach `/login` because this machine has no Supabase URL/key and the run was stopped. The spec is explicitly gated on `E2E_AUTH_STATE` and ready for the integration environment.

## OPEN ITEMS

- Apply the migration and run the authenticated Playwright scenario against an isolated Supabase test project with manager and employee storage state; this machine has neither database nor `.env` by contract.
- Validate the tenant's Resend configuration/outbox cron to prove physical delivery. The feature queues both in-app and e-mail notifications without pretending an external send occurred.
- Repository-required Node 22 is not installed/selected in this worktree environment (`/usr/bin/node` is v24.19.0); rerun the release gate under Node 22 during integration.
- Minor/night-work validation runs only when a birth date exists, as required. Position matching uses explicit employee position data supplied to the pure engine; no qualification model was invented.

## HOW TO VERIFY MANUALLY

### Desktop owner

1. Open `Neo → Dienstplan → Vorlagen`.
2. Create a Wochenvorlage, add several weekdays/time windows, select area/position, pause and required people; save, edit, copy and delete it.
3. Return to `Neo → Dienstplan`, select one location/week/template, set the deadline and click “Vorlage anwenden”. Repeat once and verify the message says no duplicates were created.
4. Click “Verfügbarkeit anfragen”; verify employee notification and `email_outbox` row.
5. After the deadline click “Vorschläge berechnen”; inspect German reasons/open shifts, confirm or manually edit, then click “Dienstplan veröffentlichen”.
6. Change an assigned published shift and verify `schedule_publication_changes` plus notifications only for the old/new affected employees.

### Mobile employee

1. Open `/mitarbeiter`, choose “Verfügbarkeit”.
2. For offered shifts select “Kann”, “Möchte” (concrete application) or “Kann nicht”; reload and verify the state persists.
3. After publication open “Dienstplan” and verify only the employee's binding shifts are shown.
4. After an owner change, verify the “Dienstplan geändert” notification and the updated shift.

## Fix round 1

### Review findings

- B1 — FIXED: `evaluateScheduleConflicts` is used by the week board and weekly-assistant proposal cards; German conflicts are visible and blocker conflicts disable confirmation. The parallel TypeScript scorer and unused template-key helpers were removed.
- B2 — FIXED: weeks without a `schedule_weeks` row retain the existing assistant path; only an opened round enforces its deadline, with specific German API messages.
- B3 — FIXED: the candidate selection itself reads `shift_availability_responses`, excludes `kann_nicht`, prioritizes `moechte`/`kann`, and can select the next eligible employee.
- B4 — FIXED: policies are dropped before creation, functions use `create or replace`, and the legacy-function rename is guarded by `to_regprocedure`; the migration dry-run below proves a clean restore/application.
- M1 — FIXED: `open_schedule_week` and “Woche manuell öffnen” open a planning round independently of a template.
- M2 — FIXED: applied-shift keys derive from stable template/weekday/start/position/headcount identity rather than regenerated slot UUIDs or row order.
- M3 — FIXED: `/mitarbeiter` reads employee-scoped publication changes and marks affected shifts “Geändert seit Veröffentlichung”.
- M4 — FIXED: manager RLS and the week board expose per-shift `kann`/`möchte`/`kann nicht` responses.
- M5 — FIXED: failed availability taps show the server message; unopened rounds and expired deadlines have distinct German messages, and only shifts in an open round are listed.
- M6 — FIXED: applying a template to a published week is rejected and mapped to a German 409 response.
- N1 — FIXED: repeat publication returns zero; notification/e-mail delivery is aggregated once per employee.
- N2 — FIXED: deadline values are formatted in `Europe/Berlin` for `datetime-local`.
- N3 — FIXED: availability uses `berlinScheduleWeek` instead of UTC weekday arithmetic.
- N4 — FIXED: reminder dates use German display formatting.
- N5 — FIXED: both schedule mails are registered outbox templates rendered through `emailShell`; raw JSON fallback is not used.
- N6 — FIXED: all five new tables have idempotently-created `unified_audit_change` triggers.
- N7 — FIXED: manager RPC actor checks require an active/training/probation status.
- N8 — FIXED: contract-hour conflict totals are limited to the shift's Berlin ISO week, with a regression test.
- N9 — FIXED: template name is stored in `shifts.notiz`; `shifts.position` remains the actual optional position.
- N10 — FIXED: planner API verifies manager location directly and backoffice/admin location ownership by tenant.
- N11 — FIXED: the unused divergent TypeScript instance-key implementation and its misleading test were removed; SQL is the single source.
- N12 — FIXED: weekday has an accessible label and template cards show their location name.

### Required evidence

- `bash /home/openclaw/factory/workspaces/codex-builder/runs/mise-neo/migration-dryrun.sh supabase/migrations/2026083113*.sql` — exit 0:
  `restore: 0 errors (extension/duplicate noise expected), tables=243`
  `OK   20260831130000_schedule_planning_loop.sql`
- `./node_modules/.bin/vitest run --configLoader runner` — exit 0: `Test Files 36 passed (36)`, `Tests 236 passed (236)`, duration 5.06s.
- `./node_modules/.bin/tsc --noEmit -p .` — exit 0, no diagnostics.
- `./node_modules/.bin/next build` — exit 0: compiled successfully and generated 227/227 static pages; existing repository lint warnings remain non-fatal. The first invocation encountered the known read-only `.next/standalone/node_modules` symlink artifact; removing that disposable generated symlink allowed the exact command to complete.
- `git diff --check` — exit 0, no output.

## Fix round 2

### Latest re-review findings

- R1 — FIXED: schedule publication now writes the production enum value `info`; the publication transaction can reach `published` and queue its employee notifications.
- R2 — FIXED: the published-shift change trigger also writes `info`. `scripts/tests/077_daily_clarity_automation.sql` now publishes a seeded week, edits an assigned shift, and asserts both the published state and `time_changed` record.
- R3 — FIXED: availability reminders use `info`, and both notification and outbox inserts check their Supabase errors before any success count is returned.
- R4 — FIXED: concrete-response re-ranking counts draft suggestion hours and excludes employees with overlapping draft suggestions, including other headcount seats.
- R5 — FIXED: concrete-response re-ranking excludes recurring `employee_availability.typ='gesperrt'` windows as well as date exceptions. The fallback reason is only emitted after both checks pass.
- R6 — FIXED: department responsibility eligibility again calls `responsibility_assignment_active_at` with validity dates, weekday scope and shift time.
- R7 — FIXED: the core engine remains authoritative for shifts without concrete responses; the extension only reconsiders shifts having `shift_availability_responses`, so core results are no longer universally discarded.
- R8 — FIXED: board severity takes the maximum of structured conflicts and ArbZG warnings; a legal `error` can no longer be downgraded by a soft conflict.
- R9 — FIXED: customer order/delivery mail keeps the original “bei uns bestellt” footer; only schedule operations opt into the employee/operation footer.
- R10 — FIXED: schedule-mail links use `${ctx.origin}/mitarbeiter`, producing an absolute URL in mail clients.
- R11 — FIXED: `schedule_template_slots` stores the parent tenant id on every create/copy path, allowing `unified_audit_change` to attribute slot audit rows to the tenant.
- R12 — FIXED: the stable instance key now includes end time and slot name in addition to template, weekday, start, position, seat and week.
- R13 — FIXED: the Berlin wall-clock deadline is converted explicitly to an instant with `Intl`-derived Berlin offsets; the browser's local timezone no longer controls writes.
- T1 — FIXED: the lock-order regression assertion now inspects `generate_weekly_shift_assignment_suggestions_core`, where both the advisory lock and `for update skip locked` row locks live.

### Required evidence

- `bash /home/openclaw/factory/workspaces/codex-builder/runs/mise-neo/migration-dryrun.sh supabase/migrations/20260831130000_schedule_planning_loop.sql` — exit 0:
  `restore: 0 errors (extension/duplicate noise expected), tables=243`
  `OK   20260831130000_schedule_planning_loop.sql`
- `./node_modules/.bin/vitest run --configLoader runner tests/scheduling/planning-contract.test.ts tests/scheduling/planner.test.ts` — exit 0: `Test Files 2 passed (2)`, `Tests 16 passed (16)`, duration 953ms.
- `./node_modules/.bin/vitest run --configLoader runner` — exit 0: `Test Files 36 passed (36)`, `Tests 238 passed (238)`, duration 5.86s.
- `./node_modules/.bin/tsc --noEmit -p .` — exit 0, no diagnostics.
- `./node_modules/.bin/next build` — exit 0: compiled successfully and generated 227/227 static pages; existing repository lint warnings remain non-fatal. The first exact invocation encountered the known read-only generated `.next/standalone/node_modules` symlink; removing that disposable build artifact and rerunning the exact command succeeded.
- `git diff --check` — exit 0, no output.
